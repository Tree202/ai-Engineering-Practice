# 待修清单 —— 教程 ↔ 内容 一致性

## 状态:14 件**全部修完**(2026-09-03 第二批)

上一版列的 14 件事(A 组 4 / B 组 3 / C 组 5 / D 组 2)已逐条落地并验过。
下面按条记录**改在哪一侧、依据是什么**,以及**施工过程中翻出来的、上一版没有的 6 条**。

六道门禁在改完后全绿:`check_site` / `check_claims` / `check_layout` /
`check_crossrepo` / `build_zip --check` / `tools/tests`(24 条)。
`myshop` 侧改了 `.gitignore` 与 `pyproject.toml`,`myshop-source.zip` 已重打
(14 条目 / 24969 字节,SHA-256 见 `变更清单.md`)。

> **这份清单现在是空的 —— 没有已知未修项。**
> 下次做一致性比对,把新提出的条目写在这里,格式照旧:`文件:行号` + 两侧原文。

---

## A 组 · 上一轮自己造的(4 件)

### A1 · 开关横幅宣称「页面里另给 PowerShell 版本」而 06 页零处 —— **已修**
按建议的修法 ①:**给 06 页那两条 `curl` 各补一个 PowerShell 版本**,横幅措辞不动
(它的承诺现在兑现了)。两条命令都在**本机 PowerShell 5.1(5.1.26100.7920)对着真的
`myshop.api` 跑过**,不是照着文档编的:

- 正常下单 → `Invoke-RestMethod -Uri … -ContentType 'application/json; charset=utf-8' -Body '…'`
  实测返回 `¥25.00`,中文完好。
- 畸形请求 → PS 5.1 把 400 **抛成异常**,不 `try/catch` 就看不到响应体。实测可用的一行是
  `try { Invoke-RestMethod … } catch { "$($_.ErrorDetails.Message) <- HTTP $([int]$_.Exception.Response.StatusCode)" }`,
  输出 `{"error": "请求体不是合法的 JSON"} <- HTTP 400` —— 与页面上 curl 那行**逐字相同**。
- 顺带核了 01 页那条 `curl -fsSL … | bash`:它**本来就**在同一个块里给了
  `irm … | iex`,横幅的承诺在那一页原本就成立。全书 `curl` 只有这三处。
- 附带记一笔实测细节:`Invoke-RestMethod` 会把 JSON **解析成对象**,打印出来不是 JSON 行;
  想要原样 JSON 行得用 `(Invoke-WebRequest -UseBasicParsing …).Content` ——
  `-UseBasicParsing` 在 5.1 上不能省(不加会因为 IE 引擎未初始化而失败)。两点都写进页面了。

### A2 · 改写规则三个地方三个数 —— **已修,但实际条数不是清单说的 7**
清单说「实际改写 7 类」。逐行读 `_cmdos.js` 后**实际是 8 类**:`toWin` 里 7 条行内替换
(`VAR=值` 前缀、`.venv/bin/python`、`python3`、`which`、`touch`、`mkdir -p`、`ls -a`)
**加上 `chainAnd` 的 `A && B` 重写**。清单漏算了 `chainAnd` 那一条。

三处已按 8 条对齐:横幅(原 6 条)、`02-python-setup.html` 那个自称权威的对照框(原 5 条,
把缺的四条并进 ⑤「其余六条小命令对照」,编号仍是五条不动)、`toWin` 上方新加的注释
(写明「三处口径必须一致,改这里就要改那两处」——上一版就是漏了这一步)。

两条没被记录的规则**都真的会触发**,各一处:`ls -a` 在 `02-python-setup.html:866`
的验收命令里,`VAR=值` 前缀在 `06-myshop.html:593` 的 `PYTHONPATH=. …`。

### A3 · `det_collision.py` 自述「五条规则」实为六条 —— **已修**
`tools/layout_rules/det_collision.py` 文件头「五条规则」→「六条规则」。
六条各产出一种 kind,都已落地,复核无误。顺带 `grep -rn 五条 tools/` 确认无别处残留
(唯一另一处命中在 `fixtures/baseline/01-overview.html`,那是**逐字节的历史快照**,不动)。

### A4 · `_cmdos.js` 头注「72 处」对不上任何数法 —— **已修,并写成可复算的**
没照抄清单的 60,自己写了核算脚本复算:**把 `_cmdos.js` 里的 `chainAnd`/`toWin` 原样
取出来**(不复制、避免漂移),对 23 页每个 `<pre><code>` 块的文本节点回放一遍。
改动前:164 块 / **60 块被改写** / 94 处行级命中 —— 与清单的 60 吻合,「72」三个数都不是。

**改完之后数字变了**,头注写的是改完的值:**166 块 / 59 块被改写 / 93 处行级命中**
(+2 块是 A1 给 06 页补的两个 PowerShell 块,它们不被改写;−1 块是 11 页那条
`cd … && git commit` 按下面 B1 改成了不带 `&&` 的单命令)。
头注里连数法一起写进去了,并加了一句「动了页面里的命令就要重跑这个数法再改这里」。

---

## B 组 · 诚信类(3 件)—— 三个修法都由用户当场定

### B1 · 16 页「真机实测」的提交说明被改写 —— **已修:把真话贴回去**
16 页三处按 `git -C myshop show -s --format=%s` 的原文改回:
`docs: 门禁演示 · 好代码应该被放行`(命令行 + `[main b27d579]` 回显 + 场景 2 的
`git log --oneline -1` 回显,三处一致)、`feat: 我绕过了门禁,坏代码就这么进来了`。
场景 1 顺带补上真实的 ` 1 file changed, 2 insertions(+)`。

**清单这一条自己错了一处**:它说 11 页那个框的 stat「与 b27d579 真实的 4 files changed
对不上」。实测 `b27d579` 的 stat **就是** `1 file changed, 2 insertions(+)`(改的是
`README.md`);`4 files changed, 26 insertions(+), 1 deletion(-)` 是 `4c5dd7c` 的。
**11 页的 stat 一直是对的,错的只有提交说明。**

11 页真正的问题比清单说的更清楚:那一页第六、七节的终端输出**本来就声明了**来自
另一个更小的练习仓库(`:497` 的 note 框:「那不是 myshop,是我另外建的一个三步小练习」),
而 `b27d579` 是**全页唯一一个真实存在的哈希** —— 另外六个(`1a1b5c7`/`31db09a`/
`74412a5`/`9f3c1a2`/`11b15b1`/`738c813`)在 myshop 里都不存在。也就是说这里是
**把一个真实的 myshop 提交借进了别的例子**。已归还:命令与回显改成该练习仓库自己的
那一步(`git commit -m "feat: 第1步 读取分数"` → `[main 74412a5] …`),
与同页下方 `git log --oneline` 里的 `74412a5 feat: 第1步 读取分数` 对上了。
(`:631` 那张「提交说明好坏对照表」里的 `feat: 第1步 price 支持满减计算` 保留 ——
它是**写法**示例,与它并列的 `fix: 货币符号改回 ¥` 也来自 myshop,不是本节的输出。)

### B2 · 19 页第 6 幕的合并形态与真实相反 —— **已修:按真实改成 `--merge`**
依据 `git -C myshop log --merges`:留下的是**双亲合并提交** `afa3103`
(`Merge pull request #1 from Tree202/feat/broken-price`,body 是 `把货币符号改成 $`),
与 `--squash` 形态相反。改了四处:

| 处 | 原 | 现 |
|---|---|---|
| `:247` 第 2 幕回显 | `[feat/broken-price a1c9e34] feat: 货币符号改成美元` | `[… fb4094e] feat: 货币符号改成 $(这是个 bug,门禁应该拦住)` |
| 同上的命令行 | `-m "…"`(双引号) | `-m '…'`(**单引号** —— 说明里带 `$(…)`,双引号下会被当命令替换) |
| `:293` 第 6 幕 | `gh pr merge 1 --squash` / `✔ Squashed and merged … (改货币符号)` | `gh pr merge 1 --merge` / `✔ Merged … (把货币符号改成 $)` |
| `:373` 第 5 幕 | `gh pr merge 1 --squash --admin` | `gh pr merge 1 --merge --admin` |

`a1c9e34` 用 `git cat-file -t` 确认**不存在**;`fb4094e` 的 stat
`1 file changed, 1 insertion(+), 1 deletion(-)` 与页面上写的一致,没动。
合并请求标题取自 `afa3103` 的 commit body(GitHub 默认把 PR 标题放在那里)。
页脚那句「所有终端输出均为真机原样抄录」后面**加了一段如实登记**,写明改了哪三处、
依据哪条 `git log`,读者可以在公开仓库里自己复算。
`references/sources.md` 里引用这几条的 4 行**一并回灌**(登记表自己不能留过期证据)。

> 没做的:没去公开仓库重开 PR 重跑一遍。PR #1 已合并、无法重来,且那会改动公开仓库。

### B3 · 18 页「真机输出」的格式不可能来自同一次运行 —— **已修:用干净 venv 真跑**
另建了一个**只装 `pytest==8.4.2`、不装 playwright** 的 venv(= 全书基线设定的默认状态),
在 `myshop/` 下跑 `python -m pytest -q`,终端宽度 80,实录:

```
======================== 17 passed, 1 skipped in 3.89s ========================
```

**三条按文件分行与百分比,与页面上原有的逐字相同**;耗时 3.89s 对页面写的 3.90s。
也就是说这个框原本是真的,**唯一的缺陷就是 summary 行丢了 `=` 包边**(24 个 `=` 各一侧)。
已补上并把耗时换成重录值;`c-ok`/`c-warn` 的着色结构保留(页面要突出那个 `1 skipped`),
箭头那行的缩进同步右移 25 列,仍指在 `skipped` 上。
改完做了一次机器比对:把该框去掉标签后与实录文件逐行对照,**四行全部逐字一致**。

同族回灌:`grep` 了全书所有 pytest summary 行,07 页 `:582`、06 页 `:586` 等
本来就是「整行一个 `c-ok` span、带 `=` 包边」的写法,自洽,未动。

---

## C 组 · 教程与项目不同步(5 件)

### C1 · 07 页 SKIPPED 行号 `:23` → `:28` —— **已修**
两种数法都做了:`grep -n importorskip` 看全部命中(`:26` 是含该字样的**注释**,
`grep | head -1` 会数成 26 —— 这个坑清单提醒过)、以及**用干净 venv 实跑
`pytest -q -rs`**,真实 SKIPPED 行确实是 `:28`。只改了行号:07 页那个框是 macOS 录的
(路径用 `/`、耗时 3.66s),没顺手改成 Windows 的反斜杠路径。
全站 `grep test_checkout_e2e.py:` 无别处写行号。

### C2 · 07 页 `test_order.py` 少了 `@pytest.mark.p0` —— **已修**
把该块抽出来做 HTML 反转义后与真实文件 `diff`,**完整差异是 6 处**:5 处是注释/docstring
去术语化(sqlite→数据库、HTTP→网络、fixture→运行器自带的准备工作、connect→重新连接、
INSERT→写库),**唯一的可执行代码差异就是那个装饰器**。
(清单说「其余 4 处」,实为 5 处。)已连注释一起补上:
`@pytest.mark.p0  # 第 18 页:P0 = 挂了就是生产事故的那一条链路` —— 与真实文件那行逐字相同,
且它自带的「第 18 页」前向引用正是全书的惯例,不会让 07 页读者一头雾水。

同页另两块顺带核了:`test_price.py` 只有 1 处注释差异(HTTP→网络),无可执行差异;
`test_api.py`(`:323` 标了「节选」)与 e2e(`:493` 标了「自 import 起与项目文件逐字一致;
模块 docstring 与写法二略」)两处限定都名副实。

### C3 · 11 页的 `.gitignore` 与真实文件四处全不一致 —— **已修(改项目),但先揪出教程里一个真 bug**
方向确认是清单说的那样(教程那个框自带「这份清单经核实后做了四处改进」的表,是**有意**
更新过的;项目没跟)。但**照抄之前先实测了一遍,发现教程那份自己有一处失效写法**:

```
myshop.db          # 本项目跑测试会生成它
```

**`.gitignore` 只认「整行以 `#` 开头」的注释,不支持行尾注释。** 在一个临时仓库里实测:
这样写时 `git status` 里 `myshop.db` 仍是 `?? myshop.db`、`git check-ignore` 不匹配;
把注释挪到单独一行后才真的被忽略。**而 `myshop.db` 此刻就在工作区里**(跑过接口就会生成)——
要是把教程那份原样抄进项目,`myshop` 的「工作区干净」纪律当场就破了。
已先修教程(注释挪到单独三行,并把这个坑本身写进去当教学点),再据此同步项目。

`myshop/.gitignore` 现已与教程那一份一致:`*.py[codz]`、补 `env/ ENV/`、
补 `.env.*` / `!.env.example` / `.envrc`、`*.db` → `*.sqlite3` + 单独一行的 `myshop.db`、
段名 `# ---- PyCharm ----` → `# ---- 编辑器 ----`。
**删掉了 `.DS_Store` 两行** —— 同页 `:431` 就在教「`.DS_Store` 建议放全局,不要放项目里」,
项目自己留着它属于不吃自家狗粮。

改完实测四项:`myshop.db` 被忽略(命中新的第 25 行)、`.env.local` 被 `.env.*` 命中、
`test.sqlite3` 被命中、**`.env.example` 正确地没被忽略**(反向放行生效)。
`git status` 干净,只剩我改的两个文件。

### C4 · 02 页「整个粘进去」的 pyproject 与真实差两段 —— **已修,但两处方向与清单相反**
先把 02 页那个块抽出来与真实文件做了完整 `diff`,再逐处判断该改哪一侧:

| 差异 | 清单的暗示 | 实际改在哪 | 依据 |
|---|---|---|---|
| `[project.optional-dependencies] / dev = […]` | 补进教程 | **从项目里删** | 教程 `02:306`、`myshop/README.md:26/32`、`ci.yml` 三处装工具用的**都是** `pip install pytest mypy ruff`,**没有一处**用 `-e ".[dev]"`;全书 grep 该词命中 0。留着它只会让读者去跑一条全书没教过的命令 |
| `check_untyped_defs = true` | 补进教程 | **从项目里删** | 02 页 `:469` 有一个专门的框写着「`strict = true` **已经包含** `check_untyped_defs`,两个都写是冗余的」,并且明确纠正「防『AI 漏写 `-> None`』靠的是 `disallow_untyped_defs`,不是它」;04 页 `:457` 再讲一遍。**而项目里那行的注释写的正好是被教程否掉的那个理由。** 教程是对的,项目在跟教程对着写 |
| markers 文案 | 对齐 | **改教程** | 真实文件的「p0: 核心链路,挂了就是生产事故」与 `18-gate-items.html:225` **逐字一致**(三方里的多数);02 页原写「p0: 核心用例,挂了就不能合并」,且那个逗号是全角。已改成多数那份 |
| `02:857`「做到第 18 页还会往里追加 markers 段」 | 独立失实 | **改教程** | 第五节那份**已经有** markers。改成「里面已经含…到那一页不用再加一遍 —— TOML 里重复键是错误」 |
| 连带:`18:220` 的指引 | 清单没提 | **改教程** | 原文「在你已有的块里追加 markers」会让照做的读者写出重复键。改成「第 2 页那份里已经有了,这里再贴一次只是给你看它长什么样;还没有的话才追加,已经有了别再加一遍」 |

删两段后重跑:`pytest` 17 passed / 1 skipped、`mypy Success: no issues found in 10 source files`、
`ruff All checks passed` —— 三道全绿,删的确实是冗余。

### C5 · 06 页目录树注 `__init__.py`「空文件」 —— **已修**
实测 77 字节、一行 docstring。改成「标志「这是一个包」,本项目里是一行说明文字」
(注释在目录树最后一列,不影响对齐)。
02 页 `:858` 那句「可以是空文件」核过,是对的,没动。
全站 grep「空文件」余下三处均无误(`conftest.py` 那条是在讲一种**可选做法**,
项目本来就没有根 `conftest.py`;两处 `tmp_path` 说的是空文件**夹**)。
另:`tests/__init__.py`、`tests/e2e/__init__.py` 都**不存在**,目录树也没列,一致。

---

## D 组 · 工具/姊妹课自述与实际不符(2 件)

### D1 · `layout_rules/README` 的调参分布加不到 95 —— **已修,并把复算法写进去**
没照抄清单的结论,用 `det_dangling_edge` **自己的 `SvgScan`** 重算了一遍 23 页:

```
带箭头端点总数: 95
0px×28  2px×9  4px×49  6px×6  8px×1  48px×2      合计 95
其中被条件 2(另一端 otherd > ANCHOR_TOL=8)排除的: 2
    00-index.html   第 205 行  终点 47.01px,另一端 63.25px
    01-overview.html 第 593 行  终点 47.01px,另一端 63.25px
参与判定的 93 个,到最近节点的最大距离: 8.0px
```

确认就是清单说的那两条(47px,四舍五入落进 48px 桶)。README 改成:95 个里 2 个被
「另一端必须贴住某个节点」那条判据挡在判定之外,**参与判定的 93 个**分布是 0/2/4/6/8、
最大值正好 8.0px,所以「容差 8px 卡在 8.0 与 10.8 之间」这个推导成立 ——
并把复算法(import 哪个模块、用哪个解析器、怎么分桶)一起写下。

**同族一处清单没提**:`det_dangling_edge.py` 自己的 docstring(`:26`)也写着
「端点到最近节点的距离**全部** <= 8.0px」—— 对 93 个成立、对 95 个不成立。已同步限定。

### D2 · kb/00 说 `style.css` 含三套主题变量 —— **已修**
`kb/style.css` 实测只有两组:`:6` 的 `:root{`(浅色)与 `:20` 的
`html[data-theme="dark"]{`(深色),**无** `prefers-color-scheme`。
「跟随系统」由 `kb/app.js` 折算成 light/dark 再写 `data-theme` ——
`kb/07-case-study.html:486` 自述「`<html data-theme>` 上写的永远只有 light 或 dark」佐证。
目录树那行改成「共用样式（含两套主题变量：浅色 + 深色）」。
全站 grep「三套/三态」确认这是唯一一处。

---

## 这一轮翻出来、上一版清单没有的 6 条

1. **11 页 `.gitignore` 的行尾注释是失效写法**(见 C3)—— 唯一一条**功能性**的真 bug,
   实测定罪。要是照清单「把项目同步到教程」一把抄过去,反而会把配套项目搞脏。
2. **清单 B1 把两个提交的 stat 记混了**(见 B1)—— 11 页的 stat 一直是对的。
3. **11 页 `b27d579` 是全页唯一真实哈希**,被借进了另一个练习仓库的例子(见 B1)。
4. **清单 C4 有两处方向是反的**(见 C4)—— 按清单的暗示改教程,会把教程自己
   已经查证过并写进正文的立场推翻。
5. **A2 的实际条数是 8 不是 7**、**C2 的注释差异是 5 处不是 4 处**(见 A2 / C2)。
6. **本机 `myshop/.venv` 现在装了 playwright**,直接 `pytest -q` 得到的是 `19 passed`,
   不是全书基线的 `17 passed, 1 skipped`。**这不是缺陷** ——
   E2E 的 docstring 写明了「装完基线从 17 passed, 1 skipped 变成 19 passed」;
   但**下次谁在这台机器上复算基线会被绊一下**,要用不装 playwright 的干净 venv。
   本轮 B3/C1 的实录就是这么做的。

---

## 落地状态与还差什么

- **教程仓库**:14 个文件已改(含重打的 `myshop-source.zip`),六道门禁全绿。
- **配套仓库 `myshop`(公开,`Tree202/myshop-gate-demo`)**:`.gitignore` 与
  `pyproject.toml` 的改动**只落在工作区,没有提交、没有推送**。
  这两条要真正生效(以及被 `cross-repo` 门禁看见)还需要:
  1. 在 `myshop` 里提交并走 PR 合进 `main`(`main` 受保护,须走 PR);
  2. 按 `tools/myshop.pin` 自己的规矩,**有意识地**把 pin 更新到新提交 ——
     否则 CI 复核的还是旧状态。
- 门禁仍然查不到的那一类(行号、文案、「是不是原样抄录」)没有变化;
  上一版给的三条堵法(给「真实输出」框加可复算标注、把「整份文件」代码块纳入
  `check_crossrepo` 逐行比对、把 `.gitignore`/`pyproject.toml` 纳入内容比对)**仍未实施**。
  本轮 C3/C4 两条都是这类漏网的实例 —— 值得下一轮优先做第三条。

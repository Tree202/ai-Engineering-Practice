# 待修清单 —— 教程 ↔ 内容 一致性(2026-09-03 全量比对)

七个只读维度扫描 + 每条对抗复核。**提出 25 条,成立 21、推翻 4;去重后 14 件事。**
一条都还没修。下面每条都带 `文件:行号` 与两侧原文,可以冷启动直接施工。

比对方法与已知刻意差异见 `变更清单.md` 顶部「当前状态」节。
被推翻的 4 条(06 页「三个实用技巧」表是示意非逐字、目录树漏列 `.gitattributes` 属漏列非说错、
pyproject 两处差异是教学分段)**不要再报**。

---

## A · 我在 2026-09-03 这一轮自己造的(4 件,优先修)

### A1 · 开关横幅在骗读者 —— curl 那句 **P1**
- 教程:`ai-workflow/_cmdos.js:47-50` 横幅原文「…以及 `curl` 这类**没有等价单命令**的调用
  (PowerShell 5.1 里 `curl` 是 `Invoke-WebRequest` 的别名,参数完全不同 —— **页面里另给 PowerShell 版本**)」
- 现实:`ai-workflow/06-myshop.html:597` 与 `:606` 两条 `curl` **没有任何 PowerShell 版本**
  (`grep -c 'Invoke-RestMethod\|curl.exe' ai-workflow/06-myshop.html` = 0)
- 修法二选一:① 给 06 页那两条补 `Invoke-RestMethod` 版本(可复用 `myshop/README.md` 里实跑过的、
  带 `charset=utf-8` 的那条);② 改横幅措辞,不再宣称「页面里另给」。**建议选 ①**

### A2 · 改写规则三个地方三个数 **P2**
- 横幅列 6 条;`_cmdos.js` 实际改写 **7 类**(多出 `VAR=值 前缀`→`$env:` 与 `ls -a`→`ls -Force`,
  见 `_cmdos.js:96` 与 `:102`,两条都真的会触发);
  `ai-workflow/02-python-setup.html:263` 那张自称「权威说明 / 完整对照」的表只有 **5 条**
- 修法:以 `_cmdos.js` 的实际改写为准,三处对齐

### A3 · `det_collision.py` 自述「五条规则」,实际六条 **P2**
- `tools/layout_rules/det_collision.py:7`「五条规则(全部经过全站 23 页实测调参…)」,
  紧接 `:9-14` 列的是 R1–**R6**。加 R6 时改了列表、没改这句话
- 六条都已落地:`:322 / :357 / :398 / :423 / :570 / :597` 各产出一种 kind

### A4 · `_cmdos.js` 头注「72 处」对不上任何数法 **P2**
- `_cmdos.js:5-6`「164 个可复制命令块,里面 72 处是 macOS/Linux 专用写法」
- 实测:164 与 63(纯文本块)都对;但按 `toWin` + `chainAnd` + heredoc/shebang 跳过规则逐块回放,
  实际被改写的是 **60 个块**。「72 处」既不是块数也不是命中数
- 修法:改成可复算的口径(建议写「60 个块」并注明数法)

---

## B · 诚信类:自称「原样抄录」而实际做过美化(3 件,最要紧)

> 这三条与其余不同 —— 其余是「过期」,这三条撞在本书自己的核心主张上。
> **修法需要先做一个决定**:是重新跑一遍把真输出贴回来,还是改掉「原样抄录」的措辞。

### B1 · 16 页「真机实测」的 git 提交说明被改写 **P2**
- 教程:`ai-workflow/16-pipeline.html:380/388/404`「docs: 好代码应该被放行」;
  `:409/411`「我绕过了检查,坏代码就这么进来了」
- 现实:`git -C myshop show -s --format=%s b27d579` → 「docs: 门禁演示 · 好代码应该被放行」;
  `4c5dd7c` → 「feat: 我绕过了门禁,坏代码就这么进来了」
- 哈希与 `--stat`(`4 files changed, 26 insertions(+), 1 deletion(-)`)**都对得上**,只有文案不同
- 加重情节:同一个哈希 `b27d579` 在 `ai-workflow/11-git-basics.html:577` 配了**第三种**文案
  「feat: 第1步 price 支持满减计算」
- 削弱情节:这两个提交对象在任何 ref 下都不可达(读者 clone 拿不到),哈希本就是装饰

### B2 · 19 页第 6 幕的合并形态与真实相反 **P1**
- 教程:`ai-workflow/19-gate-demo.html:293-294` `gh pr merge 1 --squash` /
  `✔ Squashed and merged pull request …#1 (改货币符号)`;`:609` 页脚
  「本页所有终端输出均为**真机原样抄录**……合并请求 #1 已合并」
- 现实:`git -C myshop log --merges` → `afa3103 Merge pull request #1 from Tree202/feat/broken-price`,
  双亲 merge commit,与 `--squash` 形态相反;PR 标题是「把货币符号改成 $」不是「改货币符号」
- 另:`:247` 那个哈希 `a1c9e34` 在仓库里不存在

### B3 · 18 页「真机输出」格式不可能来自同一次运行 **P2**
- 教程:`ai-workflow/18-gate-items.html:299` 标「真机输出(Windows 重录…)」,
  `:300-303` 是按文件分行(verbosity 0),而 `:304` 的 `17 passed, 1 skipped in 3.90s` 是**裸行、无 `=` 包边**
- 现实:`myshop/pyproject.toml:16` `addopts = "-v --strict-markers"`;`-v` + 命令行 `-q` = verbosity 0,
  而 pytest 的 `summary_stats()` 在 `verbosity >= 0` 时**一定**走 `write_sep("=", ...)`。两半对不上

---

## C · 教程与项目不同步(5 件)

### C1 · 07 页 SKIPPED 行号 `:23` 已过期 → 应为 `:28` **P1**
- 教程:`ai-workflow/07-four-layers-code.html:581`
  `SKIPPED [1] tests/e2e/test_checkout_e2e.py:23: E2E 需要:…`(该框 `:575` 标「真实输出」)
- 现实:`myshop/tests/e2e/test_checkout_e2e.py` 的 `pytest.importorskip(` 在**第 28 行**
  (AST 实测:调用起始行 28、结束行 31。注意第 26 行是含 "importorskip" 字样的**注释**,
  用 `grep | head -1` 会数成 26 —— 我第一次就数错在这里)
- 断代:`git show d804e80^:…` 里在第 23 行;PR #6 给 docstring 补了 Windows 路径后移到 28
- 三条分发口径(工作树 HEAD / `tools/myshop.pin` / `myshop-source.zip`)现在**都是 28**

### C2 · 07 页 `test_order.py` 少了 `@pytest.mark.p0` **P2**
- 教程:`ai-workflow/07-four-layers-code.html:203` 代码块**无「节选」标注**,
  而 `:114` lede 写「把四个测试文件的**完整代码**摊开」;同页确为节选的两块都标了限定(`:323`、`:493`)
- 现实:`myshop/tests/test_order.py:31` `@pytest.mark.p0  # 第 18 页:P0 = 挂了就是生产事故的那一条链路`
- 该块与真实文件其余 4 处差异都是注释去术语化(可接受),**唯一的可执行代码差异就是这个装饰器**

### C3 · 11 页的 `.gitignore` 与真实文件四处全不一致 **P1**
> 方向与其余相反:**教程更新了、项目没跟**

| 教程 `11-git-basics.html:389-413` | 真实 `myshop/.gitignore` |
|---|---|
| `*.py[codz]` | `:3` `*.py[cod]` |
| `.venv/ venv/ env/ ENV/` | `:9-10` 只有 `.venv/` `venv/` |
| `.env` `.env.*` `!.env.example` `.envrc` | `:13` 只有 `.env` |
| `*.sqlite3` + `myshop.db` | `:16` `*.db`,无 `myshop.db` 行 |

- 另:同页 `:431-435` 建议 `.DS_Store` 放全局别放项目里,而 `myshop/.gitignore:18-19` 里就有
- 门禁零覆盖:`grep gitignore tools/*.py` 只命中 `build_zip.py:35`(打包清单),无内容比对

### C4 · 02 页「整个粘进去」的 pyproject 与真实差两段 **P2**
- 教程 `ai-workflow/02-python-setup.html:398`「把下面的内容**整个粘进去**」(无「节选」标注)
- 真实 `myshop/pyproject.toml` 多出:`:8-10` 整段 `[project.optional-dependencies] / dev = [...]`
  (全书 grep「optional-dependencies」命中 **0** 次)、`:30` `check_untyped_defs = true`
- 且 markers 文案不同:教程 `:415`「p0: 核心用例,挂了就不能合并」 vs 真实 `:19`「p0: 核心链路,挂了就是生产事故」
  (真实那份与 `18-gate-items.html:225` 逐字一致)
- **附带一条独立的失实**:`:857` 验收清单写「(做到第 18 页还会往里追加 markers 段)」——
  第五节那份**已经有** markers,读者照 `18:220`「在你已有的块里追加」做会写出重复键

### C5 · `__init__.py`「空文件」 **P2**
- 教程:`ai-workflow/06-myshop.html:158` 目录树注「标志「这是一个包」,空文件」
- 现实:`myshop/myshop/__init__.py:1` 是一行 docstring
  `"""myshop:一个用来演示「测试金字塔」的迷你网上商店。"""`(77 字节)
- 另:`ai-workflow/02-python-setup.html:858` 写「可以是空文件」,那句是对的,不用改

---

## D · 工具/姊妹课自述与实际不符(2 件)

### D1 · `layout_rules/README` 的调参依据数字对不上 **P2**
- `tools/layout_rules/README.md:15-17`「统计了…全部 **95 个**带箭头端点…分布是
  0px×28、2px×9、4px×49、6px×6、8px×1…容差 8px 正好卡在两者之间」
- 实测(用 `det_dangling_edge.py` 自己的解析器重算):端点确为 95,但列出的分布只加到 **93**,
  漏掉 **47px×2**(那两个被 `ANCHOR_TOL` 条件排除,所以不影响结论,但「8px 卡在两者之间」的推导写得不准)

### D2 · kb/00 说 `style.css` 含三套主题变量 **P2**
- 教程:`kb/00-index.html:200`「├── style.css            共用样式(含三套主题变量)」
- 现实:`kb/style.css` 只有两组 —— `:6` `:root{`(浅色)与 `:20` `html[data-theme="dark"]{`(深色);
  「跟随系统」由 `kb/app.js` 折算成 light/dark 再写 `data-theme`
- 佐证:`kb/07-case-study.html:486` 自述「`<html data-theme>` 上写的永远只有 light 或 dark」

---

## 这一类为什么门禁查不到

14 件里 **9 件**是任何现有门禁都查不出来的:行号、文案、「是不是原样抄录」、
「这句话引用的那张表里有没有这条规则」—— 都不是数字对数字。

可考虑的堵法(尚未实施):
- 给每个「真实输出」框加一个可复算标注(对应哪个提交 / 哪条命令),让 `check_crossrepo` 能复核
- 把 07 页那类「整份文件」代码块纳入 `check_crossrepo` 的逐行比对(允许注释差异,不允许代码行差异)
- `.gitignore` / `pyproject.toml` 纳入教程 ↔ 项目内容比对

# 变更清单（兼作 CHANGELOG）

- 产出目录：`D:\ext.zhaoliuliu3\Desktop\claude_AI\`（教程副本 + myshop 副本 + 工具 + 本清单）
- 只读源（零写入，经修改时间抽查确认）：`Desktop\ai-workflow\`、`Desktop\myshop_claude\`、`learn_claude\ai-workflow\`、`*_codex\`
- 远程备份：`ai-Engineering-Practice`（私有，分支 `main` / `feat/depmap`）；myshop 用它自己的 `myshop-gate-demo`（公开）
- 共两轮：

| 轮次 | 日期 | 内容 | 提交 |
|---|---|---|---|
| **v2.1 勘误与可用性** | 2026-09-01 | 26 项勘误 + myshop 整备 + Windows 适配 + 基础设施；131 处编辑落在 19 个教程页 + myshop/README.md | `7fe23e1`（`main`） |
| **v2.2 依赖地图** | 2026-09-01 | 依赖真相表裁定 + 按需弹出的地图 + 00-index 静态图重建 + `scroll-padding-top` 修正 | `d381a3d`、`9639178`（`feat/depmap`） |
| **v2.3 页码可点** | 2026-09-01 | 273 处「第 N 页」文字引用改成链接 | `611ad04` |
| **v2.4 重核与重画** | 2026-09-01 | 13 页 Claude Code 逐条重核（撤销一处误改）、16 页 @v7 复核、14/17 两张流程图重画 | `63059f5`、`200b522` |
| **v2.5 示例漂移** | 2026-09-01 | 09/11 页的终端输出与正文项目对不上，如实标注并消除矛盾 | `main` |
| **v2.6 待修清单清零** | 2026-09-03 | `references/待修清单.md` 那 14 件全部落地(含 B 组三件诚信类);施工中另揪出 6 条,其中 11 页 `.gitignore` 行尾注释是失效写法 | `main` |

- 贯穿原则：23 页结构不动、全书真实输出数字不动、教学设计（monkeypatch / importorskip / checkout 还原法）不动。

---

## 一、事实修正（已定罪错误）

| 页 | 修正 |
|---|---|
| 12-git-undo | `git switch nosuchbranch` 的「(退出码 1)」→ **(退出码 128)**，该行绿色样式 `c-ok` → `c-bad`；页脚修正计数 5→4（switch/restore 实验标签一条与 11 页重复计数，已加括注说明） |
| 22-cheatsheet | 「144 = 128+16 是被手动终止」→ 「POSIX(macOS/Linux)环境下，130 = 128+2 是被 Ctrl+C 中断、143 = 128+15 是被 SIGTERM 终止」 |
| 14-workflow | 步骤 4 自查命令 `git diff --staged --name-only`（该时点暂存区必空）→ **`git status --short`**（同时暴露修改与未跟踪新文件） |
| 02-python-setup | `python_functions=["test_*"]` 配置与 `testnounderscore PASSED` 演示互斥 → 修正框补说明「演示基于默认配置，本页配置已收窄」；回顾表第 37 行加注 |
| 13-claude-code | 「权限模式共六种（含 auto）」→ **官方四种**（default/acceptEdits/plan/bypassPermissions）；auto 是 acceptEdits 开启时的状态栏文案，非独立模式；删「从 auto 起步/按三下」说法及 SVG 中 auto 方框；dontAsk 行保留并加「版本相关」注；页首加「以官方文档为准」免责框 |
| 20-ai-boundary | 「机器能可靠防住的只有三种(3、4、7)」→ 「能检测或部分抑制的有四种(2、3、4、7)」+ 弱门禁限定；连带「剩下四种」→「剩下三种」 |
| 21-automation | CLAUDE.md 示例命令重排为 ruff → mypy → pytest -m p0 → pytest（原顺序与「便宜的先跑」注释相反）；「三条命令」→「四条命令」 |
| 08-break-it | 「实验④全量测试救不了你」加前提「（在 AI 已篡改测试的场景下）」（正句 + 表格两处），消除与同页第五节实测的矛盾 |
| 05-pyramid | 金字塔 SVG「0.06 秒」→「0.02 秒」（与同页实测表一致） |
| 04-static-check | ruff 真实输出框两条诊断配「Found 3 errors.」→ 诊断间补占位行「...(另 1 条诊断略)」 |

## 二、跨页口径统一

| 主题 | 修正 |
|---|---|
| 「删用例能否被机器防住」 | 20/21/22 三页统一为**弱门禁**口径：用例总数门禁防纯删除、防不住「删一条补一条空测试」的语义替换，diff 落在测试文件必须人工读 |
| 用例总数门禁补正式条目 | 18 页门禁清单新增「用例总数 · 不得少于主干(弱门禁)」行（阈值门禁 rowspan 6→7，知识点 182 行「6 条」→「7 条」）；「分母漏洞」对策由「配跳过数门禁」改为「注释/删除式配用例总数门禁、@skip 式配跳过数门禁」（含知识点 185 行联动） |
| 18↔19 名实不符 | 18 页「这三条正是第 19 页配的三条」→「19 页配的是规范+类型+全量测试；换成 P0 100% 是推荐升级版」 |
| 18 页 p0 示例 | 示例测试名改为项目真实存在的 `test_订单真的落盘了_换个连接还读得到`（代码体取自真实 tests/test_order.py），加注「已有测试打标记即可，不需要新写」 |

## 三、引用与编号修正

| 页 | 修正 |
|---|---|
| 19-gate-demo | 「第 18 页那个三阶段落地路径」→ **第 17 页**（经 Grep 核实）；「27 秒是绿的」补口径（建仓推送那次运行；坏代码那次是红的） |
| 22-cheatsheet | 待验证项「第 17 页第七节」→ **第八节**；术语表「合并请求首见 19」→ **首见 16**（含链接 href 同步） |
| 16-pipeline | 删除悬空承诺「第 17 页会对照讲 Jenkins」；两环境 3.66s 对比表加脚注「耗时为 macOS 实测值，Linux 红绿计数一致、耗时未逐字核录」（19 页同款表同步加注） |
| 08-break-it | 「第 5 页那个换连接读回来技巧」→「第 5 页给结论、第 7 页给代码」 |
| 01-overview | 正文五处「第五件事」→「第六件事」（与 SVG 的 ⑤=门禁、⑥=看改动统一）；「管理员推不开」补 enforce_admins 前提；「12 个字符」加注（含前导空格，指 ` --no-verify`） |
| 14-workflow | 全景 SVG「⑤循环/⑥总审」→「⑥/⑦」（与正文步骤一致），连带修正「③–⑤主循环」→「③–⑥」与易跳边「⑤→⑥」→「⑥→⑦」 |
| 07-four-layers-code | 层级编号统一：TOC/标题/对比表中「②接口/③E2E」（与集成重号）→「③接口/④E2E」共 6 处（代码 docstring 本已正确，未动） |
| 02-python-setup | 回顾表末尾两行残渣汇总行（29–31/35–37 区间与描述互换且重复）删除 |
| 00-index | 02 卡片知识点区间补全为「9–23、29–31、35–37」；17/19/21 卡片星级按页内实测同步（★★★★/★★★★/★★☆） |

## 四、限定与背景补充（半句级）

- 02 页：新增「Python 3.9 已于 2025-10 结束官方支持，新装请用当前稳定版；教程代码按 3.9+ 兼容语法书写」背景段。
- 05/06/07 页：「差约 180 倍」结论处各加「（本项目本机实测值，不可外推到其他项目）」。
- 07 页：E2E 代码块说明加「项目文件中另有不用 fixture 的写法二」；06/07 页「(节选)」处加「完整代码见配套 myshop 项目」。
- E2E 维持 `importorskip` 的 1 skipped 基线（「完整 E2E 环境」列为 v3 议题）。

## 五、Windows 适配

- **00 页**前提改为「macOS / Linux / Windows 电脑（命令以 macOS 写法为基准，Windows 对照见第 2 页）」；项目路径指向 `D:\ext.zhaoliuliu3\Desktop\claude_AI\myshop`（附通用写法）。
- **02 页**新增「Windows 读者对照（全书唯一一处）」note 框：`.venv/bin/python` ↔ `.venv\Scripts\python.exe`、激活分 PowerShell `Activate.ps1` / CMD `activate.bat`（并重申写全路径原则）、`touch`/`mkdir -p` 的 PowerShell 对应、`$env:PYTHONUTF8="1"` 乱码处理。
- **22 页**页首加一行指回 02 页对照框。
- **路径去个人化**：全书 70 处 `/Users/daijie27/...` 中 69 处（命令输入行与正文叙述）改为 `cd <你的项目>/myshop` 占位写法、「在 myshop 项目根目录执行」框注或相对路径；**1 处保留**——11 页 L194「真实输出」框内 `Initialized empty Git repository in /Users/daijie27/...`（真实输出记录按约定原样保留）。`.venv/bin/` 写法（90 处）按方案整体保留，由 02 页对照框统一交代。
- 02 页 `touch __init__.py` 前补 `mkdir -p myshop tests/e2e`（原命令在父目录不存在时会失败）。

## 六、myshop 项目整备（claude_AI\myshop\）

- 从 `myshop_claude\myshop\` 复制（源码/测试/配置/README/ci.yml/**完整 .git 历史**），排除 macOS `.venv`、三类缓存、`.DS_Store`。
- 新建 Windows venv，钉住 **pytest 8.4.2 / mypy 1.19.1 / ruff 0.16.3**（与教程实测版本一致）。
- README 更新：教程指向同级 `ai-workflow/`（原指向第一版四份 HTML）、快速开始补 Windows 命令、耗时注明平台、加乱码提示。源码与测试文件**零改动**。
- `.git` 隐私速查（只查不改）：远程为教程 19 页的公开演示仓库 `github.com/Tree202/myshop-gate-demo`，提交作者邮箱为用户本人邮箱——无隐私风险，历史未动（6 条提交含 $→¥ 门禁演示序列）。

### 验收结果（本机 Windows · Python 3.12.10）

| 项 | 预期（教程） | 实测 | 结果 |
|---|---|---|---|
| 基线 | 17 passed, 1 skipped | 17 passed, 1 skipped in 4.69s | ✅（计数一致；耗时属平台差异） |
| mypy | Success…8 source files | 同 | ✅ |
| ruff | All checks passed | 同 | ✅ |
| 实验① 去补零（仅 test_price） | 3 failed, 2 passed | 同 | ✅ |
| 实验② 换符号（全量） | 8 failed, 9 passed, 1 skipped | 同 | ✅ |
| 实验③ 除数 100→10（全量） | 6 failed, 11 passed, 1 skipped | 同 | ✅ |
| 实验④ 边界挪（全量） | 1 failed, 16 passed, 1 skipped（DID NOT RAISE） | 同 | ✅ |
| 实验⑤ 注释 commit（全量） | 3 failed, 14 passed, 1 skipped | 同 | ✅ |
| 五次实验中 ruff/mypy | 始终全绿 | 全绿 | ✅ |
| API 契约 | POST→total_text ¥25.00 / GET 200 / 404「订单 N 不存在」 | 逐键一致 | ✅ |
| 还原纪律 | 每次实验后回到基线、工作区干净 | 确认 | ✅ |

## 七、基础设施件

- `tools/check_site.py`（纯标准库）：链接/锚点/重复 id/导航/标题检查。**运行结果：正式 23 页 0 个问题**；`_v1` 历史页 18 个失效链接（历史存档原貌，不计入、未修改）。
- `myshop-source.zip`：白名单纯净源码包，现 14 条目、0 禁止项、**19.9 KB**(历史版本 13 条目/14.9 KB)（对比原 myshop.zip 约 150MB 脏归档）。SHA-256 见附录。
- `references/sources.md`：本次修正依据的官方来源与核验日期登记（轻量版）。
- ~~**CI 硬化未做**（`ci.yml` 被教程 18 页逐字引用，改动会破坏教程↔项目一致性）~~ **这条描述本身有两处错，2026-09-03 查证后作废**：①**没有任何一页逐字贴过** `ci.yml`——第 16、18 页贴的都是变体，变体方向还相反；②**漏了第 16 页**，那一页在结构上反而更接近真实文件。我照这条错误描述判断了好几轮。
  正确的结论(依据见「工程底座逐项裁决」一节)：
  · `permissions` / `concurrency` / `timeout-minutes` —— 全书**零引用**，加了教程一个字不用动。**已做**。
  · action 钉 SHA —— 会直接推翻第 16 页「过期陷阱」框与第 18 页规矩 2 的教学立场。**不改配置，改成第 18 页第九节的一行取舍说明**。已做。
  · 钉 pytest/mypy/ruff 版本 —— 会毁掉第 16、19 页那两张环境对照表(靠的正是本地旧版 vs 云端最新)。**同上，写成取舍**。已做。
  · pip 缓存 —— 三个包装完不到 6 秒，缓存省不下什么。不做。

### A 组四项(用户点名「全部都做」)

#### A2 · 第 14 页 Ctrl+G —— 全书唯一一条没有证据的功能断言,查完发现是**错的**

这条挂账了好几轮:「按 `Ctrl + G` 可把**计划**拉进编辑器直接改」,一直没有一手佐证。
WebFetch `code.claude.com/docs/en/interactive-mode` 拿到原文:

> `Ctrl+G` or `Ctrl+X Ctrl+E` — **Open in default text editor**.
> Edit **your prompt or custom response** in your default text editor.

**它编辑的是你正在输入的内容,不是 Claude 出的那份计划。** 官方另有一个可选项
(`/config` 里的 Show last response in external editor)能把上一条回复以 `#` 注释垫在
你的输入上方,但**保存时会被剥掉** —— 能照着写,不能直接改。

第 13 页三处(正文框、快捷键表、知识点 141)与第 14 页一处已改正,`sources.md` 两条标为已核实。

#### A3 · `check_site` / `check_claims` 的独立测试套件(24 条)

先给 `check_claims` 做可测化:原来整段是**模块级顶层代码**,末尾是裸 `sys.exit` ——
**任何 import 都会直接终止进程**,根本没法写测试。收进 `run_checks(root)`、参数化根目录、
失败清单改成局部(原来是模块级全局,同进程跑两次会累加)。逻辑一行没动。

测试的准入门槛是「在修复前的版本上必须红」:
- `test_check_site.py` 15 条 —— 在 `b918f6f` 的旧版上 **8 条失败**
- `test_check_claims.py` 9 条 —— 在旧版上**连 import 都过不去**(那正是改造要解决的)

顺带修了 `read()` 的文件句柄泄漏(测试在同进程反复调用时刷 ResourceWarning,
Windows 上还会让 tempdir 清理失败)。

#### A4 · 跨仓库一致性门禁 `tools/check_crossrepo.py`

补的是登记多轮的那条:教程与 myshop 是**两个独立仓库**,任何一边单独改,另一边毫无反应。
`check_claims` 里那节 myshop 实数比对在云端**恒跳过**(myshop 被 gitignore),
它诚实地打印了「跳过」,但那几条断言在门禁上**零覆盖**。

新脚本在 CI 里 clone 公开的 myshop,核 18 条:测试条数、源码行数、CI 任务名
(教程 16 处引用它,改了会和分支保护脱钩)、三道门禁的命令、四层文件是否齐全。
拿不到配套项目时**退 2 而不是装作通过**。

**变异验证五种,全部按预期变红**:配套加一条测试、配套源码加一行、改 CI 任务名、
删掉 mypy 门禁、删掉 `web.py`。

**其中两种当场抓出我自己写的判据有问题**:
- 「删掉 mypy 门禁」第一次**仍报绿** —— 判据写成 `run:.*mypy`,
  把 `run: pip install pytest mypy ruff` 那一行也算上了,**整步删掉都测不出来**。
  改成锚在行首。
- 「删掉 web.py」第一次是**崩溃**而不是报失败 —— 行数计算先于文件存在性检查,
  直接 FileNotFoundError,后面整组检查都跑不到。改成缺文件报成一条。

CI 从四步扩到**六步**(+检查器测试 +跨仓库),并给教程仓库的 workflow 也补上了
`permissions` / `concurrency`(此前只有 myshop 那边有)。

#### A1 · 131 条高风险断言复核 —— 103 成立 / 10 判过期 / 18 无法核实,而**对抗复核推翻了 10 条里的 5 条**

13 批只读代理对着一手来源逐条核(Claude Code 官方文档、GitHub docs 与 GraphQL schema、
cli/cli 源码、PyPI、python.org、Playwright 官方文档),判为过期的再过一轮对抗复核,
默认立场「你判错了」。**10 条里 5 条被推翻** —— 50% 的推翻率,正是这一步存在的理由。

**被推翻的 5 条(断言其实仍成立)**

| 原判 | 为什么推翻 |
|---|---|
| 13 页 `Esc Esc` 的两段行为 | 原判把「第一次/第二次」误读成「两下 Esc 里的第一下/第二下」;教程同页另两处复述锁死了指代 |
| 19 页 `gh pr checks` 输出 | 原判拿「非 TTY 表头」去比,而那个分支**根本不打表头**;能打出 `fail` 字面量的恰恰是它 |
| 16 页 / 00 页「文档落后两个大版本」 | 原判说 checkout 只落后一个。但 GitHub docs 的版本号来自两个 reusable 文件,提交历史显示 checkout 是 2026-04 才升到 v6、setup-python 停在 2024-04 的 v5 —— 断言仍成立 |
| 21 页「新开会话保留旧会话」 | 官方 `/clear` 条目里 `/new` 就是它的别名,且明确说会保存上一段对话 —— 与教程说法一致 |

**存活的 5 条(已全部改正)**

| 页 | 官方原文 | 改法 |
|---|---|---|
| **13** | 「Directory references show file listings, **not contents**」 | 教程写的是「把某个文件**或目录**的内容读进上下文」。目录只带回**文件清单**。同页 `读一下 @myshop/ 和 @tests/` 这个示例受影响最直接 —— 那两行进上下文的是文件名,不是代码。已改正并补了官方的另一条:`@` 引文件会连带把各级 `CLAUDE.md` 加进上下文 |
| **14** | 「Claude responds to the command output **automatically** once it lands in the transcript … Before v2.1.186, shell mode always added output to context without a response」 | **这条最要紧**:第 4 步「你自己看改动」的全部立意是「输出直接进你的眼睛,别让 AI 评价自己的作业」,而 v2.1.186 起默认行为**恰好反了过来**。已补:想要原语义得在 `settings.json` 里设 `"respondToBashCommands": false` |
| **19** | cli/cli 源码 `merge.go` 的打印格式含 `owner/repo` 与 PR 标题 | 教程那行终端输出缺了这两段。已补全 |
| **16** | python.org / actions/python-versions:3.12 的当前补丁版已是 3.12.14 | 环境对照表写死 3.12.13。改成 `3.12.x` 并说明「配置写的是 `'3.12'`,云端每次取当时最新补丁版」—— **不再绑死一个必然过期的数字** |
| **07** | `web-first assertions` 今天仍在官方 Python 文档的 Handles 页与 v1.18 发行说明里 | 教程说「官方已经不用这个说法了」,过头了。改成:动作侧的官方术语是 auto-waiting,而 web-first assertions **并未废弃**,两个词指的是两件事(断言会重试 vs 动作前等元素可操作) |

**18 条 unverifiable 的性质**:多数是作者本机的一次性实测(某次运行耗时、某台机器的输出),
以及无法从外部验证的经验性论断。代理没有把它们判成 ok —— 这一点是对的,
**取不到一手来源就不该说「核过了」**。


## 第二次全量复核(2026-09-03)—— 39 条自查 + Codex 10 条,全部处理完

**方法上和之前几轮的区别:扫现状,不扫 diff。** 本项目「改了一处、没改同一事实的其他落点」
已复发七次,每次都是只盯当轮改动。所以这次派 9 个只读代理各扫一个维度的**全部**落点,
每个维度的定罪再由独立代理反驳(默认「误报」)。提出 46 条,成立 39,推翻 7。
随后 Codex 又给了 10 条,逐条核实**全部成立**,其中 6 条是我方扫描漏掉的。

### 把「第 18 页那份配置 ↔ 真实 ci.yml」也变成门禁

上一条记的那两处漏改暴露了一个更根本的问题:**没有任何东西在比这两份配置**。
第 3、4 节只查了任务名和三条门禁命令跑没跑,而第 18 页第九节是「可以直接抄」的
**完整配置** —— 读者抄下来的那份,应该和第 19 页真机演示跑的那份是同一个骨架。

`check_crossrepo.py` 新增第 4b 节,抽出两边的**骨架事实**逐项比(不比逐字):
顶层键集合、`permissions: contents: read`、`concurrency` 的分组与 `cancel-in-progress`、
`timeout-minutes`、任务名、`runs-on`、触发条件、actions 版本、Python 版本、`pip install` 装的工具、
以及门禁命令集合。

**两处刻意差异写进判据,不当缺陷报**:第 18 页多一道「门禁 3 · P0 核心用例」
(第 19 页用「三道检查版」这个限定语说明过),step 的 `name` 措辞两边不同。
后者的做法是:门禁命令只比 `run:` 的内容,不比 `name:`。

**变异验证五种,全部按预期报红、还原后归绿**:真实 `ci.yml` 令牌改 `write`、
真实超时改 30、第 18 页删掉 `cancel-in-progress`、真实把 `mypy` 换成 `mypy --version`、
第 18 页动作版本改 `@v6`。其中第四种同时被第 4 节和第 4b 节各抓一次 —— 判据有重叠是好事。

### 用户问「教程跟上了吗」—— 查出两处基础设施改了、正文没跟

用户的问题是对的:这一轮大量改动落在**配套项目与工具**上,而教程正文是不是跟着说了,
是另一回事。逐条对照后有两处没跟上,其中一处是我自己造的**引用断裂**。

#### ① `_cmdos.js` 的横幅引用了一张不包含该规则的表(P0,我造的)

改完 `&&` 的安全问题后,开关生成的是 `A; if ($?) { B }`,横幅也这么写,并且说
「已按第 2 页第二节那张对照表改写」。但**那张对照表第 ③ 条根本没有这条规则** ——
它写的是「三条出路:**分两行分别跑(最稳)**、用 `;` 连接、或装 PowerShell 7」。

两个问题叠在一起:横幅引用的表里没有它宣称的规则;而那张表推荐的「分两行」
**正是我因为安全问题刚刚废弃的做法**,还标着「最稳」。

已重写第 ③ 条:首选 `A; if ($?) { B }`(给出三条命令的嵌套写法),PowerShell 7 仍可直接用 `&&`,
并加警告说明为什么不能拆两行、也不能只用 `;` —— 拿书里真实存在的
`cd <你的项目>/myshop && git reset --hard HEAD` 当例子:`cd` 失败时后面那条会在当前目录执行。

#### ② 第 18 页第九节那份「可以直接抄」的配置里没有那三样(P1)

上一轮给配套项目的 `ci.yml` 加了 `permissions` / `concurrency` / `timeout-minutes`,
第 18 页只在表格**下方的说明段**提了一句「配套项目已经带上了」,**YAML 块本身没有**。
而第 19 页 :138 说「门禁配置:第 18 页第九节那份 `ci.yml` 的三道检查版」——
「三道检查版」这个限定语能解释「少了 P0 那一步」,解释不了「少了三行加固」。

已把三样写进第九节的 YAML(各带一行注释说明为什么),说明段改成「已经写进上面那份配置里了」,
并指向第 19 页。现在读者抄下来的那份,与真机演示跑的那份,除了刻意的三道/四道差别之外一致。

#### 顺带确认已经跟上的

- 第 2 页第五节的 `pyproject.toml` 已含 `markers` 段(上一轮改的)
- 第 16 页那份是「讲流水线三段结构」的变体,不是「拿去抄」的配置,不加固合理,与第 18 页不构成矛盾

### 加完门禁当场自查:我在修「静默报绿」的过程中又造了一个同族缺陷

`build_zip.py --check` 挂进 CI 之后我立刻自查了一件事:**它在 CI 里到底查不查得到东西?**
答案是查不到 —— `myshop/` 被 `.gitignore`,CI 的 checkout 里根本没有它,于是白名单里
每个文件 `os.path.exists()` 都为假、全部 `continue`,最后打印「发布物与 myshop/ 一致」退 0。

**一道刚加上去、专门用来防漂移的门禁,在真正要用它的环境里是空转的。**
而这正是我这一整轮在修的那一族缺陷(check_layout 缺样本、check_site 缺目录、
det_dangling_node 缺基线)—— 我在修它的过程中,又新造了一个。

修法:`--check` 接受目录参数(CI 传 clone 下来的 `_myshop`);目录不在退 2、
白名单文件在磁盘上缺失也报错,不再 `continue`。三种情形各验一次:
无目录 → 2、指到 `_myshop` → 0、删掉一个文件 → 1。

顺带把 `tools/myshop.pin` 跟到 PR #6 合并后的 `6fa2a36` —— 按 pin 自己写的规矩,
myshop 更新时要**有意识地**改这一行。

### 最重的三条

**① `&&` 拆行是安全缺陷(Codex 发现,我造的)。** 上一轮做 Windows 开关时,把
`A && B` 拆成两行,横幅里只写了「拆行后语义变了」当说明。实际后果:全书 12 条形如
`cd <你的项目>/myshop && git clean -fd` 的命令,`cd` 一旦失败(路径打错、还没克隆),
**破坏性命令就在读者当前所在的目录里执行**。实测确认了这个路径:PowerShell 里 `cd`
到不存在的目录后,当前目录停在 `claude_AI` 本身 —— 也就是说 `git clean -fd`
会落在教程仓库自己身上。改为 `A; if ($?) { B }`(右结合嵌套),真实 PowerShell 5.1
验过:`cd` 失败时后续命令不执行,成功时执行。

**② 发布物已经漂移。** `myshop-source.zip` 4/14 条目过期(README、ci.yml、api.py、web.py),
mtime 09-02 22:12 而 Windows 修复在 09-03 02:02 之后 —— 读者按 `index.html` 那条路径
拿到的是**修 Windows 之前**的版本。`build_zip.py` 文件头自述要防的正是这个事故。
根因是它只有「重打」没有「校验」,只能靠人记得跑。新增 `--check`(逐条目比对 zip 与磁盘 SHA),
挂进 `cross-repo` 作业。**这个门禁加上几分钟后就抓住了我自己改 `pyproject.toml` 造成的新漂移。**

**③「当前状态」快照本身是错的。** 上一版自称「看这一节就够」,而 A3、A4 做完后它没被更新:
三项「仍然开着」里两项早已关闭,门禁步数同一件事写了三个数(快照四步 / 注释五步 / 正文六步)。
已整节重写,**每一句都标了核到哪个文件**,方便下次直接复算。

### 其余成立项

| 组 | 内容 |
|---|---|
| 跨仓库门禁(Codex) | 判据只查 `run:` 后跟工具名 —— 换成 `ruff --version` 照样绿。改为锚定命令形状并排除 `--version`;CI 原来 clone myshop 当时的 main,同一教程提交今天绿明天可能红,新增 `tools/myshop.pin` 钉住提交。两条各有变异验证 |
| Ctrl+G(A2 我做错了) | 官方两页说的是两个场景:`interactive-mode` 是改输入,`permission-modes` 明写「open the **proposed plan** … and edit it directly」。上一轮只核了第一页,四处一律写成「改的不是计划」。已改为双语境 |
| Windows 走读 | 02 页让读者「整个粘进去」的 `pyproject.toml` **不含 markers**,覆盖配套项目那份后从第 6 页起收集阶段就报错 —— 已补 markers 段;`echo $?` 补 PowerShell 的 `$LASTEXITCODE`;`curl` 明确列入「开关不改写」并在横幅说清 |
| 页内矛盾 | 05 页同页两套圈码(已统一为 接口③ / E2E④);06 页 SVG 接口测试与集成测试重号;12 页「四种措辞」实为三种(四处);13 页知识点 137 与正文五条对齐;21 页「恰好就是唯一不能外包」不成立;16 页关卡 ① 图注说能跳表格说跳不掉;02 页 conftest 钩子指向 18 页(那页零处钩子) |
| 数字残留 | 3.12.13 两处、00 页 06 卡片 224/150 |
| 交叉引用 | 「钩子」两处锚点都指到 kb/05 `#s3`(那是 CLAUDE.md 写法),改 `#s1` |
| kb | kb/04 还把 `context: fork` 当分身(上轮改了 06/07 漏了 04);kb/06 工具那格把「后台收窄」放错了列 —— 官方豁免的恰恰是分身 |
| 07 页(Codex) | A1 我改的那句「前者/后者」写反了,下一段又是对的,段内自相矛盾 |
| 18 页(Codex) | 「没人升的 SHA 比浮动标签更危险」过度绝对。改引 GitHub 官方两侧原文:SHA 是唯一不可变引用;代价是 Dependabot 不再提醒 |
| 工具静默报绿(第五、六、七处) | `check_layout` 缺样本跳过不计失败、0 页扫完退 0;`check_site` 整个 `kb/` 目录消失静默跳过;`det_dangling_node` 缺样本打印「不当作通过」却仍返回 0 |
| 登记表(Codex) | 131 条复核只有汇总数进了变更清单,`sources.md` 查不到逐条结论。已加「2026-09-03 复核」列,表格自己就能复算出 108 / 5 / 18 |
| myshop | README 分支说明指向 `docs/windows-readme`(它是 main 的**严格祖先**,领先 0 提交);快速开始只建空 venv 不装工具,下一行必崩;E2E docstring 说依赖「都已经装好」且只给 macOS 路径 |

### 被推翻的 7 条(误报,没动)

「合并保护规则/分支保护」两名并用(19 页有明文桥接)、变更清单里的历史记录被当成现状、
20 页「三种/四种」谓词不同、18 页 p0 待办是教学刻意分段、21 页 `/clear` 表(该页根本没出现这命令)、
13 页 `/init` 指 21 页(括号注的是「项目配置文件」)、「工具行号引用已全部错位」(抽核仍对得上)。

### 对 Codex 那份评估的裁决

10 条全部成立,无一误报。**其中 3 条是我这个会话自己新造的**(`&&` 拆行、07 页前者后者、
kb/06 工具那格)—— 共同点都是「修 A 时顺手写了 B,B 没核」。
两处我不同意的:它说跨仓库门禁应做成「双向」—— 依赖是单向的(教程对配套做断言,反之没有),
且教程仓库私有,公开仓库的 CI 拿不到凭据;它说「tag 只是便利性折中」把 GitHub 的话说少了一半,
官方同时写明钉 SHA 之后 Dependabot **不再提醒安全更新**,这个代价不能抹掉。

### 教训

第八次复发,而且这次的重灾区是**我自己写的现状说明**。对策已经写死在流程里:
**任何一处改动落地后,拿改前的原句 grep 全站**(23 页 + kb + index + README + 变更清单 + sources.md),
残留必须为 0。本轮 17 条原句逐一验过,全部归零。

### 第四轮:把「待修清单」那 14 件清零

三件 B 组(诚信类)的修法由用户当场定:**把真话贴回去 / 按真实改成 `--merge` / 用干净 venv 真跑**。
其余 11 件按清单直接施工。全部编辑都走「先校验 `old` 片段在文件内唯一、再统一写盘」,
33 处编辑一次通过,没有半途落盘的中间态。

#### 三件最值得记的

**① 照清单盲抄会把配套项目搞脏 —— `.gitignore` 不支持行尾注释。**
C3 要求「把 `myshop/.gitignore` 同步成第 11 页那一份」。抄之前先在一个临时仓库里实测了
那一份,发现教程自己有一处**失效写法**:

```
myshop.db          # 本项目跑测试会生成它
```

`.gitignore` 只认「整行以 `#` 开头」的注释。这样写时 `git status` 里 `myshop.db` 仍是
`?? myshop.db`、`git check-ignore` 不匹配;把注释挪到单独一行后才真的被忽略。
**而 `myshop.db` 此刻就在工作区里**(跑过接口就会生成)—— 原样抄过去,
`myshop` 的「工作区干净」纪律当场就破。已先修教程(并把这个坑本身写成教学点),再同步项目。
这是本轮唯一一条**功能性**的真 bug,清单里没有。

**② 清单本身错了四处,方向反了两处。**
- B1 说 11 页那个框的 stat 与 `b27d579` 真实的「4 files changed」对不上 ——
  实测 `b27d579` 的 stat **就是** `1 file changed, 2 insertions(+)`(改的是 `README.md`),
  4 files 那个是 `4c5dd7c` 的。**11 页的 stat 一直是对的,错的只有提交说明。**
- C4 暗示把 `[project.optional-dependencies]` 与 `check_untyped_defs = true` **补进教程**。
  实际两处都该**从项目里删**:装工具的命令在教程、README、CI 三处用的都是
  `pip install pytest mypy ruff`,**没有一处**用 `-e ".[dev]"`;而 02 页 `:469` 有一个
  专门的框写着「`strict = true` 已经包含 `check_untyped_defs`,两个都写是冗余的」,
  还明确纠正了「防『AI 漏写 `-> None`』靠的是 `disallow_untyped_defs`」——
  **项目里那行的注释写的正好是被教程否掉的那个理由。** 按清单改会把教程自己查证过的立场推翻。
- A2 的实际条数是 **8** 不是 7(清单漏算了 `chainAnd` 的 `&&` 重写);
  C2 的注释差异是 **5** 处不是 4 处。

**③ 两个「可复算」的数字都自己重算了,没照抄清单。**
- A4:把 `_cmdos.js` 里的 `chainAnd`/`toWin` **原样取出来**(不复制,避免漂移),
  对 23 页每个 `<pre><code>` 块的文本节点回放。改动前 164 块 / **60 块被改写** / 94 处命中,
  与清单的 60 吻合;「72」既不是块数也不是命中数。**改完之后数字变了**,头注写的是改完的值
  166 / 59 / 93,并把数法一起写进去 + 一句「动了页面命令就要重跑这个数法再改这里」。
- D1:用 `det_dangling_edge` **自己的 `SvgScan`** 重算 95 个箭头端点的分布,
  确认漏掉的是 00/01 页时间轴那两条(47.01px,被「另一端必须贴住节点」的判据排除)。
  README 改成「95 个里 2 个被排除,**参与判定的 93 个**分布 0/2/4/6/8、最大 8.0px」,
  推导这才站得住。**同族一处清单没提**:`det_dangling_edge.py` 自己的 docstring
  也写着「距离**全部** <= 8.0px」,对 93 成立、对 95 不成立,已同步限定。

#### B 组三件的落地

| | 修法 | 依据 |
|---|---|---|
| B1 · 16 页提交说明 | 三处按 `git show -s --format=%s` 的原文改回,场景 1 顺带补真实 stat | `b27d579` = `docs: 门禁演示 · 好代码应该被放行`;`4c5dd7c` = `feat: 我绕过了门禁,坏代码就这么进来了` |
| B1 · 11 页 | `b27d579` 是**全页唯一真实哈希**,被借进了另一个练习仓库的例子(该页 `:497` 本来就声明「那不是 myshop」)。已归还成该练习仓库自己的 `74412a5 feat: 第1步 读取分数`,与同页 `git log` 框对上 | 另六个哈希 `git cat-file -t` 全部不存在 |
| B2 · 19 页 | `--squash` → `--merge`、回显 `Squashed and merged` → `Merged`、标题改真、`a1c9e34` → `fb4094e`;第 5 幕 `--admin` 一并改;命令行改用**单引号**(说明里带 `$(…)`,双引号下会被当命令替换) | `git log --merges` 显示留下的是双亲合并提交 `afa3103`;`a1c9e34` 用 `cat-file` 确认不存在 |
| B3 · 18 页 | 另建**只装 `pytest==8.4.2`、不装 playwright** 的 venv(= 全书基线设定的默认状态)实跑 `pytest -q` | 实录 `======================== 17 passed, 1 skipped in 3.89s ========================`;**三条按文件分行与百分比与页面原有的逐字相同**,唯一缺陷就是 summary 行丢了 `=` 包边 |

B2 的页脚加了一段**如实登记**(改了哪三处、依据哪条 `git log`),
并把 `references/sources.md` 里引用这几条的 **4 行一并回灌** —— 登记表自己不能留过期证据。
B3 改完做了机器比对:该框去掉标签后与实录文件逐行对照,**四行全部逐字一致**。

#### A1 补的 PowerShell 命令是真跑过的,不是照文档编的

横幅承诺「`curl` 这类没有等价单命令的调用,页面里另给 PowerShell 版本」,而 06 页零处。
补之前先启了真的 `myshop.api`,在**本机 PowerShell 5.1(5.1.26100.7920)**上跑通两条:

- 正常下单用 `Invoke-RestMethod -ContentType 'application/json; charset=utf-8'`,返回 `¥25.00`,中文完好。
- 畸形请求:PS 5.1 把 400 **抛成异常**,不 `try/catch` 就看不到响应体。可用的一行是
  `try { … } catch { "$($_.ErrorDetails.Message) <- HTTP $([int]$_.Exception.Response.StatusCode)" }`,
  输出与页面上 curl 那行**逐字相同**。
- 两个实测细节也写进页面了:`Invoke-RestMethod` 会把 JSON **解析成对象**(打印的不是 JSON 行),
  想要原样 JSON 行得用 `(Invoke-WebRequest -UseBasicParsing …).Content`,而 `-UseBasicParsing` 在 5.1 上不能省。

顺带核实:全书 `curl` 只有三处,01 页那条本来就在同一个块里给了 `irm … | iex`,那一页的承诺原本就成立。

#### 一个会绊倒下一个人的环境事实

**本机 `myshop/.venv` 现在装了 playwright**,直接 `pytest -q` 得到的是 `19 passed`,
不是全书基线的 `17 passed, 1 skipped`。这**不是缺陷** —— E2E 的 docstring 写明了
「装完基线从 17 passed, 1 skipped 变成 19 passed」;但下次谁在这台机器上复算基线会被绊一下。
本轮 B3/C1 的实录都是用不装 playwright 的干净 venv 做的。

#### 验收

| 项 | 结果 |
|---|---|
| `check_site` / `check_claims` / `check_layout` | 全绿(正式 23 页 0 问题) |
| `check_crossrepo` | 全绿(29 项,含 18 页配置 ↔ 真实 `ci.yml`) |
| `build_zip --check` | 先如实报出我改的 2 处漂移(`pyproject.toml`、`.gitignore`),重打后一致 |
| `tools/tests` | 24 passed |
| `node --check` | `_cmdos.js` / `_depmap.js` 均通过 |
| myshop 三道门禁(删两段配置之后) | `17 passed, 1 skipped` / `Success: no issues found in 10 source files` / `All checks passed` |
| `myshop-source.zip` 重打 | 14 条目 · 24969 字节 · SHA-256 `1380B4C5FB470E219BD7E2F2564E6247596193A9C54DAFF18A0F1F3F559E7AC2` |
| 全站回灌 | 20 条改前原句逐一 grep,残留全部为 0(唯一命中是 `sources.md` 那 4 行,已同批回灌) |

#### 过程中的一处教训

准备用 14 个并行代理做逐条对抗复核,**14 个全部被 API 529/Overloaded 打回**,
一个 token 都没用上。改成自己逐条做:读两侧原文、写核算脚本、实测定罪。
好处是意外的 —— 上面那四处「清单本身错了」和 `.gitignore` 那个真 bug,
都是在**动手抄之前先实测一遍**的过程里撞出来的。

## 当前状态(2026-09-03,第二次全量复核后重写)

这份清单是**编年史**,越往下越旧。想知道「现在到底是什么状态」,看这一节。

> ### ✅ 那 14 件已全部修完 —— 记录在 `references/待修清单.md`
>
> 2026-09-03 先做了一次「教程 ↔ 内容」全量比对(提出 25 条、成立 21、去重后 14 件),
> **同日全部落地**:A 组 4 / B 组 3(诚信类,三个修法由用户当场定)/ C 组 5 / D 组 2。
> 六道门禁在改完后全绿。**目前没有已知未修项。**
>
> 那份清单现在记的是「改在哪一侧、依据是什么」,以及施工中另翻出来的 **6 条**
> (其中 11 页 `.gitignore` 的行尾注释是**失效写法** —— 唯一一条功能性真 bug,
> 照着清单盲抄反而会把配套项目搞脏)。**下一轮比对提出的新条目继续写在那里。**

> 上一版这一节自己就过期了 —— 三项「仍然开着」里两项早已关闭,门禁步数同一件事写了三个数。
> 一份自称权威的现状说明本身失准,比不写更糟。所以这一版的每一句都对着文件核过,
> 并在下面标了**核到哪个文件**,方便下次直接复算。

**两个仓库**

| | 门禁 | 核到哪 |
|---|---|---|
| 教程 `ai-Engineering-Practice`(私有) | `checks` 工作流两个作业:`site`(5 步:链接与结构 / 口径 / 版式含真值自检与终点列变异锁 / 六个规则模块自测 / 检查器自己的测试)+ `cross-repo`(3 步:取配套项目、教程↔配套一致性、发布物一致性) | `.github/workflows/check.yml` |
| 配套 `myshop-gate-demo`(公开) | `质量门禁` 三道:ruff / mypy / pytest;`main` 受分支保护,须走 PR | `myshop/.github/workflows/ci.yml` |

**已了结、不再是债的**

| 事 | 核到哪 |
|---|---|
| 根 CI 覆盖版式(两页真值样本随仓库发布) | `tools/layout_rules/fixtures/` |
| 六个 `det_*.py` 不再写死作者机器路径;缺样本不再静默算通过 | `tools/layout_rules/_baseline.py`、各模块自测入口 |
| `check_site` 空目录报绿 / 跨目录锚点从未校验 / 导航检查名不副实 | `tools/check_site.py` |
| `check_claims` 循环零匹配仍报绿;卡片正则漏数字 | `tools/check_claims.py` |
| `det_dangling_edge` 逐元素吞异常 | 现报成 `svg-parse-error` 检出 |
| `build_zip.py` 失败会摧毁旧包 | 临时文件 + 原子替换 |
| **发布物漂移**(zip 与 myshop 不同步) | 新增 `build_zip.py --check`,已挂进 `cross-repo` 作业 |
| **跨仓库门禁不可复现**(原来取 myshop 当时的 main) | 新增 `tools/myshop.pin`,CI 钉住提交 |
| 检查器自己的独立测试套件(24 条) | `tools/tests/`,已进 CI |
| Windows 读者跑不起来 | `myshop/README.md` 双平台;`api.py` / `web.py` 编码保护 |
| **`&&` 拆行的安全缺陷** | `_cmdos.js` 改为 `A; if ($?) { B }`,`cd` 失败时后续命令不执行 |
| 131 条高风险断言复核 | `references/sources.md` 第五节逐条带复核结论 |
| **14 件教程 ↔ 内容不一致**(2026-09-03 第二批全部落地) | `references/待修清单.md` 逐条记着改在哪一侧 |
| **19 页的合并形态**(原写 `--squash`,真实是双亲合并提交 `afa3103`) | `19-gate-demo.html` 第 6 幕 + 页脚的改正登记;`sources.md` 四行已回灌 |

**裁定不做(有依据,不是拖着)**

| 项 | 为什么 |
|---|---|
| 三类确定性发布包 + manifest | 23 页对发布打包零内容、私有仓库无消费者。Codex 需要它是因为**它的交付物就是一堆 zip、没有仓库** |
| Actions 钉 SHA | 会推翻第 16 页「过期陷阱」框与第 18 页规矩 2 的教学立场。**已写成第 18 页第九节的取舍说明**(含 GitHub 官方两侧原文) |
| 钉 pytest/mypy/ruff 版本 | 会毁掉第 16、19 页那两张环境对照表(靠的正是本地旧版 vs 云端最新)。同上 |
| windows-latest 矩阵 | 检查名会变成 `三道检查 (ubuntu-latest)`,与分支保护 `contexts` 失配 —— 正是第 19 页警告的「不报错、只是永久等待」;且 CI 不装 playwright,任何平台都是 17 passed,**买不到信息** |
| 合并四套 SVG 解析器 | 重复但都工作正常,属重构,收益低 |
| 跨仓库**双向**门禁 | 依赖是单向的(教程对配套做断言,反之没有);且教程仓库私有,公开仓库的 CI 拿不到凭据 |

**仍然开着的**

1. **131 条高风险断言的下一次复核** —— 本轮已跑过一遍(见 `sources.md` 第五节的复核列),
   建议每半年一遍。这是维护动作,不是缺陷。
2. **pytest 9.1 `importorskip` 收紧** —— 观察项。
3. **`myshop.pin` 需要人跟** —— myshop 更新后要有意识地改那一行,否则门禁核的是旧状态。
   这是钉住换来的代价,不是缺陷。
4. ~~**`myshop` 侧那两个文件还没提交**~~ —— **已了结。** `myshop/.gitignore` 与
   `myshop/pyproject.toml` 走 **PR #7** 合进 `main`,合并提交 `1a8978e`,
   云端 `质量门禁` 三道绿;`tools/myshop.pin` 已从 `6fa2a36` 跟到 `1a8978e`。
   **中间有过一段红窗口,值得记下来**:教程仓库(含重打的 zip)先推、myshop 后合,
   而 CI 的 `cross-repo` 是按 pin 克隆的 —— 那一刻 zip 是新的、pin 指着旧 myshop,
   「发布物一致性」这一步实测报了 `pyproject.toml` 与 `.gitignore` 两处漂移。
   **两个仓库有单向依赖时,推送顺序是:先合配套、再更 pin、最后推教程。**
5. **附录那张 SHA-256 登记表没有任何检查器** —— 2026-09-04 发现:第一批 14 个文件改完,
   表里对应的 15 行(含 `myshop.pin`)全部过期,而 `.gitattributes` 写着「字节必须与
   变更清单里的 SHA-256 一致」。**一条写明的契约,零门禁**,所以它静默烂掉了 ——
   与本轮修的 `check_layout` 缺样本、`check_site` 缺目录、`build_zip` 在 CI 里空转
   是同一族。已逐行复算改正(80 行现在全相符),但**校验脚本还没进仓库、没进 CI**。
6. **门禁仍查不到「内容级」不一致** —— 上一版给的三条堵法(给「真实输出」框加可复算标注、
   把「整份文件」代码块纳入 `check_crossrepo` 逐行比对、把 `.gitignore` / `pyproject.toml`
   纳入内容比对)**仍未实施**。本轮 C3/C4 两条正是这类漏网的实例。

## 八、v2.2 依赖关系地图（第二轮）

### 起因

诉求是「每页都能看到我在全书结构中的位置」。评估了三种形态（全图常驻 / 一维轨道条 / 纯文字上下文条），
实测后全部否掉——最便宜的一种也要每页 +1.6KB 且占首屏 72–213px。
最终走了第四条路：**单个外链 JS 运行时生成，按需弹出**。实测确认 `file://` 协议下
外链的普通 `<script src>` 正常执行（被 CORS 挡住的是 `fetch`/XHR 和 ES module），单源维护因此成立。

### 做了什么

- **`ai-workflow/_depmap.js`（新增）**：22 节点 + 39 条直接依赖，运行时生成 SVG。
  - 当前页**从文件名自动识别**，23 页零 per-page 配置；
  - **传递约简**算出 21 条主干边常显，当前页的跨层依赖另以虚线画出；
  - 当前页节点 `--accent` 实心反白 + 放大字号 + 「▼ 你在这里」，且是全图唯一不可点的一个；
  - 弹层标题给出模块名与页码——**「我在第几大段」是原来那张图答不了的**（图里没有分组框）；
  - 导航栏「地图」入口由脚本注入，不改任何页面的 HTML 结构；
  - 复用现有 CSS 变量，深 / 浅 / 跟随系统三态自动跟随；移动端图在盒内横滑，不产生整页横滚。
- **23 页各加一行** `<script src="_depmap.js" defer></script>`（41 字节／页）。
- **00-index 的静态依赖图按同一份真相表重建**：21 条主干边，22 个节点全部包成 `<a>` 可点。
- **`scroll-padding-top` 修正**：模板写死 78px，而导航栏实测桌面 54.3px、375px 手机 100.9px（窄屏折行），
  两档都对不上，点页内目录跳转时标题会被压在导航栏下。改为脚本按实测高度动态设置
  （桌面 65px / 手机 110px，随视口自适应），静态兜底值 78px → 64px（**同字符数，净零字节**）。
- **`tools/build_depmap.py` + `tools/depmap.tpl.js` + `tools/patch_pages.py`（新增）**：
  依赖数据的唯一事实源与两个产物的生成器，可重复执行、幂等、支持 `--dry-run`。
- **`references/deps.md`（新增）**：22 页依赖全表、5 处裁定的理由、主干边推导、改动后的操作步骤。

### 依赖数据裁定（本轮最重要的一件事）

改版前，目录页那张图的 24 条边与 22 页 kicker 声明的前置**有 13 页对不上**，其中 5 处是两边说法互斥。
这些矛盾一直藏在两个不同的位置（图在目录页、前置在各页顶部），没人对照过。经人工裁定：

| 页 | 图上原本 | kicker 原本 | 采用 |
|---|---|---|---|
| 06 | 无入边（孤立根节点） | 02、05 | **02、05**（图画错了） |
| 13 | 12 | 01、11 | **01、11、12**（三者并存） |
| 14 | 09、13 | 01–13 全部 | **09、13** + 文案注「汇总 01–13」 |
| 17 | 15 | 16 | **16**（16 页自称「门禁的前置」） |
| 20 | 17 | 8、13、18 | **08、13、18**（并统一页码格式） |

另 8 处是一方漏写，按并集合并。3 处 kicker 同步对齐：13 补 12、18 补 16、20 的「第 8 页」→「第 08 页」。
完整理由见 `references/deps.md`。

### 成本与验收

| | 实测 |
|---|---|
| 单页增量 | **41 字节**（一行 script 标签） |
| 全站增量 | **13.3 KB（+1.58%）**，其中 11.9KB 是共享的单个 JS 文件 |
| 首屏占用 | **0**（弹层默认不存在于版面） |
| 维护份数 | **1**（对比「每页内联 SVG」方案的 22 份） |
| 逐页验收 | 23/23 通过（headless Chrome 在 `file://` 下核对节点数 / 高亮 / 入口 / 页码 / 模块名） |
| 站点自检 | 正式页 0 问题 |
| 语法 | `node --check` 通过 |

被否掉的「每页内联全图」方案对照：+177KB（+20.8%）、桌面首屏 +461.9px、维护 22 份。

### 过程中修掉的两个真 bug

- JS 严格模式下 `02:` 这类对象键会被当成前导零数字（`node --check` 抓到），已给键加引号；
- 05→06 是**从右往左的同行边**，原连线函数只处理了从左往右，导致线穿过两个节点、箭头被压在节点底下。
  已按左右关系分支处理，`_depmap.js` 与 `patch_pages.py` 两处生成器同步修正。

## 九、v2.3 页码交叉引用可点击

### 起因

评审在剩余项里把这条排在最前:**读者真正迷路的时刻,是读到一半撞见「详见第 05 页」**,
而不是打开页面的第一眼。那一刻需要的是就地一个跳转,而不是回目录页找卡片。
改版前全书 273 处「第 N 页」文字引用**没有一处是链接**(每个内容页只有 3 个站内链接:
上一页 / 目录 / 下一页,唯一例外是速查页)。

### 做了什么

`tools/link_pagerefs.py`(新增)把正文里的页码引用改成链接,三种写法都支持:

| 原文 | 改后 |
|---|---|
| 详见第 19 页 | 详见第 <a>19</a> 页 |
| 前置:第 16、17 页 | 前置:第 <a>16</a>、<a>17</a> 页 |
| 第 17–19 页 | 第 <a>17</a>–<a>19</a> 页 |

只改数字、不动「第」和「页」,读起来仍是原来的句子。链接样式(`a.pg`)放在 `_depmap.js`
注入的 CSS 里,**每页零字节**:去掉下划线、改点线,悬停才变实线加底色——273 个链接
不能把正文搅成一片下划线。

### 安全边界

这些地方一处都没动,脚本按标签深度跳过:

- `<pre>` `<code>`——终端输出与代码,注入 HTML 会破坏演示(实测 `<pre>` 内被误改 **0** 处)
- `<a>`——已经是链接,再套一层是非法嵌套(实测全书嵌套 `<a>` **0** 个)
- `<script>` `<style>` `<title>` `<svg>`
- **自指**——第 05 页正文里的「第 05 页」链到自己没有意义(跳过 44 处,每页 kicker 与页脚各一)
- 非内容页页号(01–22 之外)

幂等:改完的数字已经在 `<a>` 里,再跑一次新增 0 处(实测)。

### 成本与验收

| | |
|---|---|
| 新增链接 | **273 处**,分布 3–43 个/页 |
| 全站增量 | 23 页合计 856,886 → 871,291 字节(**+14.1KB**,含 v2.2 的 script 标签) |
| 每页 CSS | **0 字节**(样式在共享的 JS 里) |
| 站点自检 | 正式页 0 问题(会逐个校验新链接的目标文件存在) |
| 逐页验收 | 23/23 通过,地图与页码链接同时正常 |

## 十、v2.4 13 页重核与两张流程图重画

### 13 页 Claude Code 内容重核 —— 撤销 v2.1 的一处误改

用 5 路代理对照 code.claude.com 官方文档核实这一页的 33 条论断，关键结论亲自抓一手文档复核。
**最要紧的发现是 v2.1 那轮我把这一页改错了**：原文写「一共有六种」并列出 `auto` 行是对的，
我依据审读代理的结论改成「官方清单是四种，auto 不是模式名」，是错的。

官方 `permission-modes.md` 的模式表列出六种：`default` / `acceptEdits` / `plan` / `auto` /
`dontAsk` / `bypassPermissions`；`auto` 有独立章节（后台分类器审查），状态栏文案 `⏵⏵ auto mode on`
与 `acceptEdits` 的 `⏵⏵ accept edits on` 不同。我还一并删掉了两条同样正确的说法：
「Pro/Max/Team 从 auto 起步」（官方原话）与「要按三下」（从 auto：第一下回 default，
再 default → acceptEdits → plan）。本轮全部改回并补全依据。

其余修正：

- **`acceptEdits` 的描述有安全后果的简化**：原写「运行命令仍要问你」，实际它还自动批准
  `mkdir`/`touch`/**`rm`**/`rmdir`/`mv`/`cp`/`sed`（工作目录内、非关键路径）。新手会以为删文件有拦截。
- 循环图重画：加回 `auto` 起点、标出「第 1/2/3 下」、补 `dontAsk` 从不进循环。
- 撤销菜单补条件显示：两个「恢复代码」选项只在该检查点有跟踪到文件改动时才出现。
- 死角清单补官方第四条限制「符号/硬链接路径不还原」，并把「存档状态、数据库、已发请求」
  诚实标注为常识推论而非官方列出项。
- 页脚修正数 4 → 6，核验日期改 2026-09-01。

核实后确认无误、维持原样的：plan mode 那段英文引文**逐字**出自官方（此前担心是转述）；
「100 个还原点」与 30 天保留期均有官方原文；`/review` 是 `/code-review` 别名；
`@path` 在 CLAUDE.md 里的导入用法确有其事（某代理误报「查无依据」，原文在 `memory.md`）。

### 16 页 `@v7` 版本号复核 —— 确认正确

此前审读怀疑「两个组件一年各连跳两级且恰好同为 v7」。查证：`actions/checkout` 最新 v7.0.1、
`actions/setup-python` 最新 v7.0.0，**两者确实都在 v7**，教程无误，未作改动。

### 14 页流程图第二轮 —— 用户复看后指出三处，其中一处是语义错误

- **「凑合」那条线不是水平的**：5b 的中心 y=184、菱形中心 y=176，差 8px 所以拐了个小弯。
  重排三个结果框，让 5b 的中心与菱形对齐在 y=176，连线成为真正的直线（5a → ⑥ 同理拉直）。
- **5a 和 5b 都连到 ⑥ 循环 —— 这是语义错误，不只是画法问题。**
  页面正文写着 5b「改完还是跑一遍 mypy 和 pytest……这是成本最低的一档，**零回退**」，
  即它改完之后要再看一遍改动，**仍停在同一步**；而 ⑥ 的含义是「这一步完成了，做**下一步**」。
  所以只有 5a 该连 ⑥，5b 应回到 ③。已改。
- **⑥ 循环回到哪里看不出来**：把 5b 与 ⑥ 的回程合并成一条共用回程道，终点明确指回 ③，
  并在唯一的交叉处（5c 的红虚线）画了跨线小拱，避免被看成连接。
  说明框同步改为「两条回程都回到 ③：⑥ 是『这一步完了，做下一步』，5b 是『还在这一步，改完再看一遍』」。

### 第三轮:零代价三件套 + 把取舍写进正文 + 补全四页卡片

#### 又一个同族缺陷:正则的字符类漏了数字

补卡片时发现 `check_claims` 的卡片正则写的是 `href="(\d\d)-[a-z-]+\.html"` ——
**`[a-z-]+` 不匹配数字**,而第 10 页的文件名是 `10-flaky-e2e.html`,`e2e` 里带个 `2`。
**那一页的卡片从来没被这条断言碰过。** 和上一轮修的三个是同一族:
检查器没在查它以为在查的东西。已改 `[a-z0-9-]+`。

顺带纠正我上一轮的说法:缺「含 N 处修正」后缀的是 **3 页(04/07/11)**,不是 4 页 ——
第 10 页本来就有,只是旧正则看不见它。三页已补,现在**有页脚的 11 页与卡片 11 张逐个相符**。

#### myshop CI:零代价三件套(已做)

`permissions: contents: read`、`concurrency`(同分支新推送自动取消旧的)、
`timeout-minutes: 10`。**job 名、step 名、`@v7`、`pip install` 一个字没动** ——
动了要连改教程 16 处。改动是纯增行,结构校验过顶层键顺序与缩进层级。

#### 第 18 页第九节:把两条工程债转成教学取舍(已做)

那一节本来就有一张「这份配置刻意**没有**包含什么,以及为什么」表(原列四项)。
补两行,各写清**代价**与**什么时候该做**:

- **把 action 钉到 SHA** —— 防的是标签被改指向。本书刻意用 `@v7`,因为第 16 页
  那个真实的过期陷阱教的正是「以代码仓库的最新标签为准」。**该钉的场景**:
  流水线里有密钥、能写仓库、有合规要求。**代价**:安全更新不会自动跟上,
  「没人升的 SHA 比浮动标签更危险」。
- **钉工具版本** —— 本书刻意不钉,因为第 16、19 页那两张环境对照表靠的就是
  「本地装了半年」对「云端全新安装」;钉死了对照组就没了,而它恰恰是
  「云端才是干净环境」最有力的证据。**该钉的场景**:交付的生产项目而非教材。

表下另加一句:`permissions` / `concurrency` / `timeout-minutes` 这三样**不需要权衡**,
所以不在表里,直接加就行,配套项目已经带上了。

**这一步的意义**:两条一直躺在变更清单里的「工程债」,现在变成了书里读者能读到的取舍。
读者拿到的不是一份「作者忘了加固」的配置,而是一份「作者说明了为什么这么选」的配置。

#### 改掉我自己那条错误的债务描述

`变更清单.md` 原来写「`ci.yml` 被教程 18 页**逐字**引用」—— **两处都不准**:
没有任何一页逐字贴过它(16、18 页贴的都是变体,方向还相反),而且**漏了第 16 页**。
我照这条错误描述判断了好几轮。已作废并改正,附上正确结论。

### 检查器的三个 bug:它没在查它注释里声称在查的东西

第二轮。缘起是上一轮的调查:`check_layout` 有真值门槛和变异锁,而**排在它前面**的
`check_site.py` 与 `check_claims.py` 零测试,并且坐实了「检查器静默变空操作、门禁照样报绿」。

#### 三个缺陷,每个都用变异法验过

| 缺陷 | 原来会怎样 | 修法 |
|---|---|---|
| **① 空目录报绿** | `site.glob("*.html")` 在目录不存在时返回空迭代器**且不抛异常** → 四条规则全部空转 → 打印「检查 0 页 → 0 个问题」→ `RESULT: PASS` → 退出 0。**把 `ai-workflow/` 改个名就是静默空操作** | 必备的组(正式页、姊妹课)为空即报错;入口页缺失也报错 |
| **② 跨目录锚点从未被检查** | `scans.get(Path(target).name)` 只在**当前这一组**里找目标页,而 `index.html` / `kb/` / `ai-workflow/` 是分三组扫的,所以跨课锚点恒被跳过。顺带还有个只比 basename 导致同名文件互相误认的隐患 | 先把全站每个 HTML 的 id 建成一张按**真实路径**索引的表,再校验 |
| **③「导航三件套」是空头支票** | `nav_targets = [h for h in s.links]` 只是把全部链接原样拷一份,判断退化成「页面任意位置有一个指向 00-index 的链接即可」—— 上一页/下一页缺失、pager 整块消失,全测不到 | 真去解析 `<div class="pager">` / `<nav class="pager">`,按页面顺序校验相邻页 |

姊妹课也一并纳入导航校验。两课的分页约定不同(主线每页都回目录,姊妹课只在首尾),
所以「必须回目录」拆成可关的选项,而相邻页校验对两课都适用。

**变异验证(六种,全部按预期变红、还原后归绿)**:改名 `ai-workflow/`、改名 `kb/`、
删掉 `index.html`、改坏一条 kb→主线的跨目录锚点、删掉主线某页整块 pager、
把姊妹课某页的下一页指到隔壁页。

#### `check_claims`:循环零匹配仍报绿

`:66` 的卡片检查是个 `re.finditer` 循环 —— 00 页标记一变就零匹配,循环体一次都不执行、
一条断言都不产生,末尾照常打印「全部口径一致」退出 0。这正是同一类形态,
而且它藏在一个**专门用来防口径漂移**的脚本里。

变异对照:把 00 页的「处修正」全改成「处订正」,**旧版 exit=0「全部口径一致」,新版 exit=1**。

#### 两处过程中的自我纠错

- **我第一版的断言定错了**:写成「卡片张数 = 有页脚的页数」,基线当场红 —— 实测 11 页有页脚
  但只有 7 页的卡片带「含 N 处修正」后缀。改成能真正抓住零匹配的形式(至少匹配到一张 +
  卡片指向的页都得有页脚)。**顺带发现一个未修的内容问题**:04、07、10、11 四页的页脚
  报了修正数,但 00 页对应卡片没带这个后缀 —— 已单独提给用户,本轮不擅自改。
- **一条变异是我自己写错的**:第一次试「删 pager 的下一页」时检查器没报红,查下来是我的
  正则删到了顶栏的「下一页」而不是 pager 里的那条 —— **检查器是对的,变异是错的**。
  改对之后立刻报红。

#### 第 6 页行数因第一轮改动而过期,已同步

`check_claims` 当场抓出:第一轮给 `api.py` / `web.py` 加了编码保护的注释,
核心三模块 224 → **240** 行、含网页层 374 → **398** 行、`api.py` 98 → **114**、
`web.py` 150 → **158**。第 6 页六处数字已更新。**这正是这个工具存在的理由**。

### 全书 Windows 命令:加一个「命令样式」开关(登记多轮的那条工程债)

这条一直登记着没做:全书 23 页共 **164 个可复制命令块**,其中 **72 处**是
macOS/Linux 专用写法(`.venv/bin/python` 73、`&&` 52、`touch`、`which`、
`mkdir -p`、`python3`),而 **Windows 形式的示例是 0 处**。第 2 页那张对照表
是权威说明,但读到第 8 页、第 14 页的人早忘了,得一路手动换算。

**为什么不是并排写两份**:164 个块并排会让正文体量接近翻倍,违背「读起来不累」。
**为什么不是每页内联脚本**:实测内联 23 遍全书涨 **10.3%**;外置只需每页加一行
`<script>`,实测 **+0.10%**(本目录已有 `_depmap.js` 这个先例)。

新增 `ai-workflow/_cmdos.js`,在导航栏主题开关旁加一组
**macOS/Linux ｜ Windows**,只在**本页确有需要改写的命令**时才出现。

**边界(这部分比功能本身重要)**

| 只改 | 绝不碰 |
|---|---|
| `<pre><code>` —— 给你复制的命令 | `<pre>` 纯文本块(63 个)—— 那是「**真实输出**」,是 macOS 上的实测记录,改写它就成了伪造 |
| | 含 heredoc / shebang 的块 —— 那是**写进文件的 shell 脚本**(第 16 页那个 git 钩子)。Windows 上 Git 用 Git Bash 执行它们,原样就是对的,改成 PowerShell 反而把能跑的东西改坏 |

转换在**文本节点**上做,所以块内的 `<b>` 高亮不会被破坏;原文留在内存里,
切回 macOS/Linux 时逐字还原(真机验过)。

**两个容易写错的地方,都处理了**

- `&&` 拆行**必须避开字符串里的 `&&`**:第 10 页有一行
  `reason="pip install pytest-playwright && playwright install chromium"`,
  那是 Python 字符串,拆了就把代码改坏。做了引号配对判断,该行确认不变。
- bash 的 `VAR=值 命令` 前缀在 PowerShell 里要写成 `$env:VAR='值'; 命令`
  (第 6 页 `PYTHONPATH=. .venv/bin/python -m myshop.api`)。

**语义变化如实说明**:`&&` 是「前一条成功才跑下一条」,拆两行是「不管成不成都跑」。
切到 Windows 时页首出现横幅讲清这一点,并给出保留原语义的办法(PowerShell 7 或 Git Bash)。

**验证**:`node --check` 通过;把 `_cmdos.js` 里的两个纯函数**原样抽出**在 Node 里
跑遍全书 164 个代码块,逐条打印前后对照(60 块会转换、1 块整块跳过);
关键反例(字符串里的 `&&`、普通行)确认不变;起本地只读 HTTP 服务在真实 Chrome 里
点了开关:按钮状态、命令改写、横幅显隐、切回还原全部正确。第 2 页对照框补了一句指向开关。

### kb 姊妹课 8 页专项审计:25 条定罪全部属实,已全修

用户点名检查姊妹课。这 8 页当初按「逐字节复制」并入,从未进过任何审读范围——
上一轮 kb/06 那处文字溢出还是用户肉眼发现的。这次派 23 个只读代理做 8 页精读 +
4 个横切维度,每条定罪再过一轮对抗复核(默认立场「这是误报」)。
**提出 25 条,复核后 25 条全部成立、0 条被推翻。** 0% 的推翻率本身可疑,
所以最要紧的几条我自己重新对着官方文档核了一遍——确实都成立。

#### P0 · 三条事实错误(逐条 WebFetch 官方原文核实)

| 定罪 | 官方原文 | 修法 |
|---|---|---|
| **`context: fork` 讲反了**(kb/06 §5 + kb/07 可选升级框) | Skills 文档:「Add `context: fork` … The skill content becomes the prompt that drives the subagent. **It won't have access to your conversation history.**」 | kb 把两个都叫 fork 的东西混成一个:「fork 当前对话」(子代理,继承全部)与 SKILL.md 的 `context: fork`(技能在**隔离**子代理里跑,**读不到对话**)。§5 标题改为「同名不同物」,加危险框讲清区别,示例从「整理对话」换成自包含的 `deploy`;kb/07 那个「可选升级」框反转成禁止框——`/kb` 技能全部的活就是读对话,加了它就废了 |
| **`name` 字段说死了「不影响命令名」**(kb/03:60) | 「In a personal or project skill, `name` sets only the display label… In a **plugin skill**, `name` sets the last segment of the command.」 | 补上插件技能例外。讽刺的是 kb/02:265 自己写着这个例外,自称「全表」的 03 页反而删了 |
| **内置输出风格漏一种**(kb/04 §3/§9/小结/目录) | 「Default … There are **four additional** built-in output styles: Proactive, **Concise**, Explanatory, Learning」 | 「四种」改「五种」,补 Concise 行(含 v2.1.237+ 版本要求)与 token 表的反向条目 |

#### 一处更正:我先前对用户说错了

我曾报告「kb 的两处『待验证』标注都诚实」。**其中一处的依据是编的。**
kb/04:237 写「命令参考那页则说 `/config` 在两端行为一致」——我把两页都取了,
命令参考页对桌面/终端差异**只字未提**。kb 把「沉默」写成了「相反的主张」,
再据此标待验证。而输出风格页对桌面行为的说明明确无歧义,根本不需要标。
已把该框从「⚠️ 待验证」改为直接给出确定行为,并补上 VS Code 插件的第三种情形。

#### P1 · 四处交叉引用指错节(我自己逐条核到 h2 行号)

| 位置 | 原文说 | 实际在 |
|---|---|---|
| kb/02:356 | 同名覆盖「见上一节」(从第 7 节数=第 6 节) | 第 5 节「重名了谁赢」 |
| kb/03:437 | 500 行上限的理由「见下一节」(=第 7 节) | 第 8 节「技能内容生命周期」 |
| kb/03:109 | 「子代理与**钩子**(第 06 章详讲)」 | kb/06 八节零处讲钩子;钩子在 kb/05 |
| kb/07:360 | CSS「见下一节的讲解」(=第 6 节验收) | 第 7、8 节 |

#### P1 · 三条主线与姊妹课打架(这三条是主线的问题)

- **主线 13:196 承诺「`@` 导入其他文件,第 21 页会讲」——第 21 页通篇没有**
  (grep「导入」=0,`@` 只命中四处 CSS `@media`)。这块内容只在 kb/05 第 4 节。
  改为直接指向姊妹课。主线三轮审读都没发现这条悬空前向引用。
- **主线 21 页教 `.claude/commands/step.md`,kb/02:338 说「新建的一律用技能写法」。**
  官方确认两者都生成 `/step`、用法一样。主线补一句说明并指向 kb/02。
- **「钩子」一词两边指两套机制**:kb 指 Claude Code 生命周期钩子(`settings.json`),
  主线 16/22 页指 git 钩子(`.git/hooks/`)。两边都没说明是两回事。
  16 页节标题加「(git hooks)」并加一段对照,22 页那条硬规矩加「git」二字,
  kb/05 补一句反向说明。

#### P2 · 版式:两条曲线从文字中间穿过 + 深色主题对比度不达标

- kb/06 第 173、188 行的贝塞尔曲线分别从「只给一段说明」「整段对话复制过去」正中穿过。
  按曲线参数复算过重叠区间,用几何求解重新定位两个标签(213,92)与(238,274)——
  离曲线最近且不相交。
- `.concept dt .n` 等三处写死 `color:#fff`。深色主题 `--accent` 是浅橙 `#e8935c`,
  白字压上去 **2.40:1**,低于 WCAG 的 4.5(11.5px 粗体够不上大字豁免);
  受影响的是默认主题下 01–06 页共 75 个圆形序号。改为 `color:var(--bg)`:
  深色 **7.73:1**、浅色仍 4.52:1,两个主题同时达标。

#### 工具:补第六条规则 R6 `path-over-text`

原有五条规则的肇事者与受害者**只取 `rect` 与 `text`,从不看 `<path>`/`<line>`**——
这是判据的真盲区,不是阈值问题,所以那两条压字曲线巡检零检出。
新增 R6:实现 `d` 属性的 M/L/H/V/C/Q/Z 采样(遇到没实现的指令整条放弃,宁可漏报),
只抓「穿过去」不抓「指过来」——线要在文字盒中间带里连续走过 ≥0.30em,
且该段前后都跑到盒外(端点停在标签旁的引线因此排除)。

**准入门槛**:修之前,R6 在全站 31 页里**恰好报出那两条、零误报**;修之后归零。

#### 复核没能推翻、但我自己核实后确认是误报的一处

我 grep 到 `kb/01:570` 有 `src="https://…`,像是违反 kb/00「没有任何外部 CDN、
字体、图片依赖」的声明。开文件核了:它在 `<code>` 里、还是转义过的
`&lt;img src="https://..."&gt;`,是那张「AI 写页面常见的坑」对照表在**教你别这么干**。
不是真依赖,声明成立。8 页离线渲染全部正常。

#### 收尾自查又抓到 4 处「只修了那一处」(老毛病第五次)

改完 25 条后自己 grep 旧口径,发现 4 处没跟着改 —— 全是同一个模式:
**改了正文,忘了目录、小结、和引用同一段内容的别处。**

| 残留 | 问题 |
|---|---|
| `kb/06:40` 目录 | 还挂着旧标题「让技能跑在分身里」,而 h2 已改成「同名不同物」 |
| `kb/06:451` 小结表 | 还写「让技能跑在分身里 / 前置数据加 `context: fork`」—— 正是被推翻的那句 |
| `kb/00:187` 准确性框 | 还写「**两处**标注为『待验证』」并列着 `/config` 那条,而它的依据(虚构的文档冲突)已撤销 |
| `kb/07:522` | 抄的那段 CSS 还是 `color:#fff`,与已改成 `var(--bg)` 的真实文件对不上 |

第四处顺带成了教学点:那一节本来就在拆解主题切换,现在补了一句
「凡是颜色,尽量用变量而不是写死」并给出 2.4:1 这个实测数字。

另有 1 处是**我自己的误报**:`kb/02:264` 也含「不影响你打的命令」,但它原文
写的是「在个人/项目技能里…(插件技能是例外)」—— 限定齐全,不用改。

#### 计数与标注(自己重数,全部准确)

00 页「共 7 章 · 67 个知识点」:按 `<h2 id="s\d+">` 逐页数得 11/10/8/9/11/8/10,
求和正好 67,七张卡片逐个相符;难度星级卡片与页内一一对应。
全站 13 处「待验证」里有 10 处是**教学内容本身**(教你让 AI 拿不准时标它),
真正针对 kb 自身的免责只有 2 处,与 00 页「两处」的说法一致。

### Codex 第三轮：它提的 3 条全对，对比表错一行；核验又翻出我自己 10 处同类

Codex 重新评估后给了 1 条 P1、2 条 P2 和一张 10 行对比表。逐条开文件核到行号，
又把它的 11 个单元测试复制到临时目录实跑（`Ran 11 tests … OK`，事后 `find` 确认只读目录无写入），
再派 15 个只读代理**反驳我自己的判定**。四个代理推翻了我的论据（结论方向均维持），
另扫出 **10 处我新写内容里的同类矛盾 + 7 处交叉引用错**。

#### Codex 对的三条

| 定罪 | 核验 |
|---|---|
| **P1 入口页前后矛盾** | 属实。`00:620`「先把那一页（第 2 页）做完，后面每页都能立刻验证」与 `:641–646`「第 2 页只是补课」直接冲突。**归因不能往原版推**：那整个框（`:618–623`）本轮都被我改过，`:622` 还主动前指那个矛盾框；原版与 Codex 版都没有同页矛盾。更深一层：我把第 2 页定性为「补课」本身就错 —— 第 2 页从不建 `price.py` 或任何测试（`02:478` 自己写着「第 6 页会真的建起来」），而 A/B 拿到的仓库不含 `.venv`（`06:153`），第 6 页的验证命令没第 2 页跑不了。**真相是两件都必做，不是主次关系** |
| **P2 终点列判据偏经验化** | 属实，**且比它说的更弱**。`check_layout.py:129` 的「同列」只比右边缘。实测：往真值样本那张图注入一条右缘贴齐画布的通栏矩形，旧判据 **3 条 → 2 条**，「兜底」被整条吞掉。而真值自检 `:223` 只断言 `n0 > 0` 不是 `== 3`，吞了也照样绿灯 |
| **P2 根 CI 未跑版式巡检** | 属实，**但原因我写错了**。`check.yml` 注释说「云端没基线」；实际 `check_layout.py:244` 本就有 `--allow-missing-baseline`，卡住的是 `:274` 无基线时退出码恒非零（干净退 2、有发现退 1），永远拿不到绿灯 | **(已于 2026-09-03 修复:两页真值样本随仓库发布,CI 扩到四步)** |

#### Codex 错的一行：「CI 安全性 Codex 优」

它描述的双平台 / SHA 固定 / 并发取消 / 超时 / 最小权限那份 CI，**只存在于**
`codex/_ai/tools/release_overlays/ci.yml` —— 构建时塞进发布 zip，从未进入任何受 Git 跟踪的仓库历史；
Codex 本地 `myshop/` HEAD `06fd6fa`，受跟踪的 `ci.yml` 与我方**逐字节相同**；`codex/_ai` 本身不是 git 仓库，
没有任何 CI；它正文 `16:526`、`18:649` 教的仍是浮动 `@v7`。另有**两处对我方的事实性错误**：
我方 `check.yml:9–11` 已有 `timeout-minutes: 10` 与 `permissions: contents: read`；我方 CI 无 `pip install`，供应链面更小。
公道话：Codex 用 overlay 绕开「ci.yml 被第 18 页逐字引用」这个约束，是正当工程解法，不是凭空造 CI。

其余九行：「校验器自身测试」「发布物」「失败保护」Codex 优，**成立且幅度比我原判更大**；
「页面专项检查」Claude 优成立（Codex 整个 `tools/` 零 svg 命中，其 `visual-qa.json` 自认渲染被环境阻断）；
三行主观项不可证伪。**一处我原判的硬错已改**：首轮勘误两家同为 26 项，「47 处」是原版自带标注，不是我方产出。

Codex 表格漏掉、而对自学者最要紧的两个维度（均利于我方）：① 配套项目能否跑通四层 ——
Codex 的 myshop 没有网页层，其 E2E 文件 `:17` 自承「本示例项目只有 JSON 接口，没有页面」，第 10 页 flaky E2E 在它的项目上无法实操；
② 项目从哪拿 —— Codex 23 页零处告诉读者，`06:117` 写相对路径 `..\myshop`，而它的教程 zip 不含 myshop。

#### 核验翻出的我方 10 处（Codex 没抓到）—— 已全修

| 位置 | 错在哪 |
|---|---|
| `00:633`、`02:154`、上轮提交信息 | 「Windows 对照框在第 2 页**第五节**末尾」—— 它在**第二节**（`02:262–280`，h2 二 L184 / 三 L297），第五节 L385–471 零处 Windows/git。**我把自己更早写错的话原样抄进了新框** |
| `00:642` | 「第 2 页带你亲手建出 price.py 和第一条测试」—— 第 2 页不建 |
| `00:619` | 框题「**一件**建议先做的事」，实为两件必做 |
| `00:627` | 「全书从第 6 页起都在这个项目上动手」—— `02:209` 就在 `mkdir`，`03:265` 就在项目根目录跑命令 |
| `02:263` | 标题「记住这**三条**」而框内已扩成五条；「全书**唯一一处**」已被同页 `:148` 新框与 `01:203` 推翻 |
| `01:201` | 「**只有**第 13 页是 Claude Code 特有的」—— 第 21 页整页 `CLAUDE.md` / `.claude/commands/` / 钩子（8 处），第 20 页也用 |
| `00:613` | 「抄第 22 页的提示词模板」—— 第 22 页零处「提示词」；同页 `:439` 早已改成「在第 13、14 页」，**页内自相矛盾**（原版继承，Codex 同错未修） |
| `01:574` | 「第 18 页有五条设计原则」—— 那页九节无此内容（原版继承，Codex 同错） |
| `00:630/638` vs `11:190` | A 路「自带完整存档历史」，但第 11 页无条件 `git init` 并展示「Initialized **empty**」输出 |
| `index.html:86` | 「myshop/（本目录内）」—— 教程仓库 `.gitignore:3` 排除 `myshop/`，克隆的人拿不到 |

#### 工具侧（修一类，不修一处）

- **终点列判据**：「同列」改为左右边缘都在 12px 内；真值自检的断言从 `n0 > 0` 改为**定值**（3/1/7）；
  新增两条不依赖基线的阳性对照（kb/05、kb/06 必为 0），以及一条**变异锁**（注入通栏矩形后仍须报满 3 条）。
  现为 6 条自检。`layout_rules/README.md` 明写剩下的盲区：两个真断开节点若同在最右列且左右都对齐，依旧不报。
- **六个 `det_*.py`**：写死的作者绝对路径全部改为 `_baseline.py` 统一解析（`AIWF_BASELINE` → 同级 `ai-workflow/` → `fixtures/baseline/`）；
  自测入口统一为 `sys.exit(_selftest())`。以前 **5/6 永远退 0**（`det_grid_text` 打印 FAIL 也退 0，`det_dangling_edge` 硬写 `sys.exit(0)`）。
  逐个变异验证：把 `check()` 改成恒返回 `[]`（正是真值门槛要拦的那种检查器），**六个全部 exit=1**。
  又：CI 模拟跑的是**裸 `python`**（不带 `PYTHONUTF8=1`），当场抬出 `det_collision.py` 是六个里
  唯一没加 UTF-8 stdout 保护的，Windows 控制台 cp1252 下打印中文直接崩（rc=1）——
  和 Codex 上一轮抓到的 `check_site.py` 是同一类，上次修了 7/8 个，这次补齐 6/6。
  现在九个工具脚本全部用裸 `python` 逐个验过 rc=0。
- **`build_zip.py`**：改为先写 `.tmp`、校验通过再 `os.replace`。旧写法直写发布路径，缺一个文件就 `return 1`，
  而那时旧包已被清空 ——「拒绝打包」实际留下的是一个合法但残缺的短包。实测验证：白名单加一个不存在的文件 → 退 1、旧包完好、无残留 `.tmp`。
  同时把 `assert` 改成显式检查（`python -O` 会删掉 assert），并钉 `create_system = 3`（否则同内容在 Linux 重打会得到另一个 SHA）。
  **该包 SHA 因此变了一次**，附录已同步。
- **版式巡检进 CI**：两页真值样本快照随仓库发布（`tools/layout_rules/fixtures/`，约 100 KB，有自己的 README 记来源与 SHA）；
  `check.yml` 从两步扩为四步（+版式巡检、+六个规则模块自测）。把仓库整体复制到无同级基线的临时目录模拟 CI，6 条自检全过、退 0。

#### 教训（第四次复发，但是新形态）

前三次是「修报告的那一处不修那一类」与「新写的门面内容不按正文标准审」。
这一次多了一层：**把自己上一轮写的未核实断言当成事实抄进新内容**。
「Windows 对照框在第五节末尾」这句我先写进 02 页，下一轮又抄进 00 页新框，再抄进提交信息 ——
三处同一个错，而只需开一次 02 页数一数 h2 就能拦住。已写入长期记忆：
**自己此前写的断言与外来断言同等对待，引用前开目标文件核。**

#### 明确不做（维持登记）

三类发布包 + manifest、Actions SHA 固定、双平台矩阵、Python 工具版本固定。
~~另新登记一项：**全书 23 页零处 Windows 形式的命令示例**（`.venv/bin/python` 74 处）——
属整书改造，另立议题。~~ **2026-09-03 已做**：加了 `ai-workflow/_cmdos.js` 的
「命令样式」开关（外置脚本，全书只涨 0.10%），只改可复制的命令、不碰「真实输出」
与写进文件的 shell 脚本。

### Codex 第二轮交叉评审：5 条全是我自己新写的门面内容

Codex 对最新版本重评，**撤回了上一轮那条误判**（权限模式「Pro 从 auto 起步」）——
它复读官方文档后确认教程是对的，与我三次核查的结论一致。但它这一轮的新定罪
**全部打在我最近两次提交新加的四个说明框上**，逐条实核后 **5/5 属实**。

| 定罪 | 实测 | 修法 |
|---|---|---|
| 00 页「两条路」却列了 A/B/C 三条 | 原文同时写着「下面两种方式」与「两条路任选一条」 | 见下一行，一并解决 |
| **C 方案到不了 06 页基线**（本轮最重） | 02 页只建出 `price.py` 与第一条测试；`order.py`/`api.py`/`web.py`/`test_order.py`/`test_api.py`/`e2e/conftest.py` 在 02 页**出现 0 次**，而 06 页要求 `17 passed` | C 降级为 A/B 之外的**补课**：它教你弄懂每个文件为什么在那儿，但明说它建不出完整项目，完整基线仍需 A 或 B。「两条路」因此恢复成真话 |
| 00 页「第 2 页第一节会先带你装 git」 | 02 页第一节叫「先看你电脑上有什么」，只**检查**已有版本；Windows 装 git 的说明在第五节末尾那个对照框（我上一轮刚加的） | 改为直接给出 `git-scm.com` 并指向第五节 |
| 01 页把 npm 当命令行首选，且误称 CLI 需要 Node.js | 对照官方 `setup.md`：**Native Install 标着 (Recommended)** 且是第一个标签页；npm 在「Advanced installation options」之下；npm 路径要 **Node.js 22+**（不是 18），但官方明写「装完的 `claude` 二进制本身并不调用 Node」 | 方式二改为原生安装并给三平台命令（`install.sh` / `install.ps1` / winget / Homebrew），npm 降为可选并标清 Node 口径，补 `claude --version` 验证；顺带引官方那条与 02 页呼应的细节：Windows 装 Git for Windows 可让 Claude Code 用上 Bash 工具 |
| `tools/check_claims.py` 残留 224 硬编码 | 上一行已改成「宣称行数 = 实数」的动态比对，下一行却还留着 `"224 行" in t06`，提交信息里「224 字面量已消除」因此不完全成立 | 删掉这一行；删后后文引用的 `t06` 变量名一并修正为已读入的 `t06_` |

**教训（老毛病复发）**：上一轮的共性是「修报告的那一处、不修那一类」，
这一轮是它的另一面：**新写的门面内容没按正文同等标准审**。正文每一条数字都经过
三道巡检与断言登记，而我自己新加的入门框里，「两条路」后面紧接着三个选项、
交叉引用指错节、安装方式与官方推荐相反 —— 这些只要像审正文一样重读一遍就能拦住。
已写入长期记忆。

~~**没做的（工程债，维持登记）**：根 CI 未覆盖版式与跨仓库一致性、无三类确定性发布包与
manifest、工具缺单元测试、Actions 用浮动 `@v7`、Python 工具版本未固定。~~
**2026-09-03 逐条了结,见文末「工程底座逐项裁决」**：根 CI 已覆盖版式(四步门禁)；
三类发布包与 manifest **裁定不做**(私有仓库无消费者、23 页零发布内容)；
工具自测已进 CI；`@v7` 与不钉版本 **裁定刻意保留**,并写进第 18 页第九节那张取舍表。

### kb/06 文字溢出 + 版式巡检覆盖 kb(用户肉眼发现)

用户在 kb/06 发现「完整继承：历史 / 系统提示词 / 工具 / 模型」一行超出节点框。
`getBBox` 实测:该文字 x 285.5~506.8、宽 **221.3**,而分身框只有 x 306~486(宽 180),
**左右各溢出约 20px**;对照上方同位文字仅 162.6px,正好放得下。
拆成两行(「完整继承：」+「历史 / 系统提示词 / 工具 / 模型」),重测 58.5px 与
**163.8px**,底部 302.9 < 框底 308,全部落在框内。

**但真正的问题是覆盖盲区:`check_layout` 只扫 `ai-workflow/`,kb 的 8 页 SVG
从未被任何巡检看过。** 已把 kb/ 纳入扫描范围。

扩展后立刻报出 5 条 `dangling_node`(kb/05 三条、kb/06 两条)——**经查全是误报**,
而且是同一类:kb 的图**从左往右流、终点在最右一列**,而该规则的「终点=最下一排」
判据假设自上而下。这与第二轮 00-index 阅读地图那次误报是同源问题(判据与图的语义不符)。

**新增判据「终点列」**(写进驱动层 `收窄()`,附完整证据):节点位于最右一列
**且该列有 2 个以上节点** 时豁免。三组数据支撑:

| 图 | 被报节点所在列 | 该列节点数 | 处置 |
|---|---|---|---|
| kb/05 决策树 | 596~708(最右) | 4 个,全是答案终点 | 豁免 |
| kb/06 | 562~722(最右) | 2 个「只回传最终结果」 | 豁免 |
| 01 页原版真值样本「兜底」 | 666~882(最右) | **仅 1 个** | **不豁免** |

「单独一个节点在最右不算」这一条是关键 —— 它正是真值样本要抓的漏画回程边。
复验:kb 5 条误报清零、真值自检仍报满 3 条(没把真缺陷一起豁免)。

先用宽高比判流向的思路已试并放弃:01 页原版真值样本宽高比 3.15,反而比
kb/06 的 2.47、kb/05 的 1.33 更「宽」,该信号无法分离。

### 教学质量评估:补上四处断链(学习者视角)

三路教学质量评估(72/77/82 分)得出一个前三轮正确性审计从未触及的结论:
**讲法是第一流的,但学习链条断了**。三路独立给出同一判断,四处硬伤经我逐条实测确认:

| 断点 | 实测证据 |
|---|---|
| 读者无处获取 myshop | 全书 23 页 grep `git clone`/下载/获取项目 **零命中**,而 06 页就要你跑 pytest 对照「17 passed」 |
| 没教怎么装 Claude Code | 零页提及。整本书讲 AI 协作,读到 13 页才发现自己进不去 |
| 08 页用未教的 git | 两处 `git checkout -- .` 还原,git 在 11 页才教 |
| Windows 第一条命令就废 | `which python3` —— PowerShell 实测不存在;全书 48 处 `&&`,Windows PowerShell 5.1 实测解析错误,而 02 页 Windows 框一字未提 |

**补法:四个新增说明框,不改任何既有文字。**

- **00 页**「先把配套项目拿到手」:三条路(clone / Download ZIP / 第 2 页从零建),
  写明各自代价 —— ZIP 拿不到存档历史,11–12 页需自己 `git init`。
- **01 页**「先把 AI 编程工具装上」:桌面应用与 npm 两种装法,并说明
  **换别的 AI 工具也行** —— 四道防线与工具无关,只有 13 页的快捷键是 Claude Code 特有。
- **08 页**:实验前置框,点明 `git checkout -- .` 现在照抄即可、原理在 11–12 页,
  并给出「用 ZIP 拿的项目先 git init」的补救命令。
- **02 页**:第一条命令旁加 Windows 替代(`python` 而非 `python3`、
  `which` → `Get-Command`);Windows 对照框从三条扩到五条,新增
  **`&&` 在 PowerShell 5.1 不可用**(三条出路:分行 / `;` / 装 pwsh 7)与
  **git 怎么装**(顺带指出 Git Bash 里 `&&` 和 `which` 都能用,想照抄书上命令就用它)。

**新写的命令逐条实测**:clone 目标仓库 HTTP 200;PowerShell 三条替代命令
在本机真出结果(Python 3.12.10 / git 2.55.0 / 解释器路径)。三道巡检全绿。

**评审同时确认的强项**(可复用经验,记录备查):五步概念卡真执行了 35 次,
其中「③ 它在体系中的位置」强制每个新概念挂进已有知识网;04 页立可证伪预言、
08 页用五次真机实验兑现并推翻读者直觉(红的条数 ≠ 危害程度);
17/19 页把作者自己的错误模型摊开并抽象成可复用规则;
11/12 页那些「这不是报错」「按 Esc 再 :wq」专治新手以为电脑坏了。

### Codex 交叉评审的工程定罪(4/4 属实,已修)

另一套实现(Codex 版)对本目录做了对照评估。它的**四条工程定罪逐条实测全部成立**,
且暴露出一个难看的共性:**我修的是「被报告的那一处」,不是「那一类」**——
UTF-8 保护 8 个工具修了 7 个、`main()` 隔离 3 个脚本修了 2 个,漏的两个恰被它抓到。

| 定罪 | 实测 | 修法 |
|---|---|---|
| `check_site.py` 在 Windows 默认 cp1252 控制台直接崩 | 属实(退出码 1 + UnicodeEncodeError),且它是唯一没加保护的工具 | 补 `sys.stdout.reconfigure` |
| `check_layout.py` 默认基线是作者机器绝对路径 | 属实(第 192 行) | 改为 `AIWF_BASELINE` 环境变量 → 仓库同级 `ai-workflow/` 约定 → 都没有则退出码 2 |
| `build_registry.py` 无 `main()`,import 即写 `sources.md` | 属实 | 只把写盘段包进 `main()`(见下方教训) |
| `check_claims.py` 把 19、224 写死在代码里 | 属实 | 改为「教程宣称值 vs 实数」定点比对,测试真增减时不必改检查器 |

**但它的唯一一条内容定罪判错了。** 它称 13 页「Pro / Max / Team 从 auto 起步」
与当前官方文档不符、`auto` 不支持 Pro。实查 `permission-modes.md`,**三处**原文:
「On Pro, Max, and Team plans, the built-in starting permission mode is auto mode.」
Pro 明确包含在内。这条断言至今被挑战三次(v2.1 我自己误改、第一轮评审、Codex),
三次都站住 —— 它是全书被验证得最扎实的一条。

**修复过程中我又自造两个 bug,一并记录**:`_猜基线()` 用了 check_layout 里
根本没定义的 `ROOT`(直接 NameError);把 `build_registry` 整体包进 `main()` 时
**连多行字符串字面量一起缩进了**,导致重生成的 `sources.md` 有 74 行内容被加了
4 个空格。后者的教训:**包 `main()` 不能无差别缩进,含多行内容字符串的脚本
只能包住副作用段**。已核实另两个先前包过的脚本只有单行 docstring,未受影响。

~~**Codex 指出但本轮未做的**:根 CI 只跑站点与口径检查~~ ——
**2026-09-03 已做**:两页真值样本随仓库发布(`tools/layout_rules/fixtures/`),
根 CI 从两步扩到四步(+版式巡检 +六个规则模块自测)。
仍未做的只剩「教程与配套项目跨仓库同步」的云端原子门禁 —— myshop 是独立仓库,
这条需要跨仓库触发,**维持登记**。

### 第三轮全面检查修复(15 条定罪 + 2 条回归失败,教程侧 22 处)

三路评审(8.5/7/8)+ 逐条对抗复核:16 条定罪确认 15 条、打回 1 条。
最重的两条踩在「真实输出」红线上,全部用本机实测原文回灌:

- **18 页 pytest -q 进度行是任何环境都跑不出来的**(本仓库 addopts=-v 下
  -q 输出按文件分行,且模块级 importorskip 不产生 s 进度字符;两个 pytest
  大版本实测证实)。已替换为屏蔽 playwright 后的真实输出
  (tests	est_api.py ....... [ 41%] 三行 + 17 passed, 1 skipped in 3.90s),
  term-bar 如实标注 Windows 重录。
- **07 页 SKIPPED 行号 :21 与现行文件不符**,实测应为 :23 且 reason 含
  && playwright install chromium(pytest 报 importorskip 调用行,-rs 不截断
  —— 均实测证实);同页 E2E 代码块改为**从磁盘取现行文件逐字节选**,
  杜绝「代码块与真实文件漂移」这一类问题再犯。

**照抄即坏类**:18 页门禁 3(pytest -m p0)在未打标项目退出码 5 直接红 ——
三处分级处理:ci.yml 加前提注释、「可以直接抄」补前提句、22 页速查加括注;
并让教程吃自己的狗粮:**myshop 按 18 页第二节打上 p0 标记**
(feat/p0-marker 分支,pytest -m p0 → 1 passed, 18 deselected,待 PR 过门禁)。
02 页验收清单补半句(第 18 页会追加 markers 段)。ci.yml job 名「三道检查」
与四道门禁的名实差补注(改名会与保护规则脱钩 —— 本身是个教学点)。

**其余**:22 页术语表「替身首见 07」→ 20(07 全文无此词,20 页首次出现处
补 mock/stub 定义);21 页两处重复 style 属性合并(第二个 style 按 HTML
规范被丢弃,表头着色实际不生效 —— headless 实测证实);14 页「全量只要
3.66 秒」→「三道加起来 4 秒上下(pytest 占 3.66)」;kb 全部「三段式」残留
(00/01/07 共 4 处)统一为四步、"这六个字"实为七字、小结补核实步、
kb06 警示框从第 1 节概念卡内移到第 5 节 fork 示例之后(第二轮插错位置,
回归复核抓出)、上轮新增文案 6 行半角标点转全角(kb 体例);
03/22 两处未链页码由 link_pagerefs 幂等补链(共 +3)。

**工程**:build_zip 可复现化(固定条目时间戳,重打两次同 SHA —— 实测
5D7F4131 两次一致;此前 mtime 入包,内容零改动重打即换哈希,没法对账)。

~~**评审存疑挂账**:det_dangling_edge _on_start 按元素吞异常(工程债)~~ ——
**2026-09-03 已修**:解析失败不再吞掉,报成一条 `svg-parse-error` 检出
(变异验证:注入必抛故障后从 0 条变 17 条)。
**仍挂账**:14 页 Ctrl+G 行为无一手佐证。

### 第二轮复检修复(36 项)+ 口径巡检机制

三路复检(主线/kb/工程)后按定罪清单修复。**复检自己也被抓出三条误报**
——「01 页页脚含 1 处修正」(实数:无)、「直角引号 12 处」(实为 3 处正确嵌套)、
kb「目录名由 git 推导」定罪的样本全在非仓库回退分支——全部以脚本实数或官方
原文裁决,不信转述。

**主线**:00 页 12/13 卡片过期数字、「提示词模板在 22 页」错误指向、
SonarSource 例页码、224 行口径(核心三模块 224 + 网页层 150 = 374,06 页表补行、
00 卡片同步);09/17 页「18 条测试」→19(实数);05 页耗时表层号(接口③/E2E④)
与 180× 基准脚注;19 页第 5 幕落点(h2 补幕号 + 前向指引);04 页 _demo_bad.py
补「不随项目分发+自建复现」说明并使示例与输出同型;11 页最后一处 daijie27
路径残留;07 页 E2E 嵌入代码与真实文件对齐(夹具地址 + 双 importorskip),
06 页目录树补 conftest.py;22 页表头「防线」→「事项」、阈值表补棘轮行、
git clean 统一 12 页口诀;03 页收窄限定补文件名;14 页 kicker 重写与弯引号;
02 页 New-Item 补 -ItemType File;20 页「50%」→「约一半(9/17)」;
18 页 pyproject 片段加「追加别整块替换」;全角括号 14 处归半角。

**kb**:01 页 SVG 与四步代码块对齐(去步号+图题改四步+核实位置说明);
05 页记忆半句改双分支(仓库内按仓库/仓库外按路径,官方口径);07 页行数刷新为
实测 197–678、表格 allowed-tools 行挂钩警告框;02 页「唯一一处」→「唯一一处
集中说明」;03 页补分隔符说明(官方:空格/逗号/YAML 都合法,又一条评审存疑
被官方文档解决);06 页警示框转义 &&、补 -m、移到示例代码块之后。

**工程**:E2E 端口 0 化 —— 实测占用 8000 后仍 2.93 秒全绿(原为 62 秒
超时假失败);conftest 改 MonkeyPatch.context() 包全程 + join 后 is_alive
检查 + 「轮转保留」如实注释;det_collision/det_overflow 吞异常改 raise
(与 edge 统一),兜底错字修正;check_layout 加 UTF-8 控制台保护(cp1252
实测崩溃);link_pagerefs/patch_pages 包进 main()(此前 import 即改写
23 个 HTML,已验证 import 零写盘);「六条规则均通过真值验证」收敛为
「3 真值门禁+1 合成变异+2 无」;zip 由新脚本 tools/build_zip.py 产出
(白名单缺件即拒绝打包);SHA 附录「新增文件」段由 git ls-files 全量重建
(44 文件),脚注更正为 _shared_head.txt 实改两处;新增最小 CI
(.github/workflows/check.yml,跑 check_site + check_claims)。

**新机制 tools/check_claims.py(口径巡检)** —— 本轮根因药:同一事实全站
只许一种数,每条现场重数(页脚求和、def test_ 计数、wc -l、关键词残留),
27 项断言,不平即非零退出,已入 CI。「为什么上次没发现」的答案从此是
机制而不是道歉。

~~**遗留工程债(登记不做)**:六 det 模块硬编码路径与零断言自测、三套重复
SVG 解析器;check_site 跨组锚点校验限制;pytest 9.1 importorskip 收紧观察。~~
**2026-09-03 逐条了结**:六 det 的硬编码路径已改为 `_baseline.py` 统一解析、
自测入口退出码已生效(变异验证:`check()` 恒返回空时六个全部 exit=1);
check_site 跨组锚点限制**已修**(改为全站 id 索引);
`det_dangling_edge` 逐元素 `except: pass` **已修**(解析失败现在报成一条检出)。
**仍未做**:四个模块各有一套 SVG 解析器(重复但都工作正常,合并属重构,收益低);
pytest 9.1 importorskip 观察项维持。
**已决策并执行(2026-09-02,用户批准)**:删除只读原件目录中被误写入的 _depmap.js。删除前三验:与项目版逐字节相同(无信息损失)、原版 23 页零引用(无断链)、删除后基线 23 页哈希与附录「改前」列 23/23 吻合、真值自检三条全过。只读目录自此恢复原始清单(28→27 文件),本次是该目录唯一一次授权写操作。

### kb 姊妹课 A+B 处置(修定罪错 + 诚实标注,22 处)

用户在「直接修 / 挂警示 / 完整重核」中采纳推荐的 A+B 组合。
「逐字节复制」原则就此正式破例 —— 原件仍在只读目录作为备份。

**先核实,两条评审定罪被官方文档推翻**(2026-09-02 对照 code.claude.com/memory.md):

- Windows 受管路径 `C:\Program Files\ClaudeCode\CLAUDE.md`:**kb 原文正确**,
  评审按「应为 ProgramData」的类比推定是错的 —— 官方文档两处原文照录。不改。
- 自动记忆「按项目路径分目录」vs「同仓库工作树共用」:**两句都对**,`<项目>`
  目录名由 git 仓库推导。评审假设目录名来自文件系统路径才推出矛盾。
  仅补半句消歧,不改原句。

这是 13 页教训(信转述改错)的第二次红利:不拿类比当定论,先抓一手原文。

**实际修改**:

- kb/00:知识点计数三套(53/58/67)统一为实测 h2 数(67,各页 11/10/8/9/11/8/10,
  脚本自数不信转述);「均已核对官方文档」过强声明改为诚实版(2026-08 编写时核对、
  未经二次重核、以 claude --version 与官方文档为准)。
- kb/07:SKILL.md 的 allowed-tools 一轮失效矛盾**不改流程、点破成教学点**
  (新增说明框:「继续」是下一条消息,批量写文件那轮预授权已失效,两条出路);
  「600–900 行」tip 补自检坦白(8 页实测 197–667 行、5 页不达标 ——
  可验证的约束配上不执行的检查等于没写);两处 mkdir -p 补 Windows 指回。
- kb/02:「~ 是什么」框补全课唯一一处 Windows 对照(~ 对应路径、PowerShell 的
  mkdir 默认建多级目录去掉 -p 即可、资源管理器显示隐藏项)+ mkdir 块指回。
- kb/06:context: fork 推荐处新增警示框 —— 后台 fork 写的文件一键撤销还原不了
  (主线 13 页官方限制),批量写文件前先 git 提交。补上评审判定的
  「无安全网批量写文件」互补缺口。
- kb/05:三处半句 —— 记忆目录名由 git 仓库推导的消歧;子目录层与主线 21 页
  三层图互指;规则优先级与 02 章技能优先级方向相反的提示。
- 主线 21 页:figcaption 补一句「受管策略层与 CLAUDE.local.md 见姊妹课 05 章」
  (全书唯一一句跨课指引)。
- index.html:姊妹课卡片补小字「版本类内容 2026-08 抓取、未经重核」。

**明确不做**:kb 视觉统一(属改版);版本号全量重核(C 类工程);
已自标待验证的内置技能清单与 /config 行为。

验收:00 页计数与各页实际逐项相等(脚本断言);check_site 全站 PASS
(姊妹课 8 页 0 问题,含新增的跨课链接);check_layout 真值自检全过;
六页渲染抽查正常。

### 三路评审后的缺陷修复(P0/P1/P2)

对全部产出做了三路独立评审(主线 23 页 / 姊妹课+入口 / 工程配套),
按定罪清单修复三层:

**P0 · 12 条事实/口径错误(19 处编辑)**
入口页「四道防线」→ 五道,「已被取代」标注改为逐份说明(Output Style 对比
未被主线取代,后继是 kb 04);22 页「六道防线卡」→「六件事速查卡」并加口径
说明;00 页 260 倍 → 180 倍,「两轮核实」分组 14/33 → 25/22(与逐页页脚一致,
确认列同步调整保持行列自洽);退出码 2 两页统一(官方口径「被用户中断」+
本机实测「收集出错同样返回 2」,pytest 官方文档 + 实测双证);13 页 lede
4 处 → 6 处;14 页 kicker 前置与依赖图统一;08 页附注「后来补了」措辞修正、
对比表①补「仅跑 test_price」脚注;03 页发现规则补配置收窄限定 + `*_test.py`、
知识点 40 行文不对题修正;kb/01 步骤编号断裂补第 2 步「核实」;
sources.md §三两条 ✅ 补快照时点注记(生成器 VERIFIED 常量同步)。

**P1 · myshop 与发布物**
E2E 三个真 bug:URL 唯一事实源(conftest 夹具产出,测试消费,删除重复常量);
数据库隔离(MonkeyPatch DB_PATH 到临时目录 —— 此前每跑一次就往项目根
myshop.db 追加一条,已累积 32 条脏数据,连同文件一并清除);importorskip
双守 `pytest_playwright`(只装 playwright 时原来会 fixture not found 报错
而非跳过)。web.py 中文命名保留(与测试层一致的教学取舍),docstring 写明;
`Dict[str, list[str]]` → `Dict[str, List[str]]` 统一 3.9 风格;补转义安全注释。
README 四处同步(分支说明/双基线口径/目录树补 web.py 与 conftest/E2E 段重写)。
myshop-source.zip 重打:12 → **14 条目**(此前缺 web.py 与 conftest.py,
且 e2e 测试是旧版 —— 下载者拿不到第 4 层),SHA-256 见附录。

**P2 · 工具链机器层**
check_layout:退出码纳入巡检结果(发现问题即非零,此前发现 50 个也退出 0);
基线缺失从「静默跳过 + 退出 0」改为显式失败(退出码 2,可用
--allow-missing-baseline 降级但退出码仍非 0);修掉 `_段缓存` 的 `id(html)`
键 bug(CPython 地址复用使 21/23 页拿到上一页的 SVG 区间)。
det_dangling_edge 吞异常改上抛。registry 输入数据入库
(`references/registry_items.json` + `sources_v21_orig.md`),build_registry
路径改仓库相对 —— 453 条断言自此可审计、sources.md 可在任何机器重生成
(已实测重生成,注记保留)。check_site 扫描范围扩到总入口 + 姊妹课 +
第一版存档(此前 14 个 HTML 零工具覆盖;实测三组均 0 问题)。
SHA-256 附录全量重算(此前 22/24 行失效,冻结在 v2.1)。

**评审中定罪但按决策搁置的**:kb 姊妹课的系统性问题(纯 macOS 零 Windows
适配、版本号零出处、allowed-tools 矛盾、知识点计数三套)与 kb 视觉统一 ——
改动会打破「逐字节复制」原则,待用户在「直接修 / 加警示 / 完整重核」中选择。
kb/01 步骤断裂因属「照抄即坏」级别,已先行修复。

### 两课程合并与统一入口(v3 · 最后一项,已完成)

三套内容原本并存、入口分散:23 页主线、`kb/` 姊妹课(Prompt/Skill 主题,8 页)、
顶层五份第一版 HTML。现统一为:

- **`index.html`(新建)** —— 总入口页,与 23 页同一套配色与明暗主题。
  两条主线卡片(主线 23 页 · 姊妹课 8 页)+ 第一版五份的存档区
  (标注「已被 23 页主线取代,以主线为准」)。
- **`kb/`(复制)** —— 姊妹课 8 页 + style.css + app.js,**逐字节未改**
  (SHA-256 抽查与原件一致);原件中的 `kb/ai-workflow/` 系 23 页的逐字节
  副本,不复制,入口直接指向真身。排除 `.DS_Store`。
- **`legacy-v1/`(复制)** —— 第一版五份 HTML,逐字节未改,仅存档备查。

23 页与 kb 各页内部**零改动** —— 入口单向链入,不在课程页里加返回链接。
验证:入口页 7 条链接全部可达;kb 抽两页 headless 渲染正常;入口页截图核对。

### myshop 并回 main —— 被 19 页那道门禁拦住(活演示)

`docs/windows-readme` 并回 main 时,GitHub 分支保护原样拒绝了直推:
`GH006: Required status check "三道检查" is expected` —— 正是 19 页真机
演示配的那道门禁,连仓库拥有者也绕不过。**这本身就是教程结论的又一次实证。**
正确路径是开 PR 等三道检查:本机无 gh CLI,PR 需在网页端创建
(compare 链接 main...docs/windows-readme),CI 预期 17 passed, 1 skipped
(未装 playwright,importorskip 生效)。本地 main 已还原至与远程一致。

### E2E 真跑之后的教程基线同步(v3 · E2E 项收尾)

配套项目补了网页层(`myshop/web.py` + `tests/e2e/conftest.py`,myshop 仓库 `7b6c03a`),
装上 playwright 后基线变为 **19 passed**。教程侧同步遵循一条判断:
**`importorskip` 仍在,不装 playwright 的读者跑出来仍是 `17 passed, 1 skipped`**,
所以全书 macOS「真实输出」框的红绿数**一律不动**;真正改的只有三类共 16 处:

1. **`8 source files` → `10 source files`(8 处,02/03/04/08×4/14)** ——
   mypy 文件数是项目状态而非环境观测值:pyproject 里 `playwright.*` 有
   `ignore_missing_imports`,装不装、什么系统都报 10。
2. **三处现已为假的陈述**:06 页「没有页面」改为「页面只有一条最小下单流程」
   并在目录树补 `web.py`;07 页 E2E 标注「示意代码 · 默认跳过」改为
   「真实代码 · 没装浏览器驱动时跳过,装上就真跑」;21 页 CLAUDE.md 示例
   文件清单补 `web.py` 一行。
3. **08 页新增附注框(Windows 实测)**:装驱动后五次实验的新红绿数
   (② 10f/9p、③ 8f/11p、④ 1f/18p、⑤ 3f/16p),并记录关键发现 ——
   **实验④⑤里两条 E2E 全绿**:⑤ 的静默数据丢失,页面显示的是 create_order
   返回值,开真浏览器点完整流程也抓不到,只有第二层「换个连接读回来」能抓住。
   金字塔最贵那层上了,结论不变:上层全绿不代表下层没问题。

顺手修掉 06 页目录树的层号错:`test_api.py` 原标 ②、e2e 原标 ③,
与 07 页权威口径(接口=③、E2E=④)矛盾,已改为 ③/④。

另核实:**Python 基线方案 B 无需再做** —— 02 页第八节开头已有
「3.9 已于 2025-10 结束官方支持,新装请用当前稳定版」说明(v2.1 第 25a 项)。
`references/sources.md` 为 2026-09-02 快照,其中 E2E 相关条目(「示意代码」
「17 passed 基线」)记录的是快照时点的教程原文,本轮改动晚于快照,下次重跑
`tools/build_registry.py` 时自然更新。

### 版式与结构自动巡检 `tools/check_layout.py`

补上 `check_site.py` 的洞:它查链接、锚点、重复 id、导航、标题 —— 全是文本层面的,
**一条版式规则都没有**。而用户先后肉眼发现的两处缺陷(七步图三个节点没出边、
`.chk` 清单竖排)恰好都是版式类,自动检查一条都没报。

六条规则,归档在 `tools/layout_rules/`,各自可单独 `python det_xxx.py` 自测:

| 规则 | 抓什么 |
|---|---|
| `dangling_node` | SVG 里有入边、没出边的非终止节点 |
| `dangling_edge` | 箭头指向空处(端点附近没有任何节点) |
| `grid_text` | grid 容器里混有裸文本节点 |
| `overflow` | 元素坐标跑出 viewBox,渲染时被裁掉 |
| `collision` | 文字压在别的方框上、方框互相重叠 |
| `refs` | `url(#id)` 指向不存在的 marker、重复 id |

**准入门槛是真值验证,不是自说自话。** 只读原版里那两处缺陷还在,所以每条规则都必须
「在原版上报出、在已修版上不报」才算数 —— 一个永远返回空的检查器也能得到「0 个问题」。
`check_layout.py` 每次运行都先跑这道自检,不过就以退出码 1 退出:

```
通过  七步图 5b/5c/兜底 没有出边   原版 3 条 / 已修版 0 条
通过  箭头停在步骤 7 方框外        原版 1 条 / 已修版 0 条
通过  .chk 清单竖排               原版 7 条 / 已修版 0 条
[正式页] 检查 23 页 -> 0 个问题
```

**调参是实测反推的。** `dangling_edge` 先统计已修版 23 页全部 95 个带箭头端点到最近
节点的距离(0px×28、2px×9、4px×49、6px×6、8px×1),而真值缺陷是 10.8px 且垂直偏移
10px —— 容差 8px 正好卡在两者之间。`overflow` 做了变异测试:画布砍 8px 一条不报、
砍 40px 全部报出,并用浏览器 `getBBox()` 对 875 个元素做过独立校验。

**四条检出全部经对抗复核判为误报,一条都没修** —— 复核代理的默认立场是「这是误报」,
必须亲自读到原文才能翻案。结论反过来收窄了三条规则(见 `check_layout.py` 里 `收窄()`
的注释,那是收窄判据,不是白名单):

1. **导航图/依赖图豁免「无出边」** —— 00-index 阅读地图被报了两个断头节点,但
   `_depmap.js` 的 `PRE` 里没有任何一页以 19 为前置,它就是 DAG 的合法叶子。
   照检出去补边,反而会让 SVG 与唯一事实源冲突,**制造出一个真的缺陷**。
2. **`grid_text` 只报轨道写死的 grid** —— 07 页 `.term-bar` 是 flex,裸文本被拆开后
   只多出 7px 间隙,复核代理真机逐节点量过,与正常空格差 0.4px,肉眼不可分。
   致命的是 `.chk li` 那种列数写死的 grid:多出的匿名项会落进**错误的列**。
3. **丢掉 figcaption 数量比对** —— 22 页那张收尾结论卡的说明文字在红框**里面**,
   渲染正常。该判据零真阳性、已产生一例假阳性。

整合过程中真值自检**当场抓出了我自己的一个 bug**:导航图豁免判据查的是整页 HTML,
而 01 页正文有 43 个 `.pg` 页码链接(`href="12-git-undo.html"`),整页被误判成导航图、
真值缺陷直接被吞。已改成**按单张 SVG 的行号区间**判定。

### 01 页七步流程图补全 + .chk 清单布局(两处原版缺陷)

**两处都是原版就有的**,不是本轮改出来的 —— 已对照只读原件确认。

**一、01 页七步流程图:5b / 5c / 兜底 三个节点没有出边。**
原版只画了 11 条边,其中只有 5a 有出边(去步骤 7、回步骤 3)。
另外三格画完就断了,读者看不出「让它改」之后该去哪。已补三条回程边,
口径与 14 页流程图一致:

- **5b 小问题 → 回步骤 3**(还在这一步,改完再看一遍),青色虚线走右侧内道;
- **5c 方向错了 → 回步骤 1**(撤销后重新要方案),红色虚线走右侧外道;
- **兜底 全烂了 → 汇入 5c 的回程道,同样回步骤 1**,汇入点画了圆点。
  两者差别只在退多远,终点相同,如实画成同一条道。

配套调整:画布 900×540 → 960×600;步骤 7 与「推上去」下移 40px 给回程道让位;
5a 去步骤 7 那条补上「全做完」条件标签(与回步骤 3 那条的「步骤 6 · 循环做下一步」
构成一对);5b 横道在兜底下行线处画了跨线小拱;新增三个配色箭头
(原来绿线红线都配灰箭头)。图注补上三个分支各自去哪。

**二、`.chk` 验收清单竖排成一字一行。**
模板里 `.chk li{display:grid;grid-template-columns:26px 1fr}` ——
`display:grid` 会把 `<li>` 的**每个子节点**都变成网格项,包括 `::before`、
每个 `<code>`、以及每段匿名文本。于是

```html
<li><code>.venv/bin/python --version</code> 能打印出版本号</li>
```

被拆成两行:`[□][代码]` 占第一行,「能打印出版本号」落进 **26px 宽的那一列** ——
一个字一行。只要 `<li>` 里含 `<code>` 就必然错位,02 页七条里有六条中招。

改成相对定位 + 绝对定位的方框,行内内容恢复正常流动,**不必改任何 HTML**:

```css
.chk li{position:relative;padding-left:26px;margin:9px 0}
.chk li::before{content:"±0";position:absolute;left:0;top:0;...}
```

该 CSS 内联在 21 个页面加模板里,共 22 个文件同步修正。
(同模板的 `.step` 也用 grid,但它的子元素是两个真 `<div>`,用法正确,未动。)

### 断言来源总登记(v3 议题之一,已完成)

`references/sources.md` 从「本轮 26 项修正的依据」扩成**全书断言来源登记表**,
由 `tools/build_registry.py` 生成 —— **表格直接来自数据,不经模型转写**。

- **清点 453 条**可证伪断言(命令行为、配置默认值、版本号、官方文档转述、
  第三方服务行为、实测数字);教学口语、类比、价值判断不登记。
- **与教程自称的 193 条既非同一批,也非其子集或超集** —— 教程从未列出那 193 条,
  只标了出错的 47 处,原始清单无从复原。条目边界、拆分粒度、纳入门槛都不同,
  两个数字对不上是必然,文档开头已写明。
- **三个标记体例的坑**(已逐页 grep 核实,写进文档):
  🔄 = 经核实修正(源码 90 处 / 12 页,页脚声明 47 处 —— 同一处常在正文和
  知识点回顾表各标一次);🔥 = **重点强调,不是修正**(52 处 / 14 页);
  ⚠ **绝大多数是警告框图标,不是待验证** —— 全书真正的待验证**只有 1 条**,
  在 17 页第八节。
- **风险分级**:高 131 / 中 147 / 低 175。高风险高度集中 —— 13 页一页占 41 条
  (几乎全是产品 UI),而那一页已对着官方文档逐条核过。
- **第三节与第四节严格分开**:只有真查过一手出处的才进「已独立核实」,
  其余一律「仅登记、未独立复核」,「教程自称依据」一栏原样转述教程说法。
- 附「维护建议」:按类别给出复核周期,并记下这轮踩出来的三条规矩
  (不要转手核实、改之前先确认原文是错的、不许把未复核写成已核实)。

生成过程中修掉两个自身 bug:`marked` 归一时子串匹配把代理写的
「非『待验证』标记」这种**否定句**误判成待验证(4 条误报);
以及重跑时把上一版登记表整个吞成附录(文件从 154KB 涨到 307KB)——
「旧内容」改从 git HEAD 的原件读取。

### 14 页第三轮 + 05/15 页 Unicode 码位

- **⑥ 循环补上条件分支标签**：⑥ 有两条出边，原先只有「全做完 → ⑦」带标签，
  回 ③ 那条没有条件说明，读者看不出它是「还没做完」的那一支。已在 ⑥ 的回程出口加
  「还没做完」标签，与「全做完」构成一对；说明框首行改为
  「⑥ 是个岔口：方案还没做完 → 回 ③ 做下一步；全做完 → ⑦ 总审。5b 也回 ③，但它是『还在这一步』。」
- **05/15 页的圆圈零是未分配码位**：两页共 8 处用了 `&#9311;`（U+245F），
  该码位落在 OCR 块的未分配区，**多数字体直接渲染成空方块 ☐**（已渲染对照确认）。
  正确码位是 `&#9450;`（U+24EA ⓪）——14/20/22 三页本来就用的这个，全书本身不一致。
  已统一：05 页 5 处、15 页 3 处全部替换，现在五个页面口径一致。

### 14 页与 17 页流程图重画（第一轮）

两张图都有画法 bug，不是审美问题：

**14 页七步流程图**
- `5c → ①` 的边终点写成 `L 271 16`，而 ① 方框上边缘在 `y=20` —— **箭头穿过整个方框**从顶上冒出来；
- `④ ↔ 菱形` 的连接线从右往左画（`x1="348" x2="332"`），**箭头方向反了**；
- 菱形左顶点 `x=336` 压在 ④ 的右边缘 `346` 上，**重叠 10px**；
- `y=108`/`110`/`128` 三条横线贯穿全宽挤在同一条带里，是「看着乱」的主因。

重画后：菱形缩小到 112×80 且与 ④ 留出 28px；两条回头路（⑥→③ 做下一步、5c→① 撤销重来）
全部移到下方独立走线，用灰实线与红虚线区分；`5c→①` 绕行左侧与顶部，**全图零交叉**。
顺带修掉一个我自己的疏漏：第三条 bullet 在 `y=530` 而 viewBox 高 524，文字掉到画布外，已扩到 546。

**17 页三问判别图**
- 菱形过宽：问 2 右顶点 `620` 与问 3 左顶点 `610` **重叠**，问 1 左顶点 `160` 压在起始框上；
- 连接线画在菱形**内部**（如 `168→176` 落在 `160..380` 的菱形里），渲染出来像**箭头指向左**；
- 问 3 的「否」是斜折线 `M 800 86 L 830 120 L 750 190`，正好**穿过「是质量门禁」框**。

重画后：菱形缩到 116×72，一条直线主路（拿到环节 → 问1 → 问2 → 问3 → 是质量门禁），
三条「否」改为竖直下落到对应结论框，用 `--bad` 色区分，**零交叉零重叠**。

## 十一、v2.5 09/11 页示例漂移

审读发现这两页的「真实输出」与正文叙述用的不是同一个项目,读者照着敲会得到完全不同的东西。
**处理原则:真实输出一律不伪造,只如实标注它出自哪个项目,并让命令与输出不再互相矛盾。**

### 11 页 git 基础 —— 实际比审读说的更乱

全页七个终端框,实测掺了三个项目:

| 命令 | 输出实际来自 |
|---|---|
| `git init` / `git status` | myshop ✓ |
| `git diff` | 一个用 `app.py` 的项目 |
| `git diff --stat` / `git log --oneline` / `git log --stat` | 一个用 `report.py` 的三步练习(读取分数 → 计算平均分 → 输出报告) |
| `git commit` | 提交信息是 myshop 的,但分支显示 `[main …]` |

而这三条 `git diff --stat` / `git log` 的命令行都写着 `cd <你的项目>/myshop`。

改法:

- 在「六、看改动」开头加一个说明框,讲清这一节和下一节的输出来自一个更小的练习仓库,
  以及**为什么**要换(myshop 一次改动牵扯几个文件,diff 长到看不清重点;这一节要讲的是怎么读 diff)。
- 把那三条命令的 `cd <你的项目>/myshop &&` 去掉,改成项目无关的写法,命令与输出不再打架。
- **`[main b27d579]` 那处不删不改,改成一个教学点**:新增警示框指出方括号里是当前分支名,
  你若照第二节做过 `git switch -c feat/…`,这里应该显示自己的分支名;
  看到 `[main …]` 就说明分支没开成,停下来先补上 —— 直接在 main 上攒改动,
  正是第 12 页那些回退死角最难收拾的场景。

### 09 页局部 vs 全量 —— 两个「全量耗时」并存

页面已有说明框交代了复现项目的函数写法与 myshop 不同,但**没交代耗时口径也换了**:
开头讲「全量 3.66 秒」(myshop,18 条测试、含真启 HTTP 的接口层),
末尾算账用「全量 0.03 秒」(复现项目,4 个文件 7 条纯函数测试)。两个数字都真,量的不是同一个项目。

改法:说明框补一段讲清两个数字各自的来源与差别;对比表和成本账里的 `0.03 秒`
各加一行小字标注来源(并在成本账处并列 myshop 的 3.66 秒),避免被当成同一个项目的数字。
页脚说明同步更新。

## 十二、遗留（下一轮或 v3）

已销项：`feat/depmap` 已合并进 `main`（`1c55ccc`）；全书断言来源总登记已完成
（`9186265`，见 `references/sources.md`）。

~~**尚未做的三项 v3 议题**~~ —— **三项均已完成**(见后文各自章节)：

1. **两课程合并与统一入口** —— 顶层四份第一版 HTML、`kb/` 那套姊妹教程、
   现在的 23 页，三者并存且入口分散。
2. **完整 E2E 环境** —— 补一个最小网页让第四层测试真跑。
   **会改变 `17 passed, 1 skipped` 这条贯穿全书的基线**，02/06/07/08/18/19 多页要同步。
3. **Python 基线升级** —— 教程实测环境是 macOS 上的 Python 3.9.6（3.9 已于 2025-10 停止支持）。
   升级意味着重录一批终端输出。

**新增的两项待办**（本轮工作中浮现）：

4. **高风险断言复核（131 条）** —— `references/sources.md` 第五节已按类别和页码排好序，
   13 页一页占 41 条。这是一份可以直接照着做的清单，建议每半年跑一次。
5. ~~版式与结构的自动巡检~~ —— **已完成**,见 `tools/check_layout.py` 与
   `tools/layout_rules/`,六条规则(3 条真值门禁 + 1 条合成变异 + 2 条无真值)。

## 附录：被修改文件 SHA-256（前=只读原件 / 后=claude_AI 产出）

| 文件 | 改前 SHA-256 | 改后 SHA-256 |
|---|---|---|
| ai-workflow/00-index.html | CC27E161CB048162F5D937528096DDF5EC20CB3F23EC801634E1BF5035DF1FB7 | 6901E7601CF7CD2B019371AE6000E3D592F53FAE9C630BEFFEE6BFC448694E06 |
| ai-workflow/01-overview.html | C5615CE3E10E7E5F8C4A35954E414AE2F361AF81732C113B2AD0FF374D24C74B | AF52FCE2ADDB5FACACBD5578B2E47C23BD671E0CB0FAF03E962906509AD4F65C |
| ai-workflow/02-python-setup.html | 93B68B4191575889E5B3A0E2AB7FE250B5F5079255AC2C213DF65AAA04E5D70D | CC1BAA24F7B35465CE77260D5549F1C80D55CD679AD88A48663B44E6E056770F |
| ai-workflow/03-test-basics.html | 9FF16CD68930945C323CDBF8B7168280881FB0C7E575F4C43A9327A1F70F1E0A | A015224CC8DFABB07156C09EFCE95D7FC4B07256BB945012568A5C66A7C9F425 |
| ai-workflow/04-static-check.html | 897B6FC7D4C6D9019C5A9FD495EF15A0DA07BB51E152C46BEB9CAB17218C34FC | D0B6349AAE77E09E85C7C664C56C8AD49440983490AA2AF2235FF4153F37F5F0 |
| ai-workflow/05-pyramid.html | 4AB1F3BACB4224A3D1ABD12D1F5C1C13AC56F57FDCFA890AF72F6A7D969E0498 | D8D076308522B1F80E35F269056DC54F756CDC0916C2031948CB8DBB17599D58 |
| ai-workflow/06-myshop.html | 2A45A632568354CA59199ED15570A4B7461E8927B3CC07251187A55E018328E2 | 8455C616059D9FB3BCA21A384CECDAFBE7359451EEEE6149EB2BA81C1EFA3ED8 |
| ai-workflow/07-four-layers-code.html | E3F7A8A53AD984558BCED856ED45CAFC73829138556180B16E3BEC17EB8124AD | C92CECF69F663829EB6C1212806A5FCD05C07711E7F2F40A38A00CB4E1BF5A50 |
| ai-workflow/08-break-it.html | E3EFF6AB840040FECB42D5A0C5A25DEBF33C573C29C40128E8D5E32AB4A7E9A6 | C876B7B8B564290E688971038BB7E82F99251940891CD56E2A73C91FACB6138B |
| ai-workflow/09-partial-vs-full.html | 62B797A3FDBAED997EF30966313697A8C73DDA252CBD6E0402C5EF77561894E6 | 97E9A301C9DAD9F47C0C5DEBFEC0EFC0425865DA45A1E930735E2D71D04126F9 |
| ai-workflow/10-flaky-e2e.html | 5334B2AC143A2CB8BC0387DA96F6559E78478CA53193D66C85A71CF41C8E1BB0 | 1B2C3E6C31968817B9EB5F72066889579B4E1A1A83E5367989C9F9DF6D569293 |
| ai-workflow/11-git-basics.html | 5BBC9C21262EF75F7FD21A3CEC4582E63424B845FBCF01116AEDFEEFADE7CE33 | E396A0A750FD707E64DFBD528A7D5BE4F4062AB5EB77DB43A4EA7DAF69A605C0 |
| ai-workflow/12-git-undo.html | 92D83C188737A672BB710D22BF96A3B1980D7C6BD824133CF1C74B21BB90F350 | 6F9BAF1765438BED5DCD148731BDFF203D0A8D220AB8493F18EFED20E23065E7 |
| ai-workflow/13-claude-code.html | 536DB94F84174E8615D3825D44B98C6793B4F845BACA2ABCA0A6597ABDC3CDE2 | 03BA89AAF2EC8FEE4601B7D383630E68DA7A31F081F1D0725B53B5247A479B18 |
| ai-workflow/14-workflow.html | BFD09D1067D62BF0F9E4C1B895EB7887680A7CC66DC1C8BD4A22147FF6CB2894 | 40B3D443F1ABC8D23CC61D342B0E9E9AE59B35070AA492B18DF58EA057D564F4 |
| ai-workflow/15-team-roles.html | 756E6DCF328EDEF2C631F7603B900199C598B761A04F844E04D76D49F0765717 | E79EA44533F59D61B246CEFEB1577EE1C6A23C7C8D0FD2D08F8C2F458C633FDE |
| ai-workflow/16-pipeline.html | 0E258A42EDE278F91FAF96DDF58C704E938E0A0972639244995E09BFDCB0DC2E | AD607ED14EF4B40112526B8826CF7D5996D0E4336B93B1B0CE7714C7595BC4F2 |
| ai-workflow/17-quality-gate.html | 6BB9810F229C696064C017BE428F96E7D5006C66D447E1ABE61FEF4EEC3E4A1A | B5B0EAFD7525D033E718869504AFF972D2D339D177543DAC71A64A9AC69FB6D3 |
| ai-workflow/18-gate-items.html | 49A72C7019A20D936367E1B8928159AB1D09BEC472933BB9C99838158A770E50 | D91867A6B3562F23463B6149380BA91BF159CB1B489CE5F370F9357571391B5A |
| ai-workflow/19-gate-demo.html | 64CA82203E7396664BD9B23469B07D85010BD580BEBC662E64EB6C3FE5BC7CDB | 82905E1DBF18F7CC1F64A4A7EEC591F50FC1589643C16856DECAA7959142763E |
| ai-workflow/20-ai-boundary.html | 01AB08A21B821EF29CC78169906EF180934C618F56E499433D054880C7FE0638 | FFD8685E7520A7B6EFD76FF96E3B6D71CA96B7218CC952E3A1917D3613D7CF7C |
| ai-workflow/21-automation.html | 702A24ED7490D930FA38D0ED5298F5A88D8C718F35B078A89378E9A4B5CF4BDB | F18844F172DA26B7F141ED0D4DCEA9565B7D8801073DA895912EA0466564CBC1 |
| ai-workflow/22-cheatsheet.html | CAD57ACDE34977858D9CF76B0C8FF4FE6A6713B11827CB2E395E24D2B9AA0101 | 500768DAFED9299F6AD209F28532E5282AA69348B1CDCE153FF96EB9295F09E3 |
| myshop/README.md | 7E5740F1946ABAA7E4BA05C43F9AA285F45E2BFE1DE018A3C23923BB19E421B2 | EFD341DB5BE9E3FCD434D01FBE0D278E8580A438B5564D59DA0AB1BE2F8C1285 |

新增文件(全量,由脚本从 git ls-files 生成;改前列 — = 原件中不存在):

| 文件 | 改前 | 改后 SHA-256 |
|---|---|---|
| .gitattributes | — | 616F43A37D3827A711B8A54E05381399D45DF5CEF6CCB906B8A4F8A17AD5D763 |
| .github/workflows/check.yml | — | A1DC044482DF178083C5D8BA8B570D2271F066F2C2CEE5BC1F43D1792F80C452 |
| .gitignore | — | 166FA46CCFF9621E621F4017CF07546C8506E6A7E04F1B242FD6E7817261420E |
| ai-workflow/_build.sh | — | 0EF9E096968683279A29FCEA2C3188EEC7084B900EADD2176A96657CB3BC030F |
| ai-workflow/_depmap.js | — | 575CD312D5D55B5216622F7817E2C98D7579F625967BBF3F612885944423D136 |
| ai-workflow/_shared_head.txt | — | 25E636D02A206669C3539D755CE24A341B728E8335A8118DC529CF3FDB359E17 |
| ai-workflow/_shared_tail.txt | — | 597C9CE9468DEDF6CA2AB99196B4141DC98AB92CC64A25C96FEEC0F9332D298B |
| ai-workflow/_v1/00-index.html | — | A790748D5D59DD85E91E227A18E3E6C99FB1353D92982D2FEE1C2E7FE4CEB179 |
| ai-workflow/_v1/01-overview.html | — | B5AB3DD47A0D363128B05936DA3FFD0744F07F1ECE1F80AB13F257E8B3996C65 |
| ai-workflow/_v1/02-python-setup.html | — | D7B76042380390FB41A441492404CAD6A350F7AD7F3D36380135683D4D11B3C0 |
| index.html | — | 4D781A1B5FF8A4A756BC9C6D39AA17505B2358E73F842E609E21BA72AC908A43 |
| kb/00-index.html | — | 7176B8F9726BD909920220FAD0DF4CE6FF4B13B8F69FEA2B7BF7F4F6CB85F788 |
| kb/01-prompt-design.html | — | 1A0410E8DE09E29A7A5BF2CA7A3CB3752A51A1A812A5C5721584DF1365DE8193 |
| kb/02-skill-basics.html | — | E59BE6B6229B3F4462746FAFB73EFD8D718AFD8C605E6118C46AC515B6BBB0C1 |
| kb/03-skill-advanced.html | — | 710BAC318D80BF49413514552FDFD3D52CD869DEB5EAC58A5E6DAF2AB0AC52FE |
| kb/04-output-style.html | — | 276086C1D8E69DE26BDFE2CE0974E97046976BF0A9A8E869947F8A2BB5DC56A7 |
| kb/05-choose-where.html | — | B168594B15EA28CA917CF9DDA4D7357447747DB576C00D5C234B0C793470DC23 |
| kb/06-subagent-fork.html | — | 1E3722D4A0E958B38A80AAC871AC93256C385852D50D504D233E29082296D57B |
| kb/07-case-study.html | — | EE6477DCA5785757462D5BB733A2B3540CF09648EF1EA2DA8E0DBCD68A9C2755 |
| kb/app.js | — | 479736BD4497EA2FE2F5DF31762496BA5F70CCB5FB2D3D91AAB7A087D0FE4881 |
| kb/style.css | — | 4187DD170B51585EDD31F43D080451CC10A403FD9B64EA8775BFC1F2165DD99A |
| legacy-v1/claude-code-glossary.html | — | C5AF9AF52C78A4650F7156D5E7E0807AACBDE55B31743EE2D1B383D1F187A281 |
| legacy-v1/claude-code-pipeline.html | — | 427F2164F85E19AB080CC1C0DB83E4BA7282DA9758E227521BC34C39C80B83F6 |
| legacy-v1/claude-code-sop.html | — | 72FFD6FF9F1B7981D9D6FFF7C641FA907D58B7A525DAEE73D678B18512F30455 |
| legacy-v1/claude-code-testing.html | — | CBFA5CD3D39CCF3DC251A0DEB60A3926EDB77AB64064D0C7FE8BF8324E974921 |
| legacy-v1/output-styles-compare.html | — | 1B637DEFB54C614B303E9CD273021D2C7C43620156EEF301B39A58C8C2CE4B72 |
| myshop-source.zip | — | 1380B4C5FB470E219BD7E2F2564E6247596193A9C54DAFF18A0F1F3F559E7AC2 |
| references/deps.md | — | 798C2003C8CC482D8CC23342F2103CC400E3C240C9D4C4DA671E6ED0B54340E2 |
| references/registry_items.json | — | 157318258B501D44BF60589FAA802C714C3AA4A47ED47EFD2AAB4F7C08E782B2 |
| references/sources.md | — | 7F0DAED657F92321577DEC48306F15F29E3BC22955E1B07EB33CC59699A81EF9 |
| references/sources_v21_orig.md | — | 3CEA8B8310EB5B83E88C65C735B1FC3B57C5FBA3F0D1347B66712BFC4E8E2EA4 |
| tools/build_depmap.py | — | 09288624A6B9567036E3932BC5FB25C3693C152123AAD30AAD68BB07C7B208F2 |
| tools/build_registry.py | — | 724E2503896986C60BA2CDF30C22FE8FA7EB2E356BD86D66384D0EDC74221BF7 |
| tools/build_zip.py | — | C1FDF8683219C6815115AA6E609C0A80269603BC9B272B40D830F23DEB976446 |
| tools/check_claims.py | — | 142C9E73473775745CF5142B2144BCE322CF36B7BCB134F6DA3913EB53D0749C |
| tools/check_layout.py | — | 8C18DC4A037C8C6D1A4B425E53ED2C8EB730E8AE23628EEE652F7DE3572B4030 |
| tools/check_site.py | — | 1098820402778DF9E7608C9771D36EDF7651E0564F3214138E21FDDA7A32D72A |
| tools/depmap.tpl.js | — | 770FD4062E71281EBF2582CE1CAAC16AD4FF10C11128C49454A3CD4F997DFBB0 |
| tools/layout_rules/README.md | — | 96B88BF4683D9B533D2A09888B461BB6CB0FBE39602BA31ED44BCF15C9CB2F30 |
| tools/layout_rules/det_collision.py | — | B0FC56EF3A1CF06F1C3F5FA105167A8369BBE7B83906D92CF360A2DE89121079 |
| tools/layout_rules/det_dangling_edge.py | — | A65DCD9F722CA4E3CE28538F5927FC474FD6CC8F6EF188BBC92AF96485D5E6D6 |
| tools/layout_rules/det_dangling_node.py | — | 10B9B50BD84E56B73ADE3BA997D0D7A88518693195FCB85FD275F0B63766B13B |
| tools/layout_rules/det_grid_text.py | — | B1C39033DC106C6DA6DE29D4AD7FC4A42DE149DD17441C906AECC17874848F58 |
| tools/layout_rules/det_overflow.py | — | 6E66A70B59869EAD6E33D51BD300BEBAFEA07D41EED8DFAB89DF3AB6F47D4852 |
| tools/layout_rules/det_refs.py | — | 6CCCAB108CC3FCB98C1260FD0E9F81D29829231DD9EFFF6734728F0238D171A9 |
| tools/link_pagerefs.py | — | 6E246DA7EC53826156FECEEAFBF617DFFB42CADA1D8FD11F5563A10D207BB36C |
| tools/patch_pages.py | — | D0DEEA5A3F764217CB247DE28A172FD67A2624BF9E173CA5B229579375ACE970 |
| tools/layout_rules/_baseline.py | — | 978826C66AEF31AD4F205FF21D1F24F8E9A603876B435ED5E1801C12F43591BE |
| tools/layout_rules/fixtures/README.md | — | C85C024CB2BCFE6E5092133479CF402EFF68CC3A46FD8EE95F29CE1E476C9CEB |
| tools/layout_rules/fixtures/baseline/01-overview.html | — | C5615CE3E10E7E5F8C4A35954E414AE2F361AF81732C113B2AD0FF374D24C74B |
| tools/layout_rules/fixtures/baseline/02-python-setup.html | — | 93B68B4191575889E5B3A0E2AB7FE250B5F5079255AC2C213DF65AAA04E5D70D |
| ai-workflow/_cmdos.js | — | DBC8161C4CA924A1FA7E3857E00F7639F81B337C040CA929495FD571FC687F10 |
| tools/check_crossrepo.py | — | 4E2A7664FDD68636F9EE4D254613CD8E0028A55ABF96201C449D6EB571B47427 |
| tools/tests/test_check_site.py | — | 05292B68A3BF32E3ED0BD72D1EA8A500B01B43F74BD5E4185108796726ECDB1D |
| tools/tests/test_check_claims.py | — | 77B591EEF40D9D4328EE75ABA8D214776AC9BD227803A81ADC7B2910650CBDF4 |
| tools/myshop.pin | — | 80A3EA811DF931ABC7EDF8118BD9FCD8BECE4C581C16F8F682A290D92FCEED11 |

注:myshop/ 是独立仓库,指纹由它自己的 git 历史承担;myshop-source.zip 由 tools/build_zip.py 可复现产出(固定时间戳);变更清单.md 自身除外 —— 文件无法包含自己的最终指纹。

# 变更清单（兼作 CHANGELOG）

- 产出目录：`D:\ext.zhaoliuliu3\Desktop\claude_AI\`（教程副本 + myshop 副本 + 工具 + 本清单）
- 只读源（零写入，经修改时间抽查确认）：`Desktop\ai-workflow\`、`Desktop\myshop_claude\`、`learn_claude\ai-workflow\`、`*_codex\`
- 远程备份：`ai-Engineering-Practice`（私有，分支 `main` / `feat/depmap`）；myshop 用它自己的 `myshop-gate-demo`（公开）
- 共两轮：

| 轮次 | 日期 | 内容 | 提交 |
|---|---|---|---|
| **v2.1 勘误与可用性** | 2026-09-01 | 26 项勘误 + myshop 整备 + Windows 适配 + 基础设施；131 处编辑落在 19 个教程页 + myshop/README.md | `7fe23e1`（`main`） |
| **v2.2 依赖地图** | 2026-09-01 | 依赖真相表裁定 + 按需弹出的地图 + 00-index 静态图重建 + `scroll-padding-top` 修正 | `d381a3d`、`9639178`（`feat/depmap`） |
| **v2.3 页码可点** | 2026-09-01 | 273 处「第 N 页」文字引用改成链接 | `611ad04` |
| **v2.4 重核与重画** | 2026-09-01 | 13 页 Claude Code 逐条重核（撤销一处误改）、16 页 @v7 复核、14/17 两张流程图重画 | `63059f5`、`200b522` |
| **v2.5 示例漂移** | 2026-09-01 | 09/11 页的终端输出与正文项目对不上，如实标注并消除矛盾 | `main` |

- 贯穿原则：23 页结构不动、全书真实输出数字不动、教学设计（monkeypatch / importorskip / checkout 还原法）不动。

---

## 一、事实修正（已定罪错误）

| 页 | 修正 |
|---|---|
| 12-git-undo | `git switch nosuchbranch` 的「(退出码 1)」→ **(退出码 128)**，该行绿色样式 `c-ok` → `c-bad`；页脚修正计数 5→4（switch/restore 实验标签一条与 11 页重复计数，已加括注说明） |
| 22-cheatsheet | 「144 = 128+16 是被手动终止」→ 「POSIX(macOS/Linux)环境下，130 = 128+2 是被 Ctrl+C 中断、143 = 128+15 是被 SIGTERM 终止」 |
| 14-workflow | 步骤 4 自查命令 `git diff --staged --name-only`（该时点暂存区必空）→ **`git status --short`**（同时暴露修改与未跟踪新文件） |
| 02-python-setup | `python_functions=["test_*"]` 配置与 `testnounderscore PASSED` 演示互斥 → 修正框补说明「演示基于默认配置，本页配置已收窄」；回顾表第 37 行加注 |
| 13-claude-code | 「权限模式共六种（含 auto）」→ **官方四种**（default/acceptEdits/plan/bypassPermissions）；auto 是 acceptEdits 开启时的状态栏文案，非独立模式；删「从 auto 起步/按三下」说法及 SVG 中 auto 方框；dontAsk 行保留并加「版本相关」注；页首加「以官方文档为准」免责框 |
| 20-ai-boundary | 「机器能可靠防住的只有三种(3、4、7)」→ 「能检测或部分抑制的有四种(2、3、4、7)」+ 弱门禁限定；连带「剩下四种」→「剩下三种」 |
| 21-automation | CLAUDE.md 示例命令重排为 ruff → mypy → pytest -m p0 → pytest（原顺序与「便宜的先跑」注释相反）；「三条命令」→「四条命令」 |
| 08-break-it | 「实验④全量测试救不了你」加前提「（在 AI 已篡改测试的场景下）」（正句 + 表格两处），消除与同页第五节实测的矛盾 |
| 05-pyramid | 金字塔 SVG「0.06 秒」→「0.02 秒」（与同页实测表一致） |
| 04-static-check | ruff 真实输出框两条诊断配「Found 3 errors.」→ 诊断间补占位行「...(另 1 条诊断略)」 |

## 二、跨页口径统一

| 主题 | 修正 |
|---|---|
| 「删用例能否被机器防住」 | 20/21/22 三页统一为**弱门禁**口径：用例总数门禁防纯删除、防不住「删一条补一条空测试」的语义替换，diff 落在测试文件必须人工读 |
| 用例总数门禁补正式条目 | 18 页门禁清单新增「用例总数 · 不得少于主干(弱门禁)」行（阈值门禁 rowspan 6→7，知识点 182 行「6 条」→「7 条」）；「分母漏洞」对策由「配跳过数门禁」改为「注释/删除式配用例总数门禁、@skip 式配跳过数门禁」（含知识点 185 行联动） |
| 18↔19 名实不符 | 18 页「这三条正是第 19 页配的三条」→「19 页配的是规范+类型+全量测试；换成 P0 100% 是推荐升级版」 |
| 18 页 p0 示例 | 示例测试名改为项目真实存在的 `test_订单真的落盘了_换个连接还读得到`（代码体取自真实 tests/test_order.py），加注「已有测试打标记即可，不需要新写」 |

## 三、引用与编号修正

| 页 | 修正 |
|---|---|
| 19-gate-demo | 「第 18 页那个三阶段落地路径」→ **第 17 页**（经 Grep 核实）；「27 秒是绿的」补口径（建仓推送那次运行；坏代码那次是红的） |
| 22-cheatsheet | 待验证项「第 17 页第七节」→ **第八节**；术语表「合并请求首见 19」→ **首见 16**（含链接 href 同步） |
| 16-pipeline | 删除悬空承诺「第 17 页会对照讲 Jenkins」；两环境 3.66s 对比表加脚注「耗时为 macOS 实测值，Linux 红绿计数一致、耗时未逐字核录」（19 页同款表同步加注） |
| 08-break-it | 「第 5 页那个换连接读回来技巧」→「第 5 页给结论、第 7 页给代码」 |
| 01-overview | 正文五处「第五件事」→「第六件事」（与 SVG 的 ⑤=门禁、⑥=看改动统一）；「管理员推不开」补 enforce_admins 前提；「12 个字符」加注（含前导空格，指 ` --no-verify`） |
| 14-workflow | 全景 SVG「⑤循环/⑥总审」→「⑥/⑦」（与正文步骤一致），连带修正「③–⑤主循环」→「③–⑥」与易跳边「⑤→⑥」→「⑥→⑦」 |
| 07-four-layers-code | 层级编号统一：TOC/标题/对比表中「②接口/③E2E」（与集成重号）→「③接口/④E2E」共 6 处（代码 docstring 本已正确，未动） |
| 02-python-setup | 回顾表末尾两行残渣汇总行（29–31/35–37 区间与描述互换且重复）删除 |
| 00-index | 02 卡片知识点区间补全为「9–23、29–31、35–37」；17/19/21 卡片星级按页内实测同步（★★★★/★★★★/★★☆） |

## 四、限定与背景补充（半句级）

- 02 页：新增「Python 3.9 已于 2025-10 结束官方支持，新装请用当前稳定版；教程代码按 3.9+ 兼容语法书写」背景段。
- 05/06/07 页：「差约 180 倍」结论处各加「（本项目本机实测值，不可外推到其他项目）」。
- 07 页：E2E 代码块说明加「项目文件中另有不用 fixture 的写法二」；06/07 页「(节选)」处加「完整代码见配套 myshop 项目」。
- E2E 维持 `importorskip` 的 1 skipped 基线（「完整 E2E 环境」列为 v3 议题）。

## 五、Windows 适配

- **00 页**前提改为「macOS / Linux / Windows 电脑（命令以 macOS 写法为基准，Windows 对照见第 2 页）」；项目路径指向 `D:\ext.zhaoliuliu3\Desktop\claude_AI\myshop`（附通用写法）。
- **02 页**新增「Windows 读者对照（全书唯一一处）」note 框：`.venv/bin/python` ↔ `.venv\Scripts\python.exe`、激活分 PowerShell `Activate.ps1` / CMD `activate.bat`（并重申写全路径原则）、`touch`/`mkdir -p` 的 PowerShell 对应、`$env:PYTHONUTF8="1"` 乱码处理。
- **22 页**页首加一行指回 02 页对照框。
- **路径去个人化**：全书 70 处 `/Users/daijie27/...` 中 69 处（命令输入行与正文叙述）改为 `cd <你的项目>/myshop` 占位写法、「在 myshop 项目根目录执行」框注或相对路径；**1 处保留**——11 页 L194「真实输出」框内 `Initialized empty Git repository in /Users/daijie27/...`（真实输出记录按约定原样保留）。`.venv/bin/` 写法（90 处）按方案整体保留，由 02 页对照框统一交代。
- 02 页 `touch __init__.py` 前补 `mkdir -p myshop tests/e2e`（原命令在父目录不存在时会失败）。

## 六、myshop 项目整备（claude_AI\myshop\）

- 从 `myshop_claude\myshop\` 复制（源码/测试/配置/README/ci.yml/**完整 .git 历史**），排除 macOS `.venv`、三类缓存、`.DS_Store`。
- 新建 Windows venv，钉住 **pytest 8.4.2 / mypy 1.19.1 / ruff 0.16.3**（与教程实测版本一致）。
- README 更新：教程指向同级 `ai-workflow/`（原指向第一版四份 HTML）、快速开始补 Windows 命令、耗时注明平台、加乱码提示。源码与测试文件**零改动**。
- `.git` 隐私速查（只查不改）：远程为教程 19 页的公开演示仓库 `github.com/Tree202/myshop-gate-demo`，提交作者邮箱为用户本人邮箱——无隐私风险，历史未动（6 条提交含 $→¥ 门禁演示序列）。

### 验收结果（本机 Windows · Python 3.12.10）

| 项 | 预期（教程） | 实测 | 结果 |
|---|---|---|---|
| 基线 | 17 passed, 1 skipped | 17 passed, 1 skipped in 4.69s | ✅（计数一致；耗时属平台差异） |
| mypy | Success…8 source files | 同 | ✅ |
| ruff | All checks passed | 同 | ✅ |
| 实验① 去补零（仅 test_price） | 3 failed, 2 passed | 同 | ✅ |
| 实验② 换符号（全量） | 8 failed, 9 passed, 1 skipped | 同 | ✅ |
| 实验③ 除数 100→10（全量） | 6 failed, 11 passed, 1 skipped | 同 | ✅ |
| 实验④ 边界挪（全量） | 1 failed, 16 passed, 1 skipped（DID NOT RAISE） | 同 | ✅ |
| 实验⑤ 注释 commit（全量） | 3 failed, 14 passed, 1 skipped | 同 | ✅ |
| 五次实验中 ruff/mypy | 始终全绿 | 全绿 | ✅ |
| API 契约 | POST→total_text ¥25.00 / GET 200 / 404「订单 N 不存在」 | 逐键一致 | ✅ |
| 还原纪律 | 每次实验后回到基线、工作区干净 | 确认 | ✅ |

## 七、基础设施件

- `tools/check_site.py`（纯标准库）：链接/锚点/重复 id/导航/标题检查。**运行结果：正式 23 页 0 个问题**；`_v1` 历史页 18 个失效链接（历史存档原貌，不计入、未修改）。
- `myshop-source.zip`：白名单纯净源码包，13 条目、0 禁止项、**14.9 KB**（对比原 myshop.zip 约 150MB 脏归档）。SHA-256 见附录。
- `references/sources.md`：本次修正依据的官方来源与核验日期登记（轻量版）。
- **CI 硬化未做**（`ci.yml` 被教程 18 页逐字引用，改动会破坏教程↔项目一致性）。供 v3 采用的建议：`permissions: contents: read`、`timeout-minutes: 10`、`concurrency: { group: ci-${{ github.ref }}, cancel-in-progress: true }`、pip 缓存、action 钉完整 SHA、`actions/checkout@v7`/`setup-python@v7` 版本号按仓库标签页复核。

## 八、v2.2 依赖关系地图（第二轮）

### 起因

诉求是「每页都能看到我在全书结构中的位置」。评估了三种形态（全图常驻 / 一维轨道条 / 纯文字上下文条），
实测后全部否掉——最便宜的一种也要每页 +1.6KB 且占首屏 72–213px。
最终走了第四条路：**单个外链 JS 运行时生成，按需弹出**。实测确认 `file://` 协议下
外链的普通 `<script src>` 正常执行（被 CORS 挡住的是 `fetch`/XHR 和 ES module），单源维护因此成立。

### 做了什么

- **`ai-workflow/_depmap.js`（新增）**：22 节点 + 39 条直接依赖，运行时生成 SVG。
  - 当前页**从文件名自动识别**，23 页零 per-page 配置；
  - **传递约简**算出 21 条主干边常显，当前页的跨层依赖另以虚线画出；
  - 当前页节点 `--accent` 实心反白 + 放大字号 + 「▼ 你在这里」，且是全图唯一不可点的一个；
  - 弹层标题给出模块名与页码——**「我在第几大段」是原来那张图答不了的**（图里没有分组框）；
  - 导航栏「地图」入口由脚本注入，不改任何页面的 HTML 结构；
  - 复用现有 CSS 变量，深 / 浅 / 跟随系统三态自动跟随；移动端图在盒内横滑，不产生整页横滚。
- **23 页各加一行** `<script src="_depmap.js" defer></script>`（41 字节／页）。
- **00-index 的静态依赖图按同一份真相表重建**：21 条主干边，22 个节点全部包成 `<a>` 可点。
- **`scroll-padding-top` 修正**：模板写死 78px，而导航栏实测桌面 54.3px、375px 手机 100.9px（窄屏折行），
  两档都对不上，点页内目录跳转时标题会被压在导航栏下。改为脚本按实测高度动态设置
  （桌面 65px / 手机 110px，随视口自适应），静态兜底值 78px → 64px（**同字符数，净零字节**）。
- **`tools/build_depmap.py` + `tools/depmap.tpl.js` + `tools/patch_pages.py`（新增）**：
  依赖数据的唯一事实源与两个产物的生成器，可重复执行、幂等、支持 `--dry-run`。
- **`references/deps.md`（新增）**：22 页依赖全表、5 处裁定的理由、主干边推导、改动后的操作步骤。

### 依赖数据裁定（本轮最重要的一件事）

改版前，目录页那张图的 24 条边与 22 页 kicker 声明的前置**有 13 页对不上**，其中 5 处是两边说法互斥。
这些矛盾一直藏在两个不同的位置（图在目录页、前置在各页顶部），没人对照过。经人工裁定：

| 页 | 图上原本 | kicker 原本 | 采用 |
|---|---|---|---|
| 06 | 无入边（孤立根节点） | 02、05 | **02、05**（图画错了） |
| 13 | 12 | 01、11 | **01、11、12**（三者并存） |
| 14 | 09、13 | 01–13 全部 | **09、13** + 文案注「汇总 01–13」 |
| 17 | 15 | 16 | **16**（16 页自称「门禁的前置」） |
| 20 | 17 | 8、13、18 | **08、13、18**（并统一页码格式） |

另 8 处是一方漏写，按并集合并。3 处 kicker 同步对齐：13 补 12、18 补 16、20 的「第 8 页」→「第 08 页」。
完整理由见 `references/deps.md`。

### 成本与验收

| | 实测 |
|---|---|
| 单页增量 | **41 字节**（一行 script 标签） |
| 全站增量 | **13.3 KB（+1.58%）**，其中 11.9KB 是共享的单个 JS 文件 |
| 首屏占用 | **0**（弹层默认不存在于版面） |
| 维护份数 | **1**（对比「每页内联 SVG」方案的 22 份） |
| 逐页验收 | 23/23 通过（headless Chrome 在 `file://` 下核对节点数 / 高亮 / 入口 / 页码 / 模块名） |
| 站点自检 | 正式页 0 问题 |
| 语法 | `node --check` 通过 |

被否掉的「每页内联全图」方案对照：+177KB（+20.8%）、桌面首屏 +461.9px、维护 22 份。

### 过程中修掉的两个真 bug

- JS 严格模式下 `02:` 这类对象键会被当成前导零数字（`node --check` 抓到），已给键加引号；
- 05→06 是**从右往左的同行边**，原连线函数只处理了从左往右，导致线穿过两个节点、箭头被压在节点底下。
  已按左右关系分支处理，`_depmap.js` 与 `patch_pages.py` 两处生成器同步修正。

## 九、v2.3 页码交叉引用可点击

### 起因

评审在剩余项里把这条排在最前:**读者真正迷路的时刻,是读到一半撞见「详见第 05 页」**,
而不是打开页面的第一眼。那一刻需要的是就地一个跳转,而不是回目录页找卡片。
改版前全书 273 处「第 N 页」文字引用**没有一处是链接**(每个内容页只有 3 个站内链接:
上一页 / 目录 / 下一页,唯一例外是速查页)。

### 做了什么

`tools/link_pagerefs.py`(新增)把正文里的页码引用改成链接,三种写法都支持:

| 原文 | 改后 |
|---|---|
| 详见第 19 页 | 详见第 <a>19</a> 页 |
| 前置:第 16、17 页 | 前置:第 <a>16</a>、<a>17</a> 页 |
| 第 17–19 页 | 第 <a>17</a>–<a>19</a> 页 |

只改数字、不动「第」和「页」,读起来仍是原来的句子。链接样式(`a.pg`)放在 `_depmap.js`
注入的 CSS 里,**每页零字节**:去掉下划线、改点线,悬停才变实线加底色——273 个链接
不能把正文搅成一片下划线。

### 安全边界

这些地方一处都没动,脚本按标签深度跳过:

- `<pre>` `<code>`——终端输出与代码,注入 HTML 会破坏演示(实测 `<pre>` 内被误改 **0** 处)
- `<a>`——已经是链接,再套一层是非法嵌套(实测全书嵌套 `<a>` **0** 个)
- `<script>` `<style>` `<title>` `<svg>`
- **自指**——第 05 页正文里的「第 05 页」链到自己没有意义(跳过 44 处,每页 kicker 与页脚各一)
- 非内容页页号(01–22 之外)

幂等:改完的数字已经在 `<a>` 里,再跑一次新增 0 处(实测)。

### 成本与验收

| | |
|---|---|
| 新增链接 | **273 处**,分布 3–43 个/页 |
| 全站增量 | 23 页合计 856,886 → 871,291 字节(**+14.1KB**,含 v2.2 的 script 标签) |
| 每页 CSS | **0 字节**(样式在共享的 JS 里) |
| 站点自检 | 正式页 0 问题(会逐个校验新链接的目标文件存在) |
| 逐页验收 | 23/23 通过,地图与页码链接同时正常 |

## 十、v2.4 13 页重核与两张流程图重画

### 13 页 Claude Code 内容重核 —— 撤销 v2.1 的一处误改

用 5 路代理对照 code.claude.com 官方文档核实这一页的 33 条论断，关键结论亲自抓一手文档复核。
**最要紧的发现是 v2.1 那轮我把这一页改错了**：原文写「一共有六种」并列出 `auto` 行是对的，
我依据审读代理的结论改成「官方清单是四种，auto 不是模式名」，是错的。

官方 `permission-modes.md` 的模式表列出六种：`default` / `acceptEdits` / `plan` / `auto` /
`dontAsk` / `bypassPermissions`；`auto` 有独立章节（后台分类器审查），状态栏文案 `⏵⏵ auto mode on`
与 `acceptEdits` 的 `⏵⏵ accept edits on` 不同。我还一并删掉了两条同样正确的说法：
「Pro/Max/Team 从 auto 起步」（官方原话）与「要按三下」（从 auto：第一下回 default，
再 default → acceptEdits → plan）。本轮全部改回并补全依据。

其余修正：

- **`acceptEdits` 的描述有安全后果的简化**：原写「运行命令仍要问你」，实际它还自动批准
  `mkdir`/`touch`/**`rm`**/`rmdir`/`mv`/`cp`/`sed`（工作目录内、非关键路径）。新手会以为删文件有拦截。
- 循环图重画：加回 `auto` 起点、标出「第 1/2/3 下」、补 `dontAsk` 从不进循环。
- 撤销菜单补条件显示：两个「恢复代码」选项只在该检查点有跟踪到文件改动时才出现。
- 死角清单补官方第四条限制「符号/硬链接路径不还原」，并把「存档状态、数据库、已发请求」
  诚实标注为常识推论而非官方列出项。
- 页脚修正数 4 → 6，核验日期改 2026-09-01。

核实后确认无误、维持原样的：plan mode 那段英文引文**逐字**出自官方（此前担心是转述）；
「100 个还原点」与 30 天保留期均有官方原文；`/review` 是 `/code-review` 别名；
`@path` 在 CLAUDE.md 里的导入用法确有其事（某代理误报「查无依据」，原文在 `memory.md`）。

### 16 页 `@v7` 版本号复核 —— 确认正确

此前审读怀疑「两个组件一年各连跳两级且恰好同为 v7」。查证：`actions/checkout` 最新 v7.0.1、
`actions/setup-python` 最新 v7.0.0，**两者确实都在 v7**，教程无误，未作改动。

### 14 页流程图第二轮 —— 用户复看后指出三处，其中一处是语义错误

- **「凑合」那条线不是水平的**：5b 的中心 y=184、菱形中心 y=176，差 8px 所以拐了个小弯。
  重排三个结果框，让 5b 的中心与菱形对齐在 y=176，连线成为真正的直线（5a → ⑥ 同理拉直）。
- **5a 和 5b 都连到 ⑥ 循环 —— 这是语义错误，不只是画法问题。**
  页面正文写着 5b「改完还是跑一遍 mypy 和 pytest……这是成本最低的一档，**零回退**」，
  即它改完之后要再看一遍改动，**仍停在同一步**；而 ⑥ 的含义是「这一步完成了，做**下一步**」。
  所以只有 5a 该连 ⑥，5b 应回到 ③。已改。
- **⑥ 循环回到哪里看不出来**：把 5b 与 ⑥ 的回程合并成一条共用回程道，终点明确指回 ③，
  并在唯一的交叉处（5c 的红虚线）画了跨线小拱，避免被看成连接。
  说明框同步改为「两条回程都回到 ③：⑥ 是『这一步完了，做下一步』，5b 是『还在这一步，改完再看一遍』」。

### 三路评审后的缺陷修复(P0/P1/P2)

对全部产出做了三路独立评审(主线 23 页 / 姊妹课+入口 / 工程配套),
按定罪清单修复三层:

**P0 · 12 条事实/口径错误(19 处编辑)**
入口页「四道防线」→ 五道,「已被取代」标注改为逐份说明(Output Style 对比
未被主线取代,后继是 kb 04);22 页「六道防线卡」→「六件事速查卡」并加口径
说明;00 页 260 倍 → 180 倍,「两轮核实」分组 14/33 → 25/22(与逐页页脚一致,
确认列同步调整保持行列自洽);退出码 2 两页统一(官方口径「被用户中断」+
本机实测「收集出错同样返回 2」,pytest 官方文档 + 实测双证);13 页 lede
4 处 → 6 处;14 页 kicker 前置与依赖图统一;08 页附注「后来补了」措辞修正、
对比表①补「仅跑 test_price」脚注;03 页发现规则补配置收窄限定 + `*_test.py`、
知识点 40 行文不对题修正;kb/01 步骤编号断裂补第 2 步「核实」;
sources.md §三两条 ✅ 补快照时点注记(生成器 VERIFIED 常量同步)。

**P1 · myshop 与发布物**
E2E 三个真 bug:URL 唯一事实源(conftest 夹具产出,测试消费,删除重复常量);
数据库隔离(MonkeyPatch DB_PATH 到临时目录 —— 此前每跑一次就往项目根
myshop.db 追加一条,已累积 32 条脏数据,连同文件一并清除);importorskip
双守 `pytest_playwright`(只装 playwright 时原来会 fixture not found 报错
而非跳过)。web.py 中文命名保留(与测试层一致的教学取舍),docstring 写明;
`Dict[str, list[str]]` → `Dict[str, List[str]]` 统一 3.9 风格;补转义安全注释。
README 四处同步(分支说明/双基线口径/目录树补 web.py 与 conftest/E2E 段重写)。
myshop-source.zip 重打:12 → **14 条目**(此前缺 web.py 与 conftest.py,
且 e2e 测试是旧版 —— 下载者拿不到第 4 层),SHA-256 见附录。

**P2 · 工具链机器层**
check_layout:退出码纳入巡检结果(发现问题即非零,此前发现 50 个也退出 0);
基线缺失从「静默跳过 + 退出 0」改为显式失败(退出码 2,可用
--allow-missing-baseline 降级但退出码仍非 0);修掉 `_段缓存` 的 `id(html)`
键 bug(CPython 地址复用使 21/23 页拿到上一页的 SVG 区间)。
det_dangling_edge 吞异常改上抛。registry 输入数据入库
(`references/registry_items.json` + `sources_v21_orig.md`),build_registry
路径改仓库相对 —— 453 条断言自此可审计、sources.md 可在任何机器重生成
(已实测重生成,注记保留)。check_site 扫描范围扩到总入口 + 姊妹课 +
第一版存档(此前 14 个 HTML 零工具覆盖;实测三组均 0 问题)。
SHA-256 附录全量重算(此前 22/24 行失效,冻结在 v2.1)。

**评审中定罪但按决策搁置的**:kb 姊妹课的系统性问题(纯 macOS 零 Windows
适配、版本号零出处、allowed-tools 矛盾、知识点计数三套)与 kb 视觉统一 ——
改动会打破「逐字节复制」原则,待用户在「直接修 / 加警示 / 完整重核」中选择。
kb/01 步骤断裂因属「照抄即坏」级别,已先行修复。

### 两课程合并与统一入口(v3 · 最后一项,已完成)

三套内容原本并存、入口分散:23 页主线、`kb/` 姊妹课(Prompt/Skill 主题,8 页)、
顶层五份第一版 HTML。现统一为:

- **`index.html`(新建)** —— 总入口页,与 23 页同一套配色与明暗主题。
  两条主线卡片(主线 23 页 · 姊妹课 8 页)+ 第一版五份的存档区
  (标注「已被 23 页主线取代,以主线为准」)。
- **`kb/`(复制)** —— 姊妹课 8 页 + style.css + app.js,**逐字节未改**
  (SHA-256 抽查与原件一致);原件中的 `kb/ai-workflow/` 系 23 页的逐字节
  副本,不复制,入口直接指向真身。排除 `.DS_Store`。
- **`legacy-v1/`(复制)** —— 第一版五份 HTML,逐字节未改,仅存档备查。

23 页与 kb 各页内部**零改动** —— 入口单向链入,不在课程页里加返回链接。
验证:入口页 7 条链接全部可达;kb 抽两页 headless 渲染正常;入口页截图核对。

### myshop 并回 main —— 被 19 页那道门禁拦住(活演示)

`docs/windows-readme` 并回 main 时,GitHub 分支保护原样拒绝了直推:
`GH006: Required status check "三道检查" is expected` —— 正是 19 页真机
演示配的那道门禁,连仓库拥有者也绕不过。**这本身就是教程结论的又一次实证。**
正确路径是开 PR 等三道检查:本机无 gh CLI,PR 需在网页端创建
(compare 链接 main...docs/windows-readme),CI 预期 17 passed, 1 skipped
(未装 playwright,importorskip 生效)。本地 main 已还原至与远程一致。

### E2E 真跑之后的教程基线同步(v3 · E2E 项收尾)

配套项目补了网页层(`myshop/web.py` + `tests/e2e/conftest.py`,myshop 仓库 `7b6c03a`),
装上 playwright 后基线变为 **19 passed**。教程侧同步遵循一条判断:
**`importorskip` 仍在,不装 playwright 的读者跑出来仍是 `17 passed, 1 skipped`**,
所以全书 macOS「真实输出」框的红绿数**一律不动**;真正改的只有三类共 16 处:

1. **`8 source files` → `10 source files`(8 处,02/03/04/08×4/14)** ——
   mypy 文件数是项目状态而非环境观测值:pyproject 里 `playwright.*` 有
   `ignore_missing_imports`,装不装、什么系统都报 10。
2. **三处现已为假的陈述**:06 页「没有页面」改为「页面只有一条最小下单流程」
   并在目录树补 `web.py`;07 页 E2E 标注「示意代码 · 默认跳过」改为
   「真实代码 · 没装浏览器驱动时跳过,装上就真跑」;21 页 CLAUDE.md 示例
   文件清单补 `web.py` 一行。
3. **08 页新增附注框(Windows 实测)**:装驱动后五次实验的新红绿数
   (② 10f/9p、③ 8f/11p、④ 1f/18p、⑤ 3f/16p),并记录关键发现 ——
   **实验④⑤里两条 E2E 全绿**:⑤ 的静默数据丢失,页面显示的是 create_order
   返回值,开真浏览器点完整流程也抓不到,只有第二层「换个连接读回来」能抓住。
   金字塔最贵那层上了,结论不变:上层全绿不代表下层没问题。

顺手修掉 06 页目录树的层号错:`test_api.py` 原标 ②、e2e 原标 ③,
与 07 页权威口径(接口=③、E2E=④)矛盾,已改为 ③/④。

另核实:**Python 基线方案 B 无需再做** —— 02 页第八节开头已有
「3.9 已于 2025-10 结束官方支持,新装请用当前稳定版」说明(v2.1 第 25a 项)。
`references/sources.md` 为 2026-09-02 快照,其中 E2E 相关条目(「示意代码」
「17 passed 基线」)记录的是快照时点的教程原文,本轮改动晚于快照,下次重跑
`tools/build_registry.py` 时自然更新。

### 版式与结构自动巡检 `tools/check_layout.py`

补上 `check_site.py` 的洞:它查链接、锚点、重复 id、导航、标题 —— 全是文本层面的,
**一条版式规则都没有**。而用户先后肉眼发现的两处缺陷(七步图三个节点没出边、
`.chk` 清单竖排)恰好都是版式类,自动检查一条都没报。

六条规则,归档在 `tools/layout_rules/`,各自可单独 `python det_xxx.py` 自测:

| 规则 | 抓什么 |
|---|---|
| `dangling_node` | SVG 里有入边、没出边的非终止节点 |
| `dangling_edge` | 箭头指向空处(端点附近没有任何节点) |
| `grid_text` | grid 容器里混有裸文本节点 |
| `overflow` | 元素坐标跑出 viewBox,渲染时被裁掉 |
| `collision` | 文字压在别的方框上、方框互相重叠 |
| `refs` | `url(#id)` 指向不存在的 marker、重复 id |

**准入门槛是真值验证,不是自说自话。** 只读原版里那两处缺陷还在,所以每条规则都必须
「在原版上报出、在已修版上不报」才算数 —— 一个永远返回空的检查器也能得到「0 个问题」。
`check_layout.py` 每次运行都先跑这道自检,不过就以退出码 1 退出:

```
通过  七步图 5b/5c/兜底 没有出边   原版 3 条 / 已修版 0 条
通过  箭头停在步骤 7 方框外        原版 1 条 / 已修版 0 条
通过  .chk 清单竖排               原版 7 条 / 已修版 0 条
[正式页] 检查 23 页 -> 0 个问题
```

**调参是实测反推的。** `dangling_edge` 先统计已修版 23 页全部 95 个带箭头端点到最近
节点的距离(0px×28、2px×9、4px×49、6px×6、8px×1),而真值缺陷是 10.8px 且垂直偏移
10px —— 容差 8px 正好卡在两者之间。`overflow` 做了变异测试:画布砍 8px 一条不报、
砍 40px 全部报出,并用浏览器 `getBBox()` 对 875 个元素做过独立校验。

**四条检出全部经对抗复核判为误报,一条都没修** —— 复核代理的默认立场是「这是误报」,
必须亲自读到原文才能翻案。结论反过来收窄了三条规则(见 `check_layout.py` 里 `收窄()`
的注释,那是收窄判据,不是白名单):

1. **导航图/依赖图豁免「无出边」** —— 00-index 阅读地图被报了两个断头节点,但
   `_depmap.js` 的 `PRE` 里没有任何一页以 19 为前置,它就是 DAG 的合法叶子。
   照检出去补边,反而会让 SVG 与唯一事实源冲突,**制造出一个真的缺陷**。
2. **`grid_text` 只报轨道写死的 grid** —— 07 页 `.term-bar` 是 flex,裸文本被拆开后
   只多出 7px 间隙,复核代理真机逐节点量过,与正常空格差 0.4px,肉眼不可分。
   致命的是 `.chk li` 那种列数写死的 grid:多出的匿名项会落进**错误的列**。
3. **丢掉 figcaption 数量比对** —— 22 页那张收尾结论卡的说明文字在红框**里面**,
   渲染正常。该判据零真阳性、已产生一例假阳性。

整合过程中真值自检**当场抓出了我自己的一个 bug**:导航图豁免判据查的是整页 HTML,
而 01 页正文有 43 个 `.pg` 页码链接(`href="12-git-undo.html"`),整页被误判成导航图、
真值缺陷直接被吞。已改成**按单张 SVG 的行号区间**判定。

### 01 页七步流程图补全 + .chk 清单布局(两处原版缺陷)

**两处都是原版就有的**,不是本轮改出来的 —— 已对照只读原件确认。

**一、01 页七步流程图:5b / 5c / 兜底 三个节点没有出边。**
原版只画了 11 条边,其中只有 5a 有出边(去步骤 7、回步骤 3)。
另外三格画完就断了,读者看不出「让它改」之后该去哪。已补三条回程边,
口径与 14 页流程图一致:

- **5b 小问题 → 回步骤 3**(还在这一步,改完再看一遍),青色虚线走右侧内道;
- **5c 方向错了 → 回步骤 1**(撤销后重新要方案),红色虚线走右侧外道;
- **兜底 全烂了 → 汇入 5c 的回程道,同样回步骤 1**,汇入点画了圆点。
  两者差别只在退多远,终点相同,如实画成同一条道。

配套调整:画布 900×540 → 960×600;步骤 7 与「推上去」下移 40px 给回程道让位;
5a 去步骤 7 那条补上「全做完」条件标签(与回步骤 3 那条的「步骤 6 · 循环做下一步」
构成一对);5b 横道在兜底下行线处画了跨线小拱;新增三个配色箭头
(原来绿线红线都配灰箭头)。图注补上三个分支各自去哪。

**二、`.chk` 验收清单竖排成一字一行。**
模板里 `.chk li{display:grid;grid-template-columns:26px 1fr}` ——
`display:grid` 会把 `<li>` 的**每个子节点**都变成网格项,包括 `::before`、
每个 `<code>`、以及每段匿名文本。于是

```html
<li><code>.venv/bin/python --version</code> 能打印出版本号</li>
```

被拆成两行:`[□][代码]` 占第一行,「能打印出版本号」落进 **26px 宽的那一列** ——
一个字一行。只要 `<li>` 里含 `<code>` 就必然错位,02 页七条里有六条中招。

改成相对定位 + 绝对定位的方框,行内内容恢复正常流动,**不必改任何 HTML**:

```css
.chk li{position:relative;padding-left:26px;margin:9px 0}
.chk li::before{content:"±0";position:absolute;left:0;top:0;...}
```

该 CSS 内联在 21 个页面加模板里,共 22 个文件同步修正。
(同模板的 `.step` 也用 grid,但它的子元素是两个真 `<div>`,用法正确,未动。)

### 断言来源总登记(v3 议题之一,已完成)

`references/sources.md` 从「本轮 26 项修正的依据」扩成**全书断言来源登记表**,
由 `tools/build_registry.py` 生成 —— **表格直接来自数据,不经模型转写**。

- **清点 453 条**可证伪断言(命令行为、配置默认值、版本号、官方文档转述、
  第三方服务行为、实测数字);教学口语、类比、价值判断不登记。
- **与教程自称的 193 条既非同一批,也非其子集或超集** —— 教程从未列出那 193 条,
  只标了出错的 47 处,原始清单无从复原。条目边界、拆分粒度、纳入门槛都不同,
  两个数字对不上是必然,文档开头已写明。
- **三个标记体例的坑**(已逐页 grep 核实,写进文档):
  🔄 = 经核实修正(源码 90 处 / 12 页,页脚声明 47 处 —— 同一处常在正文和
  知识点回顾表各标一次);🔥 = **重点强调,不是修正**(52 处 / 14 页);
  ⚠ **绝大多数是警告框图标,不是待验证** —— 全书真正的待验证**只有 1 条**,
  在 17 页第八节。
- **风险分级**:高 131 / 中 147 / 低 175。高风险高度集中 —— 13 页一页占 41 条
  (几乎全是产品 UI),而那一页已对着官方文档逐条核过。
- **第三节与第四节严格分开**:只有真查过一手出处的才进「已独立核实」,
  其余一律「仅登记、未独立复核」,「教程自称依据」一栏原样转述教程说法。
- 附「维护建议」:按类别给出复核周期,并记下这轮踩出来的三条规矩
  (不要转手核实、改之前先确认原文是错的、不许把未复核写成已核实)。

生成过程中修掉两个自身 bug:`marked` 归一时子串匹配把代理写的
「非『待验证』标记」这种**否定句**误判成待验证(4 条误报);
以及重跑时把上一版登记表整个吞成附录(文件从 154KB 涨到 307KB)——
「旧内容」改从 git HEAD 的原件读取。

### 14 页第三轮 + 05/15 页 Unicode 码位

- **⑥ 循环补上条件分支标签**：⑥ 有两条出边，原先只有「全做完 → ⑦」带标签，
  回 ③ 那条没有条件说明，读者看不出它是「还没做完」的那一支。已在 ⑥ 的回程出口加
  「还没做完」标签，与「全做完」构成一对；说明框首行改为
  「⑥ 是个岔口：方案还没做完 → 回 ③ 做下一步；全做完 → ⑦ 总审。5b 也回 ③，但它是『还在这一步』。」
- **05/15 页的圆圈零是未分配码位**：两页共 8 处用了 `&#9311;`（U+245F），
  该码位落在 OCR 块的未分配区，**多数字体直接渲染成空方块 ☐**（已渲染对照确认）。
  正确码位是 `&#9450;`（U+24EA ⓪）——14/20/22 三页本来就用的这个，全书本身不一致。
  已统一：05 页 5 处、15 页 3 处全部替换，现在五个页面口径一致。

### 14 页与 17 页流程图重画（第一轮）

两张图都有画法 bug，不是审美问题：

**14 页七步流程图**
- `5c → ①` 的边终点写成 `L 271 16`，而 ① 方框上边缘在 `y=20` —— **箭头穿过整个方框**从顶上冒出来；
- `④ ↔ 菱形` 的连接线从右往左画（`x1="348" x2="332"`），**箭头方向反了**；
- 菱形左顶点 `x=336` 压在 ④ 的右边缘 `346` 上，**重叠 10px**；
- `y=108`/`110`/`128` 三条横线贯穿全宽挤在同一条带里，是「看着乱」的主因。

重画后：菱形缩小到 112×80 且与 ④ 留出 28px；两条回头路（⑥→③ 做下一步、5c→① 撤销重来）
全部移到下方独立走线，用灰实线与红虚线区分；`5c→①` 绕行左侧与顶部，**全图零交叉**。
顺带修掉一个我自己的疏漏：第三条 bullet 在 `y=530` 而 viewBox 高 524，文字掉到画布外，已扩到 546。

**17 页三问判别图**
- 菱形过宽：问 2 右顶点 `620` 与问 3 左顶点 `610` **重叠**，问 1 左顶点 `160` 压在起始框上；
- 连接线画在菱形**内部**（如 `168→176` 落在 `160..380` 的菱形里），渲染出来像**箭头指向左**；
- 问 3 的「否」是斜折线 `M 800 86 L 830 120 L 750 190`，正好**穿过「是质量门禁」框**。

重画后：菱形缩到 116×72，一条直线主路（拿到环节 → 问1 → 问2 → 问3 → 是质量门禁），
三条「否」改为竖直下落到对应结论框，用 `--bad` 色区分，**零交叉零重叠**。

## 十一、v2.5 09/11 页示例漂移

审读发现这两页的「真实输出」与正文叙述用的不是同一个项目,读者照着敲会得到完全不同的东西。
**处理原则:真实输出一律不伪造,只如实标注它出自哪个项目,并让命令与输出不再互相矛盾。**

### 11 页 git 基础 —— 实际比审读说的更乱

全页七个终端框,实测掺了三个项目:

| 命令 | 输出实际来自 |
|---|---|
| `git init` / `git status` | myshop ✓ |
| `git diff` | 一个用 `app.py` 的项目 |
| `git diff --stat` / `git log --oneline` / `git log --stat` | 一个用 `report.py` 的三步练习(读取分数 → 计算平均分 → 输出报告) |
| `git commit` | 提交信息是 myshop 的,但分支显示 `[main …]` |

而这三条 `git diff --stat` / `git log` 的命令行都写着 `cd <你的项目>/myshop`。

改法:

- 在「六、看改动」开头加一个说明框,讲清这一节和下一节的输出来自一个更小的练习仓库,
  以及**为什么**要换(myshop 一次改动牵扯几个文件,diff 长到看不清重点;这一节要讲的是怎么读 diff)。
- 把那三条命令的 `cd <你的项目>/myshop &&` 去掉,改成项目无关的写法,命令与输出不再打架。
- **`[main b27d579]` 那处不删不改,改成一个教学点**:新增警示框指出方括号里是当前分支名,
  你若照第二节做过 `git switch -c feat/…`,这里应该显示自己的分支名;
  看到 `[main …]` 就说明分支没开成,停下来先补上 —— 直接在 main 上攒改动,
  正是第 12 页那些回退死角最难收拾的场景。

### 09 页局部 vs 全量 —— 两个「全量耗时」并存

页面已有说明框交代了复现项目的函数写法与 myshop 不同,但**没交代耗时口径也换了**:
开头讲「全量 3.66 秒」(myshop,18 条测试、含真启 HTTP 的接口层),
末尾算账用「全量 0.03 秒」(复现项目,4 个文件 7 条纯函数测试)。两个数字都真,量的不是同一个项目。

改法:说明框补一段讲清两个数字各自的来源与差别;对比表和成本账里的 `0.03 秒`
各加一行小字标注来源(并在成本账处并列 myshop 的 3.66 秒),避免被当成同一个项目的数字。
页脚说明同步更新。

## 十二、遗留（下一轮或 v3）

已销项：`feat/depmap` 已合并进 `main`（`1c55ccc`）；全书断言来源总登记已完成
（`9186265`，见 `references/sources.md`）。

**尚未做的三项 v3 议题**（都是「重录级」改动，需先决策）：

1. **两课程合并与统一入口** —— 顶层四份第一版 HTML、`kb/` 那套姊妹教程、
   现在的 23 页，三者并存且入口分散。
2. **完整 E2E 环境** —— 补一个最小网页让第四层测试真跑。
   **会改变 `17 passed, 1 skipped` 这条贯穿全书的基线**，02/06/07/08/18/19 多页要同步。
3. **Python 基线升级** —— 教程实测环境是 macOS 上的 Python 3.9.6（3.9 已于 2025-10 停止支持）。
   升级意味着重录一批终端输出。

**新增的两项待办**（本轮工作中浮现）：

4. **高风险断言复核（131 条）** —— `references/sources.md` 第五节已按类别和页码排好序，
   13 页一页占 41 条。这是一份可以直接照着做的清单，建议每半年跑一次。
5. ~~版式与结构的自动巡检~~ —— **已完成**,见 `tools/check_layout.py` 与
   `tools/layout_rules/`,六条规则均通过真值验证。

## 附录：被修改文件 SHA-256（前=只读原件 / 后=claude_AI 产出）

| 文件 | 改前 SHA-256 | 改后 SHA-256 |
|---|---|---|
| ai-workflow/00-index.html | CC27E161CB048162F5D937528096DDF5EC20CB3F23EC801634E1BF5035DF1FB7 | C318590FD8C677F2FFFFF76D9172A9D26621F63000BC7CE63FE1437B752868D3 |
| ai-workflow/01-overview.html | C5615CE3E10E7E5F8C4A35954E414AE2F361AF81732C113B2AD0FF374D24C74B | 4167A72AC07112B807A0E92E348789AFF7FF755D5BB3B94467B06B1C50B9A5F1 |
| ai-workflow/02-python-setup.html | 93B68B4191575889E5B3A0E2AB7FE250B5F5079255AC2C213DF65AAA04E5D70D | 5CA36FEB8B060135C9A2AC0733A36C73CC46366B1B19DD5E53B28ADD66C55660 |
| ai-workflow/03-test-basics.html | 9FF16CD68930945C323CDBF8B7168280881FB0C7E575F4C43A9327A1F70F1E0A | 10D6579F21CF2141E1F90C9BC6A392CA7FE309786CA2F3EF2381C8F6ABA99B98 |
| ai-workflow/04-static-check.html | 897B6FC7D4C6D9019C5A9FD495EF15A0DA07BB51E152C46BEB9CAB17218C34FC | BB5CCB00F47BAC4C60135F6DD108B3CDA43E414CB7E9E9AA632921654409D8AD |
| ai-workflow/05-pyramid.html | 4AB1F3BACB4224A3D1ABD12D1F5C1C13AC56F57FDCFA890AF72F6A7D969E0498 | E3E162318394B9EF624265A056409579200C96468F0F9075C3DD74C4E43F737A |
| ai-workflow/06-myshop.html | 2A45A632568354CA59199ED15570A4B7461E8927B3CC07251187A55E018328E2 | 1AE46AE57D0E9723AAF1F5A73011594B4C287D60DE18A8DF951B169AFEDF2B56 |
| ai-workflow/07-four-layers-code.html | E3F7A8A53AD984558BCED856ED45CAFC73829138556180B16E3BEC17EB8124AD | 20BA6742DDC7E3ECF87F909732C6AE58AA497E5784057425D476DA619E7CDA96 |
| ai-workflow/08-break-it.html | E3EFF6AB840040FECB42D5A0C5A25DEBF33C573C29C40128E8D5E32AB4A7E9A6 | 174AB02CC50E441005F7047787D06EB7FA96868D32B5B9D57C5E87CAE638A30A |
| ai-workflow/09-partial-vs-full.html | 62B797A3FDBAED997EF30966313697A8C73DDA252CBD6E0402C5EF77561894E6 | D3F47D2085F64E8DA2DDD48E1CF0BE5204941B470423927A717624BEB67C70BA |
| ai-workflow/10-flaky-e2e.html | 5334B2AC143A2CB8BC0387DA96F6559E78478CA53193D66C85A71CF41C8E1BB0 | 3D01C0A4EAE6936450A7B896D0E788378EBF1382834022A5E83F3B9A1DE6FD71 |
| ai-workflow/11-git-basics.html | 5BBC9C21262EF75F7FD21A3CEC4582E63424B845FBCF01116AEDFEEFADE7CE33 | C99E6F720A64F6A5635C4AB249A00F13B76209935F774DA62739C016E7A2D33B |
| ai-workflow/12-git-undo.html | 92D83C188737A672BB710D22BF96A3B1980D7C6BD824133CF1C74B21BB90F350 | C3615C40776D976EC5F7DFFDF2477C916D95B5CBB496FF8701B4372A8A0A50DF |
| ai-workflow/13-claude-code.html | 536DB94F84174E8615D3825D44B98C6793B4F845BACA2ABCA0A6597ABDC3CDE2 | E62A41EEB1C07B63B637210A2D3E765D44892836F65EFFC4E8AF40413F3452D2 |
| ai-workflow/14-workflow.html | BFD09D1067D62BF0F9E4C1B895EB7887680A7CC66DC1C8BD4A22147FF6CB2894 | 0568E654D9157CC5565A12CCD1E09DCEF4425C62AC0EF024545AADFFE78015FF |
| ai-workflow/15-team-roles.html | 756E6DCF328EDEF2C631F7603B900199C598B761A04F844E04D76D49F0765717 | 6EF4615C35610FCA56AF94A80E99443291793B7D6330078C0F13A6EAA312E88E |
| ai-workflow/16-pipeline.html | 0E258A42EDE278F91FAF96DDF58C704E938E0A0972639244995E09BFDCB0DC2E | C18E7DB3F62E8EE3BB0E40986BE31274E8CA39A8D3AB28C99BA8C9FE36CD58C3 |
| ai-workflow/17-quality-gate.html | 6BB9810F229C696064C017BE428F96E7D5006C66D447E1ABE61FEF4EEC3E4A1A | DA7A4902D21E3F576F6845D37E0A0F1DEC48BDB243B74BF3B5379DC1EDA88CD8 |
| ai-workflow/18-gate-items.html | 49A72C7019A20D936367E1B8928159AB1D09BEC472933BB9C99838158A770E50 | 1F1BB313F14B842DB5C97BD3157E099BB76823934700EE19168989CB1A669ADC |
| ai-workflow/19-gate-demo.html | 64CA82203E7396664BD9B23469B07D85010BD580BEBC662E64EB6C3FE5BC7CDB | D30F3A894ED6CDAEF745C484F59E4AF7614A35ABAD199454045B1113CF7107DA |
| ai-workflow/20-ai-boundary.html | 01AB08A21B821EF29CC78169906EF180934C618F56E499433D054880C7FE0638 | F01A92FC922CCEC5E1EBA47BD26FB742467717BBFD17C01209893510DFA0351C |
| ai-workflow/21-automation.html | 702A24ED7490D930FA38D0ED5298F5A88D8C718F35B078A89378E9A4B5CF4BDB | 96A92EA03FB163D1C2227FBA7C856BF1348D74C203A6084BDBC103775A602297 |
| ai-workflow/22-cheatsheet.html | CAD57ACDE34977858D9CF76B0C8FF4FE6A6713B11827CB2E395E24D2B9AA0101 | A0DE2A72D4193CB155FCF2D742C675E0F0E426AF8A50D655BFAD66B0C4C05144 |
| myshop/README.md | 7E5740F1946ABAA7E4BA05C43F9AA285F45E2BFE1DE018A3C23923BB19E421B2 | 67AF6B1FC80AD6E4D5FC48FDE01BAB4A9152FB2F067C606D7E2BCBD030073F5C |

新增文件：

- `ai-workflow/_depmap.js` : 575CD312D5D55B5216622F7817E2C98D7579F625967BBF3F612885944423D136
- `tools/build_depmap.py` : 09288624A6B9567036E3932BC5FB25C3693C152123AAD30AAD68BB07C7B208F2
- `tools/depmap.tpl.js` : 770FD4062E71281EBF2582CE1CAAC16AD4FF10C11128C49454A3CD4F997DFBB0
- `tools/patch_pages.py` : 70861B6B91C75B16D817A298C50F38F8F23971DB8BB799B0420EC552C5CB7C09
- `tools/link_pagerefs.py` : 2445AC9623C18772AE7B24D595D8BA7581AB800FD358CA9C8EC8A36D83575E2A
- `tools/check_site.py` : 2D80B480165E42CEAF3453672B17D04B295C5B306D0F1CA60579CB1D0D54F669
- `references/deps.md` : 798C2003C8CC482D8CC23342F2103CC400E3C240C9D4C4DA671E6ED0B54340E2
- `references/sources.md` : 3CEA8B8310EB5B83E88C65C735B1FC3B57C5FBA3F0D1347B66712BFC4E8E2EA4
- `myshop-source.zip` : 77CFEC63FEE19B878CE741F30D2598540025477E861DCE3AFACD5895A29BAA20

`_v1/`、`_build.sh`、`_shared_tail.txt` 未触碰；`_shared_head.txt` 仅改 `scroll-padding-top` 一处。
